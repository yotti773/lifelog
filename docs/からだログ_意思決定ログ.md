# からだログ 意思決定ログ

作成日: 2026-07-06
関連ドキュメント: からだログ_要件定義書.md、からだログ_画面設計書.md

要件定義書・画面設計書に混在していた「決定済み論点(経緯の記録)」を本ログに集約する。**本ログは経緯の記録であり、現在の仕様の真実の情報源は要件定義書・画面設計書の本文**である(本文と食い違う場合は本文が正)。新しい決定は末尾ではなく該当テーマの節に追記し、日付が分かるものは日付を添える。

## 全体方針・フェーズ

- **2026-07-06: アプリの最終形を「必要なことを入力するとパーソナルなコンサルティングができるアプリ」と再定義した(フェーズ3)。** 記録(現状把握)だけでなく「記録 → 評価 → 助言 → 行動修正」のループを回すことを長期目標とする。フェーズ3の機能構成は以下で決定(要件定義書4.7章):
  - 採用: 身体プロフィール+目標カロリー自動計算(#43)、実測TDEE推定(#44)、週次レビュー(#45)、記録率の可視化(#46)、PFC目標値(#47)、AIコーチング(#12)
  - 見送り: 活動データ(歩数・運動消費)の手入力欄の先行実装 → 実測TDEEの逆算(#44)があれば活動量データが無くても収支評価は成立するため、Garmin/Google Fit連携(#11・#13)に統合し単独では実装しない
- **2026-07-06: AIコーチングの実現方式** → 数値の計算・集計・安全判定はすべてコード側で決定論的に行い(`WeeklyDigest`)、AIには解釈と言語化だけをさせる構成で決定。モデルは低コストな軽量モデル(Gemini Flash-Lite級)を既存のWorker中継で呼び出す。詳細はAIコンサルティング設計書を参照
- **2026-07-12: 週次レビューへの筋トレ・水分・日記の統合(#103) → 3つとも統合することに決定・実装済み。** 筋トレ(記録日数・種目数・総セット数)・水分(記録がある日の平均摂取量・目標達成日数)は`WeeklyDigest`の週サマリーとして追加し、画面表示とAIコーチングの入力の両方に使う(活動(#82)と同じ「記録が無い週は省略」方式)。日記は週内の本文・気分タグを週次レビュー画面にローカル表示する
  - **あわせて「AIコーチングにおける日記本文の扱い」(#12の残論点)を決着**: 設定画面に「日記の本文をAIに送る」トグル(`Settings.sendDiaryTextToAi`、デフォルトOFF)を新設し、ONの週だけ本文を`WeeklyDigest.diaryEntries`としてGeminiに送る。常時送信案は「本文はデフォルトで外部AIに送らない」という設計原則(AIコンサルティング設計書7章)を崩すため不採用、画面表示のみ(AI非連携)案はAIコメントを生活背景に沿わせられないため不採用とし、オプトイン方式を採用した。日記カードには本文がAIに渡る状態かどうかを常に注記する
- **2026-07-13: 週次レビューへのクロス分析追加(#112) → 「睡眠×食事」「気分×食事」「飲酒×食事」の3軸で実装。** 蓄積済みの複数系列(体重・食事・日記・Garmin活動)を掛け合わせ、記録コストを増やさずに既存データの価値を高める。実装時の決定事項:
  - **飲酒を分析軸に使うため、`DiaryRecord`に飲酒タグ(boolean・気分タグと同列の任意入力)を追加した。** 飲酒はカロリーとして食事記録に載っていても「飲んだ日」というフラグにはならず、日記本文の自由記述はAI送信オプトイン制のため集計に使いにくい。晩酌の有無は1日単位の事実で日記の1日1件モデルと一致するため、日記側に持たせた。トグルOFFは「飲酒なし」ではなく「記録なし」(undefined)として扱い、集計の「それ以外の日」もタグの無い日全体として表現する
  - **睡眠×食事のペアリングは「翌日」ではなく同じ日付にした。** Issue本文の候補は「睡眠時間×翌日の摂取カロリー」だったが、Garminの睡眠時間は「その日の朝までの夜間睡眠」を指すため、同日の食事がまさに「睡眠不足明けの日の食事」に相当する(日付+1でペアリングすると1日ズレる)
  - **見せ方は断定を避けた事実の提示のみとし、解釈はAIコメントに委ねる。** 週単位(最大7日)はサンプル数が少なく「相関」とは言い切れないため、画面は「睡眠6時間未満の日が2日あり、その日の平均摂取カロリーは…」形式+「差は偶然の範囲のこともある」注記に留め、AIプロンプトにも「相関・因果を断定しない」「飲酒を責めるトーンにしない」制約を追加した
  - 候補にあった「歩数・消費カロリー×体重変化」の軸は見送り(実測TDEEとGarmin計測消費の突き合わせ(#82)で既に同じ目的をカバーしているため)
  - シートの飲酒列は日記記録タブのID列(E)の後ろのF列に置く(既存シートのID列を動かさない。種目マスタの部位列(#104)と同じ方針)
- **2026-07-14: 月次レビューを追加した(#114) → 週次(#45)と役割を分け、俯瞰専用の内容に絞って実装。** 週次は「今週の達成→来週のアクション」で行動を変えるレビュー、月次はペースの加速・減速や目標までの残距離を見る俯瞰、と役割を分けた。実装時の決定事項:
  - **月の定義を「その月に日曜(週の終わり)が含まれる月曜始まりの週の集合」とした(4〜5週)。** 暦月は月曜始まりの週と端が揃わない(月初・月末で週が分断される)ため、週を分割せず日曜の属する月へ丸ごと割り当てる割り切りを採った。これにより全ての週がちょうど1つの月に属し、隣接月と重複・欠落しない。代償として月次の期間境界は暦月とズレる(例: 2026年7月は6/29〜7/26で、7/27〜31は8月レビューに入る)が、週平均体重・週次TDEE逆算という「週を最小単位とする既存の集計」をそのまま再利用できる利点を優先した。実装は`src/lib/date.ts`の`weekStartsOfMonth`
  - **UIはサブタブを増やさず、レビュータブ内に「週/月」の`SegmentedControl`を置いた(Issue本文の方針どおり)。** 推移画面のトップは「グラフ/履歴/レビュー」のまま、レビューの中で週次・月次を切り替える(タブの階層を深くしない)
  - **表示内容は月次ならではの項目に絞り、週次の全セクションは持ち込まなかった。** 採用: 週平均体重の推移(折れ線4〜5点)・目標へのマイルストーン(今月ペース維持時の到達見込み)・月間実測TDEE(週単位よりブレの少ない安定値)・カロリー/記録率・月窓クロス分析・月次AIコメント。見送り: PFC平均・活動・筋トレ・水分・日記(週次の粒度で足り、月次の俯瞰では冗長なため)
  - **月間の体重変化・ペースは「記録がある最初の週と最後の週の週平均」で計算した。** 端の週に体重記録が無くても、記録のある週同士の差を週数差で割ることでペースの意味を保つ(単純な暦月端の差にしない)。体重記録のある週が1週以下ならペース・見込みはnull(比較できないため)
  - **月次TDEEは月内の全有効週の週次逆算値の平均にした。** 週次レビューの「直近3週の平滑化」より窓が広く、目標カロリー補正提案(#44)の信頼度を上げる。週ごとの逆算値の幅(最小〜最大)も併記してブレを可視化する
  - **クロス集計・クロス分析カードは週次と共有した(`buildCrossAnalysis`を期間パラメータ化、`CrossAnalysisCard`を共通コンポーネント化)。** #112の集計をそのまま月幅で再実行する。月次は標本数が多いぶん注記を「傾向の目安」に変えるが、因果を断定しない原則は週次と同じ
  - **記録率低下フラグは「経過日数」を分母にした(月全体の総日数ではない)。** 進行中の当月は今日までに経過した日数、完了済みの月は総日数を分母に「7割未満」を判定する。月全体を分母にすると、当月の序盤で毎日記録していても常に「記録不足」警告が出てAI判定も歪むため。データ不足フラグ(記録7日未満)は絶対値のままにし、月序盤にまだ評価に足るデータが無いことは正しく伝える
  - **AIコメントは週次の仕組み(Gemini中継+キャッシュ)を月キーで流用した。** 出力契約(`WeeklyAdvice`)は共通で、winsを「今月の良かった変化」・actionsを「来月の重点」として使う。キャッシュは`MonthlyAdviceRecord`(月キー)。プロンプトだけ月次版(`worker/monthlyAdvice.ts`)にし、`weeks`系列からペースの加速・減速を読ませる。月次digestに日記本文は含めない(週次のオプトインは月次には設けていない)
- **2026-08-03: フェーズ4までの実装が一巡したことを受け、以降の発展方向を検討した。** 日常運用は安定して不満が無い一方、Open Issueが運用改善寄りの数件だけになり「作るものリスト」が尽きた状態だったため、伸ばす方向を洗い出した。既に対応不要としている方向(マルチユーザー化#67、Google Fit連携#13、あすけんからのデータ移行#11、デプロイのテストゲート#18)は検討から外した。挙げた5方向と結論:
  - **入力コストの削減** → リマインド通知(#158)・食事のテキスト一行入力(#159)を採用。LINE Botの再合流(#160)は対応不要
  - **評価・助言のループの短縮** → 日次コーチング(#161)を採用。#119(残りカロリー・PFC予算からの食事提案)を統合するかは#161内で判断する
  - **対話できるログ** → #162を採用。要件定義書1章に掲げたNotebookLM代替の、現在の一方通行のAIコメントでは満たせていない残り半分にあたる
  - **目標日(10/31)以降の長期視点** → #163を採用。維持モード(#116)と組で扱う
  - **土台の耐久性** → 復元の実地リハーサル(#164)を採用。BYOK配布の検証(#165)は対応不要
  - **着手順は #164 → #161 → #162 とする。** #164は手を動かせば結論が出るうえ、そこで判明した「週次・月次AIコメントが復元されない」問題の重さが、#161(AIコーチングを日次にすると保全対象が週1件から日1件に増える)の判断材料になるため
  - **#161と#162は同時に検討する。** 両者は「`WeeklyDigest` 以外の粒度でAIに何を渡すか」という同じデータ契約の問題を別角度から扱っており、片方を先に決めるともう片方の設計が縛られる。特に#162を固定の質問テンプレート方式にするなら、#161の日次コーチはそのテンプレートの1つとして吸収できる可能性がある
  - 発信(note/Zenn)の方向はIssue化せず、記事の企画・進捗ログを引き続き真実の情報源とした(Issueに写すと二重管理になるため)。当時の置き場は `articles/README.md`、2026-08-10以降は `content-ops` の `README.md` と `docs/進捗ログ.md`
- **2026-08-08: 残りIssueの着手順を見直し、#174 → #159 → #161/#162 → #158 → #116/#163 とした。** 上記2026-08-03の着手順(#164 → #161 → #162)は#164の決着で先頭が空き、その後に#174(活動量低下の検知)が立ったため引き直した。判断軸は目標日(10/31)までの残り約12週に対する効き方:
  - **#174を先頭に置いた。** 実測TDEEの下落が実際に起きているのにアプリが一度も知らせておらず、しかも目標カロリー提案がBMRへクランプされる結果「さらに食べる量を削れ」としか読めない状態になっている(必要なのは消費側の回復)。判断を誤らせる方向に働いているため放置コストがマイナスで、かつ**遅れるほど残り期間が減って価値が落ちる**。実装も既存の週次ダイジェストの純関数にフラグを足す範囲で、新規インフラを伴わない
  - **#159を#158より上げた(ラベルも 優先度: 中 → 高 に変更)。** どちらも「記録の継続性」への投資だが、入力コストの削減(#159)は**記録がある日すべてに効く**のに対し、リマインド通知(#158)は**忘れた日にしか効かない**。加えて#158はSWの差し替え・KV namespace・cron・VAPID鍵の手動準備を伴い残Issueで最も重く、#159は上記のとおり既存経路の再利用で最も軽い。費用対効果の差が大きいと判断した
  - **#116(維持モード)・#163(長期視点)は10月に入ってから。** 必要になるのは「達成後」ではなく「達成が見えた頃」で、#116は設計の書き戻しが済んでおり実装のみが残っている
  - **#107(ESLint)は今期は着手しない。** 減量目標にもアプリの価値にも寄与しないため。Issue本文の代替案どおり `lint` script を削除して閉じるかは別途判断する
  - **#168(デモモード)は11月以降。** 記事の導線資産としての価値はあるが、最大の論点(デモデータと実データの混在防止)が重く、10/31までは自分の減量を優先する
- **2026-08-03: LINE Botの入力窓口としての再合流(#160) → 対応不要と判断しクローズ。** 書き忘れはリマインド通知(#158)、アプリ内の入力コストはテキスト一行入力(#159)で対処でき、LINE経由でしか解けない課題が残らないため、入力窓口を増やすより既存経路を速くする方を採った。加えて、#126で食事記録が「当日分の丸ごと置き換え」保存になっているため、LINE由来のレコードとアプリ側の記録を同じ日・同じ区分で共存させるには同期・取り込み設計に例外を作る必要があり、得られるものに対して代償が大きい。#158・#159の運用後もなお「アプリを開かずに記録したい」場面が残れば立て直す
- **2026-08-03: 記録のリマインド通知(#158)の実現方式を決定した(実装は未着手)。** 決定事項は2つ:
  - **未記録の判定はWorker(シート)ではなくService Worker側(IndexedDB)に置く。** 自動同期の発火は「起動時・アプリ復帰時・オンライン復帰時」(#105)のため、アプリを開いて記録しそのまま閉じると当日の記録はまだシートに載っておらず、Worker側で判定すると誤って催促してしまう。ローカルが真実の情報源というローカルファースト方針とも一致する。Workerはスケジューラに徹し、KVに持つのは購読・通知時刻・対象トグルのみで記録の中身は持たない
  - **全部記録済みの日にも通知を出す**(「今日の記録は完了(◯日連続)」)。通知を出さない設計にすると `userVisibleOnly: true` の制約下でsilent push扱いになり、ブラウザが代替通知を出す・購読を失うリスクがあるため、どのpushも必ず通知になる形にしてこの問題を構造的に消す
  - Service Workerは `generateSW` のままとし、`workbox.importScripts` で `push` ハンドラを取り込む最小改修とする(`injectManifest` への全面移行は影響が大きいため)。SWからはDexieが使えず生のIndexedDB APIで読むことになり、スキーマがSW側に二重管理になる点は把握したうえで受け入れる
- 2026-07-06: ホーム画面での表示順 → 体重・食事・筋トレ・水分・日記から、体重・食事・水分・日記・筋トレの順に変更した(ユーザーの希望により筋トレセクションを最後尾に)。画面設計書の章立てもこの順に並び替えた

## 技術スタック・インフラ

- 技術スタックの選定 → React + Vite + PWA、IndexedDB(ローカルファースト)、Cloudflare Workers中継の構成で決定し、実装済み(要件定義書6章)。当初検討したReact + Spring Bootのフルスタック構成、クラウド同期(Firebase/Supabase等)は以下の理由で見送り:
  - Render/RailwayなどのPaaS無料枠は、DBの自動削除やスリープ挙動があり、日次記録アプリの運用には不向き(2026年時点の調査結果より)
  - Oracle Cloud Always Freeによる自前サーバー運用はコストゼロだが、運用の手間・アカウント審査等の不確実性がある
  - 「PC閲覧はたまに確認できれば十分」という要件確認により、常時同期のためのサーバー・DBを持つ必要がなくなった
- ホスティング → 当初想定のCloudflare Pagesから、AI判定・同期の中継Workerと同一のWorker(`wrangler.toml` の `[assets]`)で静的配信までまとめる構成に変更
- オフライン/オンラインの判定方法 → `navigator.onLine` のみで判定する(要件定義書4.3章)
- **2026-07-11: Worker APIの保護(#87) → 共有トークン方式(Workerシークレット`API_AUTH_TOKEN` × 設定画面の`apiToken`のBearer照合)で決定・実装済み。** リポジトリ全体レビューで「無認証のAPIから健康データの読み出し・Gemini課金の発生が可能」と指摘されたため、マルチユーザー化(#67)を待たず単独で先行実装した。単一ユーザー前提の最小構成。シークレット未設定時は無認証で通す(移行期間・ローカル開発への配慮)
- **2026-08-16: `API_AUTH_TOKEN` をカンマ区切りの複数トークン対応にした(#218。配布版 #213 の子Issue)。** 数人に配ると全員が同じトークンを共有することになり、**1人分だけ止められない**。人ごとに違う値を配れば、その1本をシークレットから外すだけで失効させられる。決定事項:
  - **D1によるユーザー管理は作らない。** 課金状態と突き合わせる話(検討メモ12.2・12.3)は有料化に進むときの論点で、無償配布には過剰。発行・失効のUIも作らず、Cloudflareのシークレットを手で書き換える運用にする
  - **「シークレット自体が無い(`undefined`)」ときだけ認証を要求せず、シークレットは在るのにトークンが0本(`""`・`","`・`"  "`)なら全拒否する(実装中のレビューで方針を変更)。** 当初は「`""` を未設定とみなす既存の挙動(#87)をそのまま延長する」として空も全許可にしていたが、**この変更自体が持ち込む運用**(シークレットを手編集してトークンを1本ずつ抜く)を考慮できていなかった。最後の1本を抜いた結果 `""` や `","` が残ると、**失効させたつもりの操作でAPIが黙って全開放**され、健康データの読み出し(`/api/import-sheets`)もGemini課金(`/api/judge-meal`)も無認証で通る。事故ったときに倒れる向きとして全拒否を選んだ(401ですぐ気づける)。ローカル開発・移行期間が必要とするのは「一度も設定していない」状態の穴だけで、そこは `undefined` の分岐で従来どおり維持される
  - **全員を止めたいときは、シークレットを空にするのではなく誰にも渡していない新しい値に差し替える。** 空にする操作は上記のとおり全拒否になるため実害は無いが、意図が読み取れる形にしておく
  - **クライアント側は変更しない。** 複数指定はWorker側の運用の話で、各利用者は従来どおり自分の1本を設定画面に入れるだけ(画面設計書9章)
  - #215 でシート同期がクライアント直呼び出しになると `/api/*` に残るのは有料機能(AIコメント2本・写真判定)だけになり、このトークンは実質「自分のGemini APIキーを使わせる権利」になる。原価が自分持ちである以上、個別に失効できることの価値はそこで上がる
- **2026-07-11: マルチユーザー化の方針検討(#67) → 費用対効果が見合わないと判断し対応不要としてクローズ。** Issue内で行った事業性評価の結論: 食事・体重記録アプリは競合(あすけん等)が「AI写真判定+管理栄養士コメント+巨大な食品DB+PFC管理」を月額300円前後で既に提供しており、個人開発の構造的弱点(食品DB・AI精度)に直撃するうえ技術的な堀にならない。加えて共有ホスティング・SaaS化には認証基盤・課金・法務(特定商取引法・健康データは要配慮個人情報)対応が必要でコストに見合わない。当面は「本人+知り合いへのBYOK/無料配布」を前提とし、共有ホスティング・SaaS化は行わない。Worker API認証(#87)は本論点と切り離し、無認証API自体のセキュリティリスク対策として既に実装済みのまま維持する
- **2026-08-03: 知り合いへのBYOK配布の基盤整備(#165) → 対応不要と判断しクローズ。** 要件定義書8章の「本人+知り合いへのBYOK/無料配布を前提とする」という位置づけ自体は維持するが、そのための基盤整備を先回りしないという判断(配布を禁じるものではない)。理由: 配る相手が実在するかの確認が先で、需要がない状態で配布基盤を整えるのは#67と同じ誤りを繰り返すことになる。配布形態も、各自がCloudflare・Google Cloud・Gemini APIキー・スプレッドシートを用意する案はエンジニア以外に現実的でなく、既存Workerへの相乗り案は健康データが同一スプレッドシートに混ざるためシート分離かユーザー識別が必要になり、#67で否定したマルチユーザー化に踏み込む。他人の要配慮個人情報を自分のインフラで預かる責任も、#67で法務コストを理由にSaaS化を避けた判断と整合しない。実際に配りたい相手が出てきた時点で、その相手の環境に合わせて改めて検討する
- **2026-07-11: デプロイパイプラインへのテスト実行組み込み(#18) → 費用対効果が見合わないと判断し対応不要としてクローズ。** GitHub Actions移行(push→test→build→deployのワークフロー化)や簡易案(Cloudflare Build commandを`npm run test && npm run build`に変更)を検討したが、個人開発・単一ユーザー向けというプロジェクト規模には見合わないと判断。デプロイ前に開発者自身が`npm run test`/`npm run build`をローカルで確認する現状の運用を継続する
- **2026-08-09: 上記#18を部分的に見直し、PR単位のCIチェックだけを追加した(#201)。デプロイのゲート化は引き続き見送る。** #18が比較したのは「Cloudflareのビルドコマンドにテストを挟む案」と「Actionsへ全面移行してデプロイまで担わせる案」の2つで、**デプロイに関与しないPRチェック**という選択肢は検討されていなかった。これはCloudflare APIトークンの管理もGit連携の無効化も不要で、#18の結論(デプロイはCloudflare Git連携のまま・ゲート無し)と矛盾しない。加えて#18当時と違い、既にGarmin連携(#80)でGitHub Actionsが常用されており「Actions導入コスト」という前提自体が消えていた。`.github/workflows/ci.yml` でPR時と`main`へのpush時に`npm run test`/`npm run build`/`npm run typecheck:worker`を回す。**これは品質シグナルであってデプロイは止めない**(止めるにはブランチ保護の設定が別途必要)。**E2E(#198)はCIに入れない** — Chromiumの取得が要り数分かかること、ランナー起因で不安定になりうることから、ローカル実行の運用を明文化する側で担保する(CLAUDE.mdの開発フローに必須項目として記載)

- **2026-08-09: PWAのService Worker破損による全API不通の障害を受け、アプリ側にSWの復旧手段を持たせた(#203〜#206)。** AIコメント生成・シート同期を含む `/api/*` の呼び出しが全て `Failed to fetch` になる事象が発生。切り分けの結果、原因はインストール済みPWAのService Worker/キャッシュの状態で、Cloudflare Worker・Gemini・Google Sheetsはいずれも健全だった(Gemini・Sheetsの健全性は、ローカルに実Workerを立てた検証と、同日3:27 JSTに成功していたGarmin連携のGitHub Actionsで確認)。ユーザーがPWAを再インストールして復旧。決定事項:
  - **復旧手段を「再インストール」に頼らない(#203)。** 当時アプリ側にSWを扱うコードが1行も無く(`vite-plugin-pwa` の既定の自動登録のみ)、ユーザーに残された手段が再インストールしかなかった。Android ChromeではPWAのアンインストールでサイトのストレージごと消えることがあり、**記録が全てIndexedDBにある本アプリでは再インストールがデータ喪失のリスクを伴う**。設定画面に「アプリのリセット」(SW登録解除+Cache Storage削除+リロード、IndexedDBには触れない)を置き、同じ復旧を安全に行えるようにした
  - **SWの更新検知・更新プロンプトのUIは入れない。** `registerType: "autoUpdate"` で更新自体は既に自動で行われており、今回欠けていたのは更新ではなく**壊れたときの復旧**だったため。必要になった時点で別途検討する
  - **エラー表示で「サーバーに届かなかった」と「サーバーがエラーを返した」を区別する(#204)。** 今回、画面に出たのがブラウザ生の `Failed to fetch` だけだったため切り分けに何往復も要した。この2つは必要な対処が違う(通信環境の確認・アプリのリセット / 時間をおいて再試行)ため、共通処理(`src/api/request.ts`)で `ApiConnectionError` として投げ分ける
  - **`/api/*` にタイムアウトを設け、Worker側のGeminiリトライを一時的な失敗(5xx・429・408・スキーマ検証落ち)に限定した(#205)。** タイムアウトが無いと応答が返らないまま処理中の表示が続き、失敗か処理中かを判別できない。またリトライが無条件だったため、APIキー不正・モデル名不正のような投げ直しても通らない4xxでも待ち時間が倍になっていた。**ただし同期側のタイムアウトは短くしない(180秒)** — `runSync` は未同期分を分割せず1リクエストで送るため、長期オフライン後の大量の未同期を短いタイムアウトで打ち切ると、次回も同じ量を送って同じように打ち切られ永久に同期できなくなる。ここでの目的は「速く諦めること」ではなく「無限に待たせないこと」
  - **`[observability]` を有効にした(#206)。** 今回「そもそもリクエストがWorkerまで届いたのか」を確認する手段が無かった。今回の原因はクライアント側でWorkerには届いておらず、ログがあっても直接の解決にはならなかったが、**「Workerには届いていない」と即断できた**はずで切り分けのコストは大きく下がる

- **2026-08-10: 発信(note / Zenn / X)を別リポジトリ `yotti773/content-ops` に分離した。** `articles/`(記事の下書き・画像・X投稿の運用ファイル、7.2MB)と `scripts/x/` を、`git subtree split` で履歴ごと移設。`zenn-content` を発信専用に分けたのと同じ切り分けで、アプリ本体のリポジトリはアプリと仕様書だけを持つ状態に戻した。
  - **きっかけはXのリプ案を作る仕組みの置き場の検討。** リプの相手はからだログの読者に限らずTech Stack Logや一般のエンジニア・ダイエット層で、lifelogのスコープから外れていた
  - **X運用だけを分ける案は採らなかった。** `articles/README.md` にX運用の話(投稿カレンダー・予約投稿から手動への転換)が混在しており、一部だけ移すと正が2か所に割れる。同じ構図で `generate_paste_list.mjs` の貼り付け用ファイルがストック本体とズレた実績があるため、**全部移すか何も移さないかの二択**と判断した
  - **`docs/からだログ_マネタイズ検討メモ.md` は本リポジトリに残す。** 公開スケジュール・事前登録フォームのプレフィルURL・PV閾値の判定はプロダクトの意思決定であり、記事の下書きではないため。`content-ops` 側への参照13箇所はリポジトリ名を添えた表記に張り替えた
  - **リプ案づくりの手順とコーパスはリポジトリではなくGoogle Driveの「X運用」フォルダに置く。** 日々のリプはスマホ・PCのClaudeチャットから行うため、その場で読めて直せる場所が要る。過去投稿のログはXのアーカイブから毎回作り直す生成物で、gitに入れても中身の無いdiffが積み上がるだけ。生成は `content-ops` の `scripts/build_corpus.mjs`

## データ同期(スプレッドシート)

- **2026-08-08: 復元されないデータの保全方式(#164)を決定・実装した。** シート同期はバックアップの主線だが、**シートに列を持たないデータは復元できない**(週次・月次のAIコメント、設定、食事のAI推定値・写真参照)。とくにAIコメントは生成が非決定的で、**再生成しても同じものは二度と出ない**ため、失ったら復旧手段が無い唯一のデータになる。#164が挙げていた2案のうち、**`db/backup.ts` のJSONエクスポート復活を採用**した:
  - **シートに専用タブを追加する案は不採用。** `AdviceRecord` は生成時の `digest`(ネストした構造化データ)も再現・デバッグ用に保存しており、1セルにJSON文字列を詰めることになる。「シートは人間が読める閲覧用の写し」という同期の利点を損なううえ、設定(`Settings.apiToken` を含む)をシートに書くのはセキュリティ上も避けたい
  - **着手時に判明した重要な事実: 残置されていた `db/backup.ts` は `weightRecords`/`mealRecords`/`settings` の3テーブルしか扱っていなかった。** フェーズ1時代の実装のまま放置されており、水分・日記・筋トレ・マスタ・活動・血圧・周囲径・習慣、そして**肝心のAIコメントが入っていなかった**。「ロジックは残置してあるので容易に戻せる」(要件定義書5.2章の旧記述)という前提自体が成立していなかったため、全テーブル対応に作り直した
  - **同じ腐り方を繰り返さないよう、テーブルの網羅をテストで縛った。** `backup.test.ts` が `db.tables` と `BACKUP_TABLES` を突き合わせ、`syncDeletions` 以外に漏れがあれば落ちる
  - **`syncDeletions` は意図的に対象外。** 「シートのこの行を消す」という同期の途中状態であって利用者のデータではなく、復元先へ持ち込むと復元したレコードがまだ載っているシートの行を消しにいく恐れがある。書き出し前に「今すぐ同期」を済ませれば保留中の削除はシートへ反映される
  - **復元は全削除+書き戻しだが、削除より先に検証を通す。** 旧実装は読み込んだデータをそのまま `clear()` → `bulkPut()` に流しており、壊れたファイルを掴むと既存データを失う作りだった。`parseBackupData()` で形式・型・空判定を通してから復元し、UIでも件数を提示して確認を挟む
  - **IndexedDBがオリジン単位である点を要件定義書・画面設計書に明記した。** 機種変更やブラウザデータ削除だけでなく、**アプリの配信URLを変えるときも同じ退避が必要**(同じブラウザでもデータは引き継がれない)。これは実際に、Cloudflareのサブドメイン変更(`tatu1228` → `n1lab`。#137でドキュメントだけ先に更新されていた)を実行する前に気づいた論点
  - **2026-08-08(同日追記): 上記の結論を一部覆し、AIコメントはシート同期の対象にした。** JSONを実装した直後に「シートとJSONの2本立てはどうなのか」という問いが出て、確認したところ**JSONがシートを完全に包含していた**(復元用途では)。しかもJSONは手動運用なので必ず腐る。役割分担ではなく片方がもう片方を飲み込んでいる状態だった。
    - **シート専用タブ案を却下した根拠(`digest`がネストした構造化データ)を確かめたら、`digest`はアプリのどこからも読まれていなかった。** `src/`全体で`.digest`の参照はゼロで、画面が出す数値は`getWeeklyDigest()`が毎回レコードから再計算している。つまり「1セルにJSONを詰めることになる」という懸念は、**誰も読んでいないデータのために立っていた**
    - 失って困るのは`advice`(判定・総評・良かった点・アクション)だけで、これはテキスト4項目。**普通のシート1タブに収まる**ため、却下の根拠が消えた
    - よって「週次AIコメント」「月次AIコメント」の2タブを追加し、記録と同じID列upsertに乗せた。`digest`は書き出さず、`AdviceRecord.digest`を任意にした(シート由来のレコードでは未設定)。取り込みは追加のみ・ローカル優先で、**手元にdigest付きのレコードがある場合は上書きしない**(生成時の証跡を失わないため)
    - **これによりJSONバックアップの役割は縮んだ。** 残る固有価値は設定(`Settings`)と食事のAI推定値・写真参照だけ。日常の保全はシート1本(自動)に戻り、JSONは「移行前・節目のスナップショット」に位置づけが下がった。JSON自体は残す — 設定の再入力を省けること、および1ファイルで断面を丸ごと持てることに価値があるため
  - **2026-08-08(さらに追記): 設定もシート同期の対象にした(本人判断)。** 「JSONに残る固有価値は設定と食事のAI推定値だけ」という整理に対し、本人から「設定もシートに書き出してよい / AI推定値は不要」との判断が出たため。1設定=1行の key-value 形式(`項目 | 値 | ID`)にして、設定項目が増えても列構成を変えずに済むようにした。
    - **`apiToken` だけは対象外にした。** 取り込みAPIの呼び出しに `Authorization: Bearer` としてこの値が要るため、入力済みでなければ取り込みが始まらない = **シートからは原理的に復元できない**。#164が挙げていた「認証情報をシートに書くのはどうか」という懸念とも一致する。`lastSyncedAt` も端末ごとの同期状態のため対象外
    - **差分同期のため `SettingsRow.synced` を足したが、`lastSyncedAt` だけの更新では未同期に戻さない。** 同期完了のたびにこの値を書くため、素直に実装すると設定が永久に同期待ちのままになる
    - **取り込みはローカルで未設定の項目だけを埋める。** 上書きにすると、機種変更後に手で入れ直した値をシートの古い値が潰しうる
    - 日付・真偽をそのまま書くと `USER_ENTERED` でSheetsが解釈し直すため、日付は「2026年10月31日」、真偽は「はい/いいえ」で書く(月キーで踏んだのと同じ問題)
    - **これでJSONバックアップに残る固有価値は、食事のAI推定値・写真参照と`apiToken`だけになった。** 本人が「AI推定値は不要」としているため実質的な固有価値はほぼ無いが、**移行前の断面をまるごと持てる保険としては残す**(#164の実地リハーサルが未了で、シートからの復元で何が戻らないかを実測していない段階で保険を捨てるのは順序が逆のため)。実測後に不要と分かれば、そのとき UI ごと削除する — 残置したロジックは腐る(まさに `backup.ts` が3テーブルのまま腐っていた)ので、消すときは中途半端に残さない(**後日の実測により残す判断となった。下記のリハーサル記録を参照**)
  - **2026-08-08(再レビュー): 既定値の実体化という設計バグを潰した。** 実装後の再レビューで、`updateSettings` が読み取り時のマージ結果(既定値込み)を保存していることが2つの事故につながると指摘された: (1) 新規端末で最初にAPIトークンを入れた瞬間に既定値が「ユーザーが設定した値」として保存され、**次の自動同期で既定値がシートの実値を上書きする**、(2) 取り込みも既定値を「設定済み」とみなし、**最重要の3項目(目標体重・目標日・目標カロリー)を復元しない**。取り込みにはAPIトークン入力が必須なので、これは新規端末で必ず通る経路だった。対処として**既定値は読み取り時にだけ被せ、保存行には明示的に設定された項目だけを持たせる**形に変えた(`SettingsRow`を`Partial<Settings>`に)。「未設定」と「既定値のまま」を区別できることが、同期・取り込みの正しさの前提になる。あわせて、送信中に入った設定変更が`markSettingsSynced`で同期済みに塗り潰される競合(設定は数ヶ月触らないことがあり、取りこぼすとシートが古いまま放置される)を、送信時スナップショットとの比較で防いだ。シートの手編集で性別に候補外の文字列が入るとBMR計算が黙って狂う問題も、判定(verdict)と同じ「候補外はスキップ」で塞いだ
  - **2026-08-08(リハーサル実施): 実データで復元経路を検証し、#164を決着させた。** シート同期・JSON復元の両経路でデータが戻ることを確認済み。リハーサルは**ドメインを変える前に、同一オリジンのプライベートウィンドウ**で行った — IndexedDBが空でオリジンは本番と同じなので、移行直後の端末とまったく同じ状態を、本番データを危険に晒さずに再現できる。この段取り自体が今後の機種変更にもそのまま使える
    - リハーサルの直前に、**移行後の端末が必ず通る「APIトークンだけ入れた状態」で未同期件数が1のまま消えない**不具合を潰した。`apiToken`はシートに載せない項目のため送信対象は0件だが設定行は`synced: false`になり、「行が未同期か」で判定していた同期エンジンとバッジが空ペイロードのpushを走らせ続けていた。判定を「送信できる項目があるか」に変更。**バックアップは復元を試して初めて成立する**という#164の前提が、実際に1件の不具合として現れた例
    - **JSONバックアップは残す。** 実測後も固有価値(食事のAI推定値・写真参照・`apiToken`)は消えておらず、さらに**移行が「JSON復元1回」で完結する**という手順上の価値が確認できたため。シート経由だとAPIトークンの手入力が要り、AI推定値・写真参照も戻らない
    - 手順は画面設計書9章「機種変更・ドメイン移行の手順」に残した(#164の論点「手順のドキュメント化の要否」への回答)。**「シートから取り込み → JSONから復元」の順は禁止**という順序の罠が最も伝えるべき点 — 復元は全テーブルを削除してから書き戻すため、先に取り込んだ内容が捨てられる

- 同期先・フォーマット(#3) → アプリのデータモデルに沿った新規専用スプレッドシート(体重記録/食事記録の2タブ)で決定。既存の手動運用シートは「1日1行、その日の食事の合計カロリー/PFC」という集計形式でアプリの`MealRecord`(1食事=1レコード)と粒度が合わないため、合流を見送った。認証方式はGoogleサービスアカウント(Workers側のみが保持)、書き込みAPIはGoogle Sheets API v4
- 同期失敗時のリトライ間隔・エラー通知の要否(#3) → 新しい仕組みは追加せず、既存の「未同期フラグを維持し次回起動時 or 手動ボタンで再試行」+ 設定画面でのエラーメッセージ表示のみで十分と判断
- **2026-07-12: 自動同期のトリガー拡充(#105) → 起動時のみ→「起動時+アプリ復帰時(`visibilitychange`)+オンライン復帰時(`online`)」に拡充・実装済み。** スマホPWAは「閉じずにホーム画面へ戻り数日後に復帰する」使い方が多く、起動時1回では未同期が溜まりやすいため。連続発火対策として自動トリガーに最小間隔5分のスロットリングと実行中の再入抑止を設けた(`src/sync/autoSync.ts`)。オンライン復帰は「同期が可能になった」明確なシグナルのため間隔チェックを飛ばし、オフラインでスキップした試行は間隔に数えない。定期ポーリング(setInterval)は、イベント駆動で実用上足りるうえ電池・API呼び出しの無駄になるため採用しなかった
- 同期済み記録の編集・削除の反映(#30) → ID列をキーにした upsert(編集)+ 削除トゥームストーン + `deleteDimension`(削除)で決定・実装済み(画面設計書10章)
- **2026-07-11: 水分・筋トレ・日記記録をスプレッドシート同期の対象に含めるか(#72) → 3つとも含めることに決定・実装済み。** 新規タブを1つずつ追加し、体重・食事と同じID列upsert方式で反映する(画面設計書10章)。あわせて以下2点を決定:
  - 日記の自由記述本文もそのまま同期する(気分タグのみに絞らない)。プライバシー面は認識した上で、バックアップの完全性を優先した
  - 筋トレは「その日の全セットをまるごと置き換え」という既存の保存方式(#8)を変えず、置き換えのたびに全セットへ新しいIDを振り直す。置き換え前の全セットIDを削除トゥームストーンとして残すことで、シート側の古いセット行を消す。1回の保存が「その日の既存行を全削除→新規行を全追記」という比較的重い同期になるが、他の記録と同じupsert/削除の仕組みをそのまま流用できることを優先した
- **2026-07-11: ローカルデータ消失リスクに備えたバックアップ促し通知(#14) → 対応不要と判断しクローズ。** 起動時の自動同期(`App.tsx`)+設定画面の手動同期ボタン+シートからの取り込み・復元(#54)により、ユーザーが手動でバックアップ操作を行う前提が既に無くなっており、「エクスポートを促す通知」という当初の課題設定が成立しなくなったため。ただしシートに同期されない設定・食事マスタ・種目マスタ・食事のAI推定値/写真参照は引き続きバックアップ対象外であり、このうち食事マスタ・種目マスタはインポート・エクスポート機能(#96)で別途対応する
- **2026-07-12: 食事マスタ・種目マスタのインポート・エクスポート機能(#96) → CSV/JSONファイル方式ではなく、既存のスプレッドシート同期・取り込みへの統合で決定・実装済み。** 検討結果(詳細は https://github.com/yotti773/lifelog/issues/96#issuecomment-4949599412 ):
  - ファイル方式はユーザーが手動でエクスポートしない限りバックアップされないが、既存同期に乗せれば起動時の自動同期でマスタも常時バックアップされ、#14から引き継いだ残課題が根本的に解消する。一括登録もシートのマスタタブに行を直接書いて「シートから取り込み」すればよく、ファイルアップロードUIが不要になる
  - マスタ専用スプレッドシートの新設案は不採用(スプレッドシートIDの追加管理・Workerのマルチスプレッドシート対応が必要になる割に、同一シート内のタブ分離で足りる以上のメリットがないため)。既存の同期用シートに「食事マスタ」「種目マスタ」の2タブを追加し、タブが無ければ同期時にWorkerが見出し行付きで自動作成する
  - 取り込みは記録と同じ「追加のみ・ローカル優先」に加え、IDが違っても同名(前後空白無視)の既存品目・種目をスキップする(種目マスタは名前がサジェストのキーで同名を許さないため)
  - コード埋め込みの定番メニューシードデータ(`foodMasterSeedData.ts`)と「定番メニューを一括登録」ボタンは撤去し、データはシート貼り付け用サンプル `docs/data/食事マスタ_定番メニューサンプル.csv` へ退避した

- **2026-08-08(#194): 同期のタブ・列構成をドキュメントに再掲するのをやめ、コードを唯一の正にした。** 同じ数値が複数箇所にあり、実際にズレていたため:
  - **タブ数は4箇所(CLAUDE.md・画面設計書10章・10.1章・10.2章)にあり、3箇所が古かった。** 10章は「7タブ(記録5+マスタ2)」と #96 当時のまま、10.1章・10.2章は「12タブ」とフェーズ4追加時点のままで、#164 でAIコメント2タブ+設定タブを足した際にどこも更新されていなかった(実際は15タブ)
  - **CLAUDE.mdのID列一覧は7タブ分で止まっていた**(#30 当時のまま。血圧・周囲径・習慣・AIコメント・設定の8タブ分が欠落)
  - 方針: **タブ名・ID列・列の並び・タブ数は `worker/sheetsSync.ts`(`*_CONFIG`・`*_HEADER`・`*ToRow`)と `worker/sheetsImport.ts`(`handleImportSheets`)が正**とし、ドキュメントには再掲しない。ドキュメントが持つのは**コードから読み取れない「なぜその形なのか」だけ**(後付け列をID列の後ろに置く理由、月キーを生で書かない理由、`apiToken` を書き出さない理由、AIコメントに削除の仕組みを設けない理由など)
  - 「記録5タブ」「マスタ2タブ」のような**閉じたグループ名は残した** — 増減しないため陳腐化せず、文章の可読性に効く。消したのは増えうる総数(「15タブ」)と、タブごとの列の列挙
  - レビューチェックリスト3.3の照合手順も「数値が一致するか」から「ドキュメント側に数値を書き足していないか・『なぜ』がコード変更で嘘になっていないか」に変えた

- **2026-08-17(#214): ユーザー自身のGoogle認可を実装した(配布版 #213 の中核)。** 検討メモ12.8の**案A**(Sheets APIはクライアント直・refresh tokenもクライアント保持)に沿って、認可とトークンの取り回しだけを先に入れた(Sheets APIの移設は #215、シートの新規作成は #216)。実装上の決定:
  - **認可コードフロー + PKCE をリダイレクトで行う**(ポップアップやGoogle Identity Servicesのライブラリは使わない)。インストール済みPWA(standalone)ではポップアップの挙動が不安定でブロックもされやすいため。外部スクリプトを読み込まないぶんCSP・可搬性の面でも素直。**Console側に登録が要るのは「承認済みのリダイレクトURI」**(`/oauth/callback`)で、Issue本文が挙げていた「承認済みのJavaScript生成元」ではない — コード交換をWorker側で行うため、ブラウザからGoogleのトークンエンドポイントを直接叩かないため
  - **PKCEは必須ではないが付けた。** トークン交換はWorkerが `client_secret` を添えて行うのでPKCE無しでも成立するが、認可コードはリダイレクトのクエリとして一度ブラウザのURLに現れる。横取りされたコードを単体で使えなくするための保険
  - **`access_type=offline` と `prompt=consent` の両方を送る。** 前者が無いとrefresh tokenが発行されず、後者が無いと2回目以降の認可でrefresh tokenが返らない(初回だけ返る仕様のため、再連携が空振りする)
  - **refresh token は専用テーブル `googleAuth` に置き、`Settings` に相乗りさせない。** Settingsはシート同期にも完全バックアップにも乗るため、相乗りさせると同期先シートとバックアップJSONの両方へ流出する(検討メモ12.8の制約3「バックアップに含めない」に反する)。あわせて `backup.ts` に `BACKUP_EXCLUDED_TABLES` を導入し、テーブル追加時に「対象」か「除外(理由付き)」かの分類を強制する番人に作り替えた(従来は `syncDeletions` を除外することが `backup.test.ts` にハードコードされていた)
  - **access token は永続化しない。** 短命(約1時間)なのでメモリに置き、期限切れならrefresh tokenから作り直す。保存された認証情報を増やさないことを、リロード時の1往復より優先した
  - **失効(401)は正常系として扱い、保存済みトークンを捨てて未連携に戻す。** 使えないトークンを持ち続けても再連携の妨げにしかならない。**一方、一時障害(502等)では捨てない** — 捨てるとサーバー側の不調で連携が失われる。この分岐のために `requestApi` へ `ApiStatusError`(ステータスを持つエラー)を追加した。文言でエラー種別を判定すると、メッセージを変えた瞬間に壊れるため
  - **Googleのエラー応答をそのままクライアントへ返さない。** 応答本文にはリクエストの反射でトークンが混ざりうるため、既知のフィールド(`error` コード)だけを拾って組み立て直す。「トークンをログに出さない」(#206)と同じ趣旨をレスポンスにも適用した
  - **レート制限はコードでは入れていない。** Workerに状態(KV/D1/Durable Object)を持たせない構成のため、意味のある制限が書けない。CloudflareのWAFレート制限ルールで掛けるのが正しい層で、これはダッシュボード側の設定作業として残る(検討メモ12.8が要求している項目)

- **2026-08-23(#215・#216): シート同期をWorker経由からクライアント直に移し、同期先をユーザー自身のDriveに作るシートへ変えた。** 検討メモ12.8の案A。`worker/sheetsSync.ts`・`sheetsImport.ts`(約2,300行)を `src/sync/sheets/` へ移設し、`/api/sync-sheets`・`/api/import-sheets`・`/api/import-activity` とサービスアカウント認証(`worker/googleSheetsAuth.ts`)を削除した。決定と、その過程で分かったこと:
  - **#215と#216は分けて出せなかった。** `drive.file` スコープは「アプリが作成したファイル」にしかアクセスできないため、クライアント直に切り替えた瞬間、アプリが作っていない既存シートは403になる。**同期先を作る機能(#216)が無いと#215は動作確認すらできない**ため、1つのPRにまとめた
  - **既存シートの引き継ぎにGoogle Pickerを使う案(#216の当初の完了条件)は採らなかった。** Pickerには外部スクリプトの読み込み・APIキーの発行・Picker APIの有効化が要るのに対し、**使う相手は開発者本人1人だけ**(配布相手は既存シートを持たない)。代わりに「アプリで新しいシートを作り、Garminの書き込み先をそちらへ向け直す」を選んだ。手作業は新シートのサービスアカウントへの共有とGitHub Secretの差し替えの2つで済む(2026-08-23、本人の判断)
  - **シート作成時に全レコードを未同期へ戻す(`markAllRecordsUnsynced`)。** 新しいシートは空なのに手元のレコードは `synced: true` のままで、そのままでは1件も送信されない。ローカルのIndexedDBが真実の情報源なので、戻して一度フル同期すれば新シートに再現される。**トゥームストーンは戻さない** — 新シートには消すべき行が最初から無い
  - **記録5タブも自動作成の対象にした(#216)。** 「記録5タブは手動作成が前提」という前提(#3・#96)は、他人のDriveにアプリがシートを作る以上もう成立しない。**活動記録タブだけは作らない** — Garmin側(`scripts/garmin/garmin_to_sheet.py`)が自分で作る側で、列構成の正があちらにあるため
  - **スプレッドシートIDの手入力欄は用意しない。** 入力できてもGoogleが403を返すだけで、「設定したのに動かない」という分かりにくい失敗を作る
  - **移設に伴い、worker側にあった型の複製を削除した。** `worker/` が `src/` に依存しない独立ビルドだったため `*Input`・`Imported*Output` を手で同期していたが、同じビルドに入った以上その理由は消えた。あわせて enum 的なセル(食事区分・気分・部位・判定)のパース結果を実型へ寄せ、JSON境界で緩んでいた型が効くようにした
  - **Garmin連携は移設対象外。** GitHub Actions の Secrets は Worker と同名だが別物で、消すと翌朝の同期が黙って落ちる。サービスアカウントをシートの編集者から外すのも不可

- **2026-08-23(#213・#246): 配布先の継続率を数値で測るのはやめた。** #213 は「5人に配り、4週間後に3人以上が週4日以上記録していれば継続の目安」という合格基準を置いていたが、**#215 で記録が各自のDriveへ直接入るようになり、配布者からは記録が一切見えなくなった**(それが #215 の目的そのもの)。測定手段を足すには、シートを共有してもらうか、利用状況を送るかのどちらかが要る:
  - **シートの共有は採らない。** 他人の健康データを見ることになり、「記録はお預かりしません」という配布の前提(#238のプライバシーポリシー)と食い違う
  - **テレメトリも採らない。** ポリシーに「アクセス解析は行いません」と書いた直後に入れることになる
  - 結論として**数値の合格判定を降ろし、感想と体感で判断する**(2026-08-23、本人の判断)。測る仕組みを足すより、測らないことを選んだ

## Garmin連携・外部データ

- **2026-07-11: 既存ツールからのデータ連携方針(#11) → Garmin連携を実装して決着(#80〜#82)。** 検討結果(詳細は https://github.com/yotti773/lifelog/issues/11#issuecomment-4946239901 ):
  - ライブラリはPythonの `python-garminconnect` を採用(2025年頃のGarmin認証基盤刷新に追随済みで活発にメンテされている。Node系の `garmin-connect` は更新停止のため不採用。公式のGarmin Health APIは法人向け審査が必要で個人利用には非現実的)
  - アーキテクチャはGitHub Actionsのcron(毎日1回)で前日分を取得し、同期用スプレッドシートの新タブ「活動記録」(1日1行)へ日付キーでupsert。アプリ側は既存の「シートから取り込み」(`runImport`)を拡張してこのタブも読む(取り込み専用・常にシート側で上書き。#81)
  - UIは週次レビュー(`WeeklyDigest.activity`+逆算TDEEとの突き合わせ)・推移グラフ(歩数・睡眠)・履歴(読み取り専用の「活動」種別)に統合(#82)。ホームは「今日」のデータが出せないため対象外
  - 非公式APIのため壊れうる前提とし、壊れても日付範囲指定のバックフィルで復旧できる設計にした
  - あすけん側は実用的な非公式APIが無く、手動継続 or 本アプリへの移行が現実的だったが、Garmin連携により活動量・消費カロリー側のデータ一元化は達成できたため、あすけんからのデータ移行は対応不要と判断した(#11はこの結論を含めてクローズ済み)
- **2026-07-11: Google Fit連携(#13) → 対応不要と判断しクローズ。** 歩数・消費カロリーの自動取得というニーズはGarmin連携(#80〜#82)で既に満たされており、同種のデータソースを二重に連携する必要性がないため

## 体重記録・推移画面

- **2026-07-11: 水分・筋トレ・日記の履歴確認(#73) → 既存の履歴画面(推移>履歴)の種別サブタブを6択(体重・食事・水分・筋トレ・日記・活動)に拡張する方式で決定・実装済み。** 水分・筋トレは日付ごとに集約した1日1行、日記は1日1件をそのまま1行で表示し、タップで各記録画面の該当日に遷移する(日記は`?date=`パラメータで過去日を編集可能にした)

- 体重の増減表示(前回比) → 体重入力欄の直下に表示する(画面設計書3章)
- 履歴確認画面の導線 → 推移画面内のサブタブ。期間フィルタは推移グラフと連動させず、全期間・日付降順の固定表示(画面設計書8.1章)
- 基準日ちょうどの体重記録が存在しない場合の進捗バー起点 → 一番古い体重記録にフォールバックする(画面設計書9章)

## 食事記録

- AI判定結果画面と手動修正画面 → 1画面に統合(区分選択も含め、食事記録は1画面構成。画面設計書4章)
- PFCの入力UI → 3つの数値入力欄を横に並べる(円グラフ的な比率表示は採用しない)
- **2026-07-06: PFCの目標バランス設定要否(#7) → PFC全項目(たんぱく質・脂質・炭水化物)の目標値を持つことに決定。** 目標摂取カロリーと体重からの自動計算値を初期値とし、手動調整も可能とする(#47。要件定義書4.7.5章)
- 食事マスタ管理画面 → 設定画面から遷移する専用画面として分離した(画面設計書4.1章)。手動での新規追加ボタンは別Issue(#35)として実装済み
- **2026-07-15(#126): 食事記録の導線を筋トレ方式(区分=入り口・品目=複数登録・当日分の丸ごと置き換え保存)に刷新した(画面設計書2章・4章・8章)。** それまでは1画面で区分をセグメントで切り替え、1品ずつ「もう1品追加」でリストに溜めて保存し、編集は単品ごと(`?id=`)という構成だった。筋トレ記録が既に「1画面で当日の全種目・全セットをまとめて編集し、当日分を置き換え保存」という形で運用できていたため、食事も同じ構造に揃えることにした。区分(朝食/昼食/夕食/間食)を入り口にして `/record/meal?type=&date=` でその区分の記録画面を開き(筋トレの `種目カード` に相当)、画面内で品目カードを複数登録し(`セット追加` に相当)、その区分の当日分を丸ごと置き換えて保存する(`replaceMealRecordsForDateAndType`)。
  - 保存方式は追記でなく**丸ごと置き換え**をユーザーと合意して採用。編集・削除がその区分の1画面で完結する一方、別端末で同区分に追記された品目を上書きしうる点は許容した(単一ユーザー・1日数件の規模のため。筋トレの当日置き換えと同じ整理)。削除反映はIssue #30の削除トゥームストーンを流用する
  - 写真AI判定は、旧「検出リストから1品ずつフォームに反映→未追加確認ダイアログ」をやめ、**検出品目をそのまま品目カードとして追加**する方式にした。品目カード自体が確認・修正の場になったため、旧方式の中間UI(`DetectedItemsCard`・`PendingItemsCard`・`ManualMealItemAdder`・単品編集モード)は不要になり撤去した
  - ホーム・履歴も「区分が単位」で揃えた。ホームは区分ごとに1つの入り口カード(品目数・料理名プレビュー・区分合計)、履歴は日付ごと→区分ごとの行にして、いずれもタップでその区分の記録画面へ遷移する
- **2026-08-08(#159): 食事のテキスト一行入力の実現方式を決定した(実装は未着手)。** 「ゆで卵2個と納豆」のような一行テキストをAIに解釈させて品目カードに展開する。決定事項は2つ:
  - **既存の写真判定経路(`/api/judge-meal`)を写真任意化して再利用し、新規エンドポイント・新規画面・新規データモデルを作らない。** レスポンススキーマ(`MEAL_JUDGMENT_RESPONSE_SCHEMA`)・検証(`parseMealJudgment`)・共有トークン認証(#87)・品目カードへの反映(`appendItems`)・`isUncertain` の注意表示はすべてそのまま流用できる。加えて備考(`note`)欄とそれをプロンプトへ「ユーザーからの補足情報(最優先)」として流す経路が既にあるため、テキスト判定はこの補足情報を主役に昇格させる形になる。実質的な作業は `buildMealJudgmentPrompt` のテキスト分岐(写真前提の記述の置き換え。1人前の分量アンカー・PFC整合性チェック・揚げ物の油分注意といった精度の核は流用)に集約される
  - **v1では食事マスタとの突き合わせを行わない。** 「納豆」がマスタにあればマスタの固定値を引く方が正確で無料だが、それを入れると表記ゆれの吸収・一部ヒット時の分割・マスタ品目とAI品目の混在表示が付いてきて、この機能が安く済む理由がそのまま消える。品目カードは修正前提のUI(要件定義書4.2章)のため、まずAIに全部投げる形で運用し、値のズレが実際に負担になったときに前段として足す。**v1のまま完結する可能性も織り込んだうえでの判断**(2026-08-08、本人決定)
  - 写真が無いぶん分量の視覚的な手がかりが消え、推定はプロンプト内の標準1人前アンカーに寄る(「ゆで卵2個」のように個数が明示される入力とは相性が良く、「ご飯軽く1杯」は写真判定より外しやすい)。品目カードで直せる範囲として許容する。副次的に、画像トークンもリサイズ処理も無いぶん写真判定よりレイテンシ・コストは低い
- **2026-08-16(#159): 上記の決定方針どおりに実装した。** 変更箇所は決定時の見立てどおり4箇所に収まった: `worker/index.ts`(写真0枚でも`note`があれば通す。両方無い場合のみ400)、`src/api/judgeMeal.ts`(`files`が空配列でも呼べる。itemsが空だった場合のエラー文言を「写真から/テキストから」で出し分け)、`MealRecordPage.tsx`(解析ボタンの活性条件を「写真かテキストのどちらかがあれば」に緩和。解析後は写真だけでなく備考もクリアするよう変更 — 備考が旧来の「補足」から新たに判定対象の本文にもなったため、以前の「備考は残す」動作のままだと再解析ボタンで二重登録になる)、`buildMealJudgmentPrompt`(`photoCount`が0のとき、導入文・手順1・手順2・isUncertain条件・分量アンカーの見出しを写真前提からテキスト前提の文言に差し替える分岐を追加。手順3・分量アンカーの数値・整合性チェック・揚げ物の油分注意・出力形式は共通のまま流用)。UIの「写真から記録する」カードは「写真・テキストから記録する」に改称し、テキスト欄のプレースホルダー・下部のヒント文言を写真0枚でも迷わない内容に更新した

## フェーズ2機能(筋トレ・水分・日記)

- 筋トレ記録の粒度・種目管理・画面統合方針(#8) → セット単位(重量×回数をセットごとに記録)、種目マスタ+自由入力(食事マスタに準ずる仕組みだが重量・回数は持たない)、ホーム画面に体重・食事と並べて表示、推移グラフはMVPでは持たず一覧確認のみ、で決定(画面設計書7章・7.1章)
- 水分記録の記録単位・目標設定・画面統合方針(#9) → ml単位でクイック追加ボタン+手入力、目標摂取量の設定+ホームでの差分表示あり、ホーム画面に体重・食事と並べて表示、推移グラフあり(カロリー推移と同様)、記録の訂正は削除→再記録のみ(編集UIは持たない)で決定(画面設計書5章)
- 日記の形式・記録粒度・画面統合方針(#10) → 自由記述+気分タグ(3段階)のハイブリッド形式、体重と同様に日付キーで1日1件(後勝ち)、ホーム画面に体重・食事・水分と並べて表示、写真添付はMVPでは行わない、で決定(画面設計書6章)
- **2026-07-07: フェーズ2の画面構成をClaude Design製プロトタイプ(docs/archive/handoff_lifelog_app/prototype.dc.html)に合わせて変更した(#8〜#10の実装時)。** #41で決めたテキスト仕様をもとにデザインプロトタイプを作成・確認した結果、以下の3点を見た目・操作感を優先してデザイン側の構成で確定した:
  - 水分(#9): 「ホーム内でワンタップ完結+手入力モーダル」→「専用の記録画面(クイック追加 100/200/350/500mlの4ボタン+今日の記録リスト)。手入力は設けない」。ホームの水分行は合計+進捗バーの表示とタップ遷移のみ
  - 日記(#10): 気分タグを3段階(良い/普通/悪い)→5段階(絶好調/良い/普通/眠い/不調)に変更。編集画面の明示的な削除ボタンは持たず、本文・気分とも空で保存した場合に記録を削除する挙動とした
  - 筋トレ(#8): 「1画面1種目+セット逐次確定(＋ボタン)+前回値prefill+メモ+マスタ登録チェック」→「1画面で当日の複数種目・全セットをまとめて編集(種目カードの追加/削除、セット行の追加/削除)。保存は当日分の置き換え」。前回値prefill・メモ・マスタ自動登録は見送り、要否は画面設計書12章の論点として残す。種目マスタは種目名入力のサジェスト+設定画面からの管理画面として維持
    - **2026-07-12(#100): 前回値の扱いを再検討し、「自動prefill」ではなく「参照表示+任意の転記」として実装した。** 見送った理由は「開いた瞬間に前回値で勝手に埋まると、当日実際に行った内容と混同しやすく記録画面が煩雑になる」点だった。そこで、種目名が過去の記録と一致したときに前回の重量×回数を参照行として見せ、ユーザーが「入力」ボタンを押したときだけセット欄へ転記する形にした。自動で埋めない原則は保ちつつ、繰り返し種目の再入力の手間を減らせるため採用。メモ欄・マスタ自動登録は引き続き見送り(画面設計書12章の論点)
- **2026-07-12: 種目マスタの部位分類(#104) → 持たせることに決定・実装済み(画面設計書7.1章)。** 入力方式は固定選択肢(胸/背中/肩/腕/脚/体幹/その他)からの選択とし、自由入力は不採用(「脚/足」のような表記ゆれが起きると、将来の部位別集計にそのまま使えなくなるため)。任意項目のため未分類も許容する。シートの種目マスタタブには「部位」列を追加したが、既存シートのID列(C)を動かすとupsert・行削除が既存行を見失うため、後付け列はID列より後ろ(D)に置き、取り込みの読み取り範囲だけを`lastColumnLetter`で広げる方式にした

## UI全般・バックアップ

- ホーム画面の記録導線 → フローティングアクションボタン+アクションシートの構成を廃止し、体重/体脂肪率カードおよび食事区分ラベルへの直接タップで対応する記録画面に遷移する方式に変更した(MUI移行 Issue #27)。カード・ラベルへの直接タップという明確な導線ができたことで、両者を選ばせるアクションシートは冗長と判断した
- JSONバックアップ(エクスポート/インポート)UI(#27) → MUI移行時に設定画面から削除した。ロジック(`db/backup.ts`)は残置し、必要になれば復元可能。手動移行の主目的はスプレッドシート同期でおおむね代替できるため(スプレッドシート側からの復元はできない点は把握した上での判断)。これに伴い、定期エクスポートのリマインド通知(#14)も保留とした
- スプレッドシートからの復元(#54) → #14(バックアップ促し通知)の検討過程で、「復元不可」だったスプレッドシート同期に手動取り込み(インポート)を追加し、シートを復元可能なバックアップ兼過去データ移行の入口に位置づけ直した。JSONバックアップUIは引き続き再追加しない(シートに無いAI推定値・食事マスタ・設定まで含めた完全バックアップが必要になった場合のみ`db/backup.ts`の再利用を検討)。取り込みのマージは「追加のみ・ローカル優先」とし、シート側の編集をローカルに反映する双方向同期にはしない(画面設計書10.1章)
- **2026-08-19(#235): 記録のサマリーを「SNS投稿用の画像」として書き出せるようにした。** 減量の経過をXで発信する際、投稿に載せる数字(週平均体重・前週比・平均摂取・記録日数など)を毎回画面から手で書き写しており、転記の手間とミスの元になっていた。**投稿文の生成ではなく画像の出力にしたのは本人の要望**(SNSに貼るのは画像で、文面は毎回自分で書く)。決定事項:
  - **対象は週次レビューとホーム(当日)の2か所。** 発信の型が「月曜=週次レポート」で週次が主だが、日々の記録をそのまま出したい場面もあるため日次も入れた。**月次レビューからの出力は見送り**(週次・日次の運用が固まってから判断する)
  - **「何を載せるか」を純関数(`src/lib/shareCard.ts`)に閉じ、描画(`src/lib/shareCardCanvas.ts`)はその結果を置くだけにした。** 週次レビューが `WeeklyDigest` の値をそのまま表示するのと同じ方針で、数字を決める場所を1か所にするため。単体テストも純関数側だけで完結する
  - **画像はcanvas 2Dで手描きし、ライブラリを足さない。** 推移グラフを手書きSVGにしているのと同じ理由(デザインガイドのパレットを厳密に制御する)。SVG→画像の変換はWebフォントの埋め込みが要るうえ端末差が大きいため採らなかった。代わりに、描画前に `document.fonts.load()` で M PLUS Rounded 1c / Noto Sans JP を明示的に読み込む(未読み込みのままcanvasに描くと既定のsans-serifで描かれ、画面と別のフォントの画像ができる)
  - **「体重の数値を隠す」トグルを入れた。** 公開の場に出す画像のため、体重の実数を伏せて増減(前週比・前回比)だけを残せるようにした。**伏せている間は「目標までの残り」も出さない** — 目標体重は公言していることが多く、差分から現在の体重が復元できてしまうため
  - **載せる数字は「値のあるものを優先順に最大4項目」の自動選択にした。** 項目を選ばせるUIも検討したが、投稿のたびに選ぶ手間が増えて「ボタン1つ」にした意味が薄れるため、選択は隠すトグル1つに絞った
  - **署名(アプリアイコン + アプリ名)は見出し行の右端に置き、フッターは持たない。URLも載せない。** 当初は配信元(`location.host`)を右下に入れていたが、**投稿本文側にリンクを置く運用のため画像内のURLは読み手の役に立たず**、生のホスト名(`*.workers.dev`)は画像の中で浮く。識別はアイコンとアプリ名で足りると判断して外した(2026-08-19、本人指摘)。
    位置は当初カード下端(フッター)に置いていたが、**「右上のほうがしっくりくる」という本人判断で見出し行の右端へ移した**(2026-08-19)。
    レビュー時に挙げた懸念は「署名がカード内で最も彩度の高い要素のため、上に置くと数字より先に目に入る」「フッターの区切り線が消えると下端が間延びする」の2点で、
    前者は本人判断を優先、後者は**フッター分の余白を各段へ配り直して解消した**(縦位置の定数は `shareCardCanvas.ts` の先頭に集約。明細の最終行と数値欄のラベルが重ならないことが制約)
  - **アイコンはPWAアイコンのマスターのコピー(`public/icons/icon.svg`)を読み込んで描く。** canvasに手でパスを書き写すと、マスターを直したときに共有画像だけ古い絵が残るため(PNGをマスターSVGから書き出す運用と同じ理由)。同一オリジンのSVGなのでcanvasは汚染されず書き出しも通る。読み込みに失敗した場合はアイコン無しでアプリ名だけを描く
  - **実装で踏んだ罠**: MUIのDrawerは中身をシートの表示と**同時ではなく次のレンダーでマウント**するため、親のエフェクトから `canvasRef` を見ると必ず `null` で、何も描かれない画像ができる(白紙のまま保存できてしまうため気づきにくい)。描画をプレビュー用の子コンポーネントの中に移し、自分のマウント後に描くようにした
- **2026-08-19(#235): 日次カードに筋トレの内訳(種目ごとの重量×回数・セット数)を載せた。** 当初は数値欄に「筋トレ 3種目」とだけ出していたが、**種目数という数字だけでは中身が何も伝わらない**(投稿として最も具体的で読まれる情報が落ちている)ため、主数値の横の空き領域に明細として置く形にした。決定事項:
  - **数値欄(最大4項目の横並び)ではなく明細ブロックにした。** 「60kg×8回 3セット」は1マスに収まらず、他の数値と同じ大きさで並べると読めない。主数値の右側はもともと空いていたため、そこを一覧に充てた
  - **種目の区切りは名前ではなく `exerciseOrder`。** 同じ種目名を別カードとして2回記録できる仕様のため(履歴確認画面の `groupWorkoutHistoryDays` と同じ数え方に揃えた)
  - **セットごとに重量・回数が違う場合は範囲で示す**(「50〜60kg×6〜10回」)。トップセットだけを選ぶ案もあったが、実際にやった内容と食い違って見えるため採らなかった。重量0は「自重」と表記する(自重トレーニングの記録として妥当な読み)
  - **明細を出す日はバッジ(前回比)を主数値の横から見出し行へ移す。** 長いバッジが明細の列に重なるため。明細の無い日は従来どおり主数値の横に置く(数値の隣にある方が視覚的に強い)
  - **週次カードには入れない。** `WeeklyDigest.workout` は日数・種目数・総セット数の集計しか持たず、週の全種目を並べても情報量が多すぎて読めないため
- **2026-08-19(#226): ホームに日付ナビを付け、過去日を同じ画面で振り返れるようにした。** ホームは当日固定で、日付が変わるとその1枚は二度と表示できなかった。Issueが挙げていた**決めるべき2点**への回答:
  - **過去日でも記録の追加・編集をできるようにした(閲覧専用にしない)。** 各カードの遷移先に表示日を引き継ぐだけで済んだ — 記録画面は既に `?date=` を受け付ける作りだったため(#141で履歴からの過去日追加を入れた際の下地)。日付キーの記録画面(体重・血圧・周囲径)は未記録の過去日を開くと既定で「見つかりません」になるため、ホームからの遷移には `create=1` を添える。**ホームのカードをタップする行為自体が「その日に足したい」という明示的な意思**であり、#141が履歴の「記録を追加」に与えたのと同じ扱いにするのが自然と判断した
  - **連続記録日数は「その日時点」の値にした。** `currentStreakDays` の「当日は未記録でも前日までの連続を継続中と数える」猶予は**当日にだけ意味がある**もので、過去日に適用すると「未記録の日に連続◯日」と出て事実と食い違う。猶予を持たない `streakDaysEndingOn` を切り出し、当日は従来どおり・過去日はその日が未記録なら0とした。過去日ではチップの文言からも「記録中」を外す
  - **未来日は開けない**(`?date=` が未来・不正なら当日に倒す)。未来の記録は存在せず、空の画面を見せても意味がないため
  - **日付は画面のstateではなくURL(`?date=`)に持たせた。** リロード・共有で同じ日が開き、記録画面から戻ったときも表示日が保たれる。ホーム側は `today` を1箇所で作って各カードに配るだけの作りだったため、この差し替えで既存のカードはそのまま動いた
  - **SNS共有カード(#235)は表示日に追従する。** 本Issueの動機が「前日の記録をXに投稿したいが、翌朝にはその画面を出せない」だったため、前日を表示してそのままカードを作れる状態にして初めて解決になる。カードの見出し・キャプションも当日以外は「この日の〜」に変えた

- **2026-08-23(#238): プライバシーポリシーの画面を追加した。** 発端は技術的な要求 — #214 のGoogle認可を実際に使うには同意画面を**本番公開**する必要があり(テストのままだとrefresh tokenが7日で失効する)、Google Cloud Console は本番公開の条件として**プライバシーポリシーURLの実在**を求める。つまり配布版(#213)を止めていたのはコードではなくこのページ1枚だった。決定:
  - **アプリ内の1画面(`/privacy`)として持つ。** 外部にページを置くと、ホームページURLとポリシーURLでドメインが分かれ、Googleの「承認済みドメイン」の所有権確認が増える。同一ドメインに収めるのが最も手数が少ない
  - **公開はエピック(`epic/distribution`)のマージ後にする。** 単独で完結するためmain直行も選べたが、そうすると同期先が「運営者のシート」である現状に合わせた但し書きが要る。#215・#216 と一緒に出せば、公開時点で「あなた自身のDriveのシート」と書ける(2026-08-23、本人の判断)
  - **連絡先はGitHubのIssueにし、個人のメールアドレスは載せない。** 公開ページであり、同意画面に出るサポートメールとは別物のため
  - **「まとめ」4行を冒頭に置く。** 法的文書としては本文だけで成立するが、配布相手が最初に読む画面になるため結論を先に出す
  - **AIに送るものだけを表にした。** 他は段落で足りるが、「日記の本文は既定で送らない(オプトイン)」は文章だと読み飛ばされ、誤解が同意の前提を壊す

## フェーズ3機能(パーソナルコンサルティング)

- **2026-08-09: 初回セットアップの必須項目を「目標3項目」に絞った(#217)。** `DEFAULT_SETTINGS` に開発者本人の減量目標(64kg / 2026-10-31 / 1900kcal)がハードコードされており、他人に配ると初期値が他人の目標として表示され続けるため撤去した。撤去に伴い「目標が空の状態」が初めて成立するようになり、その受け皿として `/setup` を新設した。主な決定:
  - **必須は目標体重・目標日・1日の目標カロリーの3項目のみ。** 身体プロフィール(#43)・PFC目標(#47)・目標水分量も同じ画面で入力できるようにしたが任意に留めた。これらは自動計算や補助表示にしか使わず、無くても記録も進捗表示も成立するため。必須にすると、入れる気の無い項目のせいで記録が始められなくなる
  - **完了判定は `isInitialSetupComplete()` に集約し、ホームのリダイレクトと画面の「はじめる」活性で共用する。** 実装過程で両者の条件が3項目/7項目に割れ、目標だけ設定したユーザーがどちらの画面からも抜けられない状態が生まれたため
  - **完了しても自動遷移しない。** 当初は必須が揃った瞬間にホームへ遷移させたが、最後の必須項目を保存した瞬間に画面から追い出され、「はじめる」ボタンにも任意項目にも到達できなくなっていた。離脱は明示操作だけにした
  - **「あとで設定する」で `initialSetupSkipped` を立てる。** 既存ユーザーの移行経路。これが無いとホームが毎回セットアップへ差し戻す。端末ごとの状態のためシート同期の対象外(`apiToken`・`lastSyncedAt` と同じ扱い)
  - **未設定はダミー値で埋めず `null` のまま扱う。** 実装過程で目標未設定時に `goalWeightKg: 0` / `goalDate: "1900-01-01"` / `calorieTargetKcal: 1` を代入する案を採ったが、(1) 推移グラフに「目標 0kg」「目標 1」が描画される、(2) digestがそのままAIプロンプトへ渡るため、AIが0kg/1900年を実在の目標として助言し、その助言はシート同期されて再生成もできない、という2点で破綻した。`WeeklyDigest`/`MonthlyDigest` の `goal.*` と `calories.targetKcal`/`daysOnTarget` を nullable にし、UI側は行・凡例ごと非表示にする方式に改めた(`water.targetMl`/`daysOnTarget` と同じ形)
  - **下部ナビは `/setup` で非表示にする**(記録フロー画面と同じ全画面構成)。表示したままだと目標未設定のまま他タブへ素通りでき、導線として成立しない

- **2026-07-15: 維持モード(目標達成後の運用)(#116)の設計を先行して固めた(実装は目標達成が近づいた段階で行う。時期到来前の設計先行)。** 週次レビュー・目標カロリー計算・AIコーチング・進捗バーの評価軸がすべて「減量前提」で目標到達後に意味を失う(必要ペースが0へ収束・着地予測が無意味化)ため、目標タイプ(減量/維持)を導入して維持期に切り替えられるようにする。設計原則(数値はコード側で決定論的に判定・自動計算値は提案に留める)は減量期と変えない。要件定義書4.7.7章・画面設計書8.2/8.3/9章・AIコンサルティング設計書(3〜5章・10章)に書き戻した。主な決定:
  - **維持レンジの表現** → 単一体重ではなく「目標体重を中心 ± 許容幅(`Settings.maintainToleranceKg`、初期値±1.0kg)」のレンジで持つ。min/max直接指定ではなく中心±幅方式にしたのは、既存の `goalWeightKg` をそのままレンジ中心に流用でき、減量→維持の移行が最小で済むため。目標日(`goalDate`)は維持期には意味を持たないため維持モードでは非表示・未使用にする
  - **モード切替** → 設定画面での手動切替に加え、週平均体重が維持レンジに入った週が続いたら「維持モードに切り替えますか?」を提案する導線を出す(自動では切り替えない)。4.7章の「提案に留める・自動で設定を上書きしない」原則をモード切替そのものにも適用した
  - **目標カロリー** → 維持モードでは自動計算(#43)を「維持カロリー = 消費カロリー(実測TDEE(#44)優先、無ければ計算式TDEE)」に切り替える。必要日次赤字・ペース超過ガードレールは維持モードでは無効化(基礎代謝クランプは論理的に残すが実質発火しない)
  - **週次レビューのフラグ** → 減量専用の `PACE_TOO_AGGRESSIVE`・`BEHIND_PACE` は維持モードでは判定せず、維持用の `ABOVE_RANGE`(レンジ上抜け=リバウンド方向)・`BELOW_RANGE`(レンジ下抜け=絞りすぎ)を新設。記録率低下・摂取が基礎代謝未満などモード非依存のフラグは共通。単週の `ABOVE_RANGE` に加え、複数週にわたる上抜けの継続は月次(#114)でリバウンド傾向として捉える
  - **AIの `verdict`** → 減量の `on_track`/`slightly_behind`/`behind` とは別に、維持用の `within_range`/`drifting`/`out_of_range` を追加する(`needs_attention` は共通)。既存4値を読み替えて流用する案もあったが、UIの色分けだけ流用でき意味は「レンジ内か」に変わるため誤解を招くと判断し、別語彙を追加した。UIの3色マップ(teal/amber/coral)は両モードの値を扱う
  - **AIプロンプト** → `digest.goal.goalType` で役割・評価軸・`verdict` 語彙を分岐する。維持期は「維持できていること自体を承認・称賛+リバウンド兆候への軽い注意」とし、再度の減量を煽らない・下抜けはむしろ緩める方向で助言する
  - **進捗バー(#25)** → 「起点→目標の到達度」バーは減量の一方向前提のため維持期に破綻する。維持モードでは維持レンジ帯の中で現在体重(直近の週平均)がどこにいるかをマーカーで見せる表示に差し替える(到達度%・残り日数・着地予測は出さない)。達成演出用のaccentは常時表示のこのバーには使わない(デザインガイドの制約は維持)
  - **月次レビュー(#114)の役割** → 維持期は「目標に向かう」局面が終わるため、必要ペース・マイルストーンをレンジ内安定度に置き換え、月単位でレンジ内に安定しているか・リバウンド傾向が無いかを俯瞰する。月次が維持期の中心ビューになる

- **2026-08-08(#174): 活動量の低下を週次レビューの警告フラグにした。「activity由来の警告フラグは設けない」という当初方針の変更。** #82の時点では「歩数・睡眠の多寡は安全判定ではなく解釈の領域」としてフラグを設けなかったが、6月以降の実測TDEEの下落(週次で2,447→直近3週の平滑値1,895)を**アプリが一度も知らせなかった**実例が出たため方針を変えた。摂取を約400kcal/日減らしても減量が加速せず(直近56日の回帰で−0.20kg/週)、原因は歩数(月平均17,995→10,811)と筋トレ(5月12日→7月0日)の低下で、削った分を消費の低下が打ち消していた。**「水準の多寡」は依然として解釈の領域だが、「過去の自分との比較による低下」は決定論的に判定できる**というのが方針変更の根拠。決定事項:
  - **`activity`/`workout` の中ではなく `activityTrend` という独立ブロックにした。** Issue本文の案は `WeeklyDigest.activity` に `prevWeeksAvgSteps` を持たせるものだったが、**筋トレが0日の週は `workout` 自体が省略される**仕様のため、`workout` の中に基準を置くと最も見せたい「中断した週」でだけ基準が消える。歩数側だけ `activity` に入れると2つの比較が別の場所に散るため、比較専用のブロックに揃えた
  - **歩数(`ACTIVITY_DROP`)と筋トレ(`WORKOUT_STOPPED`)は別フラグにした。** Issue本文では「同じフラグに寄せるか別フラグにするかは実装時に判断」としていたが、取るべき行動(歩く / トレーニングを再開する)が異なり、1つに寄せると助言が曖昧になるため分けた
  - **歩数にはデータ品質ゲート(活動記録5日以上)を課し、筋トレには課さない。** 歩数はGarminの欠測(時計を着けなかった日)がありうるのに対し、筋トレはアプリ自身の記録で「記録が無い=やらなかった」が正しいため。同じ理由で、基準4週の平均も歩数はゲートを満たす週だけ・筋トレは全週で取る
  - **`WORKOUT_STOPPED` は「直近4週の平均筋トレ日数が週1日以上」を条件にした。** もともと筋トレの習慣が無い人に「中断しました」と出さないための自己ゲートで、履歴の週数を数える条件を別途持たなくて済む
  - **`INTAKE_BELOW_BMR` との併発時の見せ方を画面・プロンプト双方に入れた(本Issueの主目的)。** 摂取が基礎代謝を下回りつつ活動量も落ちている週は、目標カロリー提案がBMRでクランプされる一方で「さらに削れ」としか読めなくなる。画面は実測TDEEカードの提案直下に「消費側の回復が必要」の注記を出し、プロンプトでは**この組み合わせで摂取削減を提案することを禁止**した
  - **月次レビュー(#114)では判定しない。** 「直近4週との比較」は週次固有の判定のため。型でも `MonthlyDigestFlag`(この2フラグを除いた語彙)を切り、月次のラベル定義に取りこぼしが出ないようにした
  - 閾値(−20%・基準4週・最低2週・品質ゲート5日)は運用しながら調整する前提の初期値として置いた
  - **実装レビューで潰した誤検知(いずれも「フラグが出るべきでない週に出る」方向の誤り)**: (1) 品質ゲートを活動記録の**行数**で数えていたが、Garmin連携のcronは歩数が取れなかった日も空欄の行を書く(`scripts/garmin/garmin_to_sheet.py`)ためゲートが素通りしていた → **歩数の値がある日数**で数えるよう修正。(2) `WORKOUT_STOPPED` に週の経過日数の条件が無く、既定表示が進行中の今週であるため週明けに必ず発火していた → 経過5日以上の週だけ判定。(3) 筋トレ平均の分母から「記録が無い週」を間引いていたため、隔週トレーニングの平均が1.0(実際は0.5)になり通常の休養週で発火していた → 過去週すべてを分母に。(4) 歩数の基準週数と筋トレの基準週数を1つの `comparedWeeks` で表していたため、画面ラベルとAIへの説明が実際の基準を偽っていた → 別々に持つ
- **2026-08-16(#210): 摂取側(目標超過・PFCの偏り)を週次レビューの警告フラグにした。#174で塞いだ消費側の穴の裏返し。** 2026-08-09、8/3週の実データ(平均1,988kcal/目標1,730kcal=+14.9%・目標以内1/7日・脂質80g/目標48g=+67%・体重は前週比+0.04kg)で、アプリもAIコーチも摂取超過について一言も触れず、AIの行動提案は「たんぱく質を目標値に近づけるよう、意識的に摂取量を高めてみましょう」になった。たんぱく質不足(94g/143g)は事実だが、1日258kcal超過している週に「摂取量を高めて」は字面として妙で、原因はflagsに摂取側の判定が無くAIが根拠を持てなかったこと。決定事項:
  - **3つのフラグに分けた(`INTAKE_OVER_TARGET`・`FAT_OVER_TARGET`・`PROTEIN_UNDER_TARGET`)。** Issue本文の例では総超過258kcalが脂質+32g(288kcal)でほぼ説明がつき、「総カロリーを見るより脂質1項目を見るほうが行動に効く」週だった。総カロリーの超過と脂質の超過を同じフラグにすると「何を減らせばいいか」がぼやけるため、#174の歩数/筋トレと同じ理由で分けた
  - **たんぱく質は不足側だけをフラグ化し、炭水化物・脂質不足はフラグ化しない(実装中のレビューで追加検討)。** 当初は `FAT_OVER_TARGET` のみだったが、「たんぱく質・炭水化物の超過/不足も出すべきか」を検討して線を引いた。**たんぱく質不足は入れる** — 減量中の筋量維持に直結し、取るべき行動も明確(たんぱく質を増やす)で、かつ当初実装では `INTAKE_OVER_TARGET` との併発時しか拾えず「摂取が目標内に収まっている週の単独のたんぱく質不足」がAIに届かない穴が残っていた(#210の元の構図そのもの)。**炭水化物は入れない** — 超過側は `INTAKE_OVER_TARGET` がほぼ捕捉し、PFCの中で炭水化物は「P/Fを決めた残り」の性格が強く単独では行動が定まらない。不足側は意図的な低炭水化物運用がありえて「不足=問題」と断定できず、血圧の高値・activityの水準をフラグ化しなかったのと同じ「解釈の領域」にあたる。**脂質不足も同様に見送り**(実運用で起きにくく、起きても `mostOffTargetNutrient` が事実として拾う)。対称性のためにP/F/C×超過/不足の6通りを揃えるのではなく、行動に結びつくものだけをフラグ化するという既存の判断基準を踏襲した
  - **たんぱく質不足の閾値は目標比 −20% とした。** 目標が体重1kgあたり2.0g前後で設定される前提だと、−20%は減量中の推奨下限(1.6g/kg)にあたる。超過側は多くても実害が乏しいため判定しない
  - **`verdict` の判定規則が全フラグを網羅していない穴を塞いだ。** `FAT_OVER_TARGET` を追加した時点では規則のどこにも現れておらず、このフラグだけが単独で立った週は「flagsが空 → `on_track`」にも当てはまらず `verdict` が決まらない状態だった。`ACTIVITY_DROP`・`WORKOUT_STOPPED` と同じ `slightly_behind` の行にまとめ、あわせて **`DigestFlag` の全値が判定規則に現れることを検証するテスト**(`weeklyAdvice.test.ts`)を追加した。フラグ追加時に見落としやすい種類の欠落のため、番人を置いて再発を防ぐ。**この番人の対象リストは `Record<DigestFlag, true>` として持ち、足し忘れがコンパイルエラーになるようにした** — 手書きの配列にすると「フラグを増やしたときリスト側の更新も忘れる」という、まさにこの番人が防ぎたい漏れが番人自身に起きるため。テストから `src/types.ts` を型のみimportしているが、Workerがクライアントのコードをimportしない方針はバンドルの独立性を保つためのもので、(a)テスト専用で本番バンドルに入らない (b)型のみで実行時コードを生まない、の2点から対象外とした(`ALL_DIGEST_FLAGS` を実行時に読むためにnode の `fs` を使う案は、worker の tsconfig に node 型を足すことになり「Node のライブラリではなく Web Crypto を使う」というガードレールが緩むため採らなかった)
  - **実装中のレビューで潰した2件(いずれもフラグが「出るべきでない状況で出る/誤った助言になる」方向の誤り)**:
    - (1) `INTAKE_OVER_TARGET` に記録日数ゲートが無く、**記録2日・両日とも超過の週で `daysOnTarget=0` となり発火**していた。当初は「記録の少なさは `LOW_RECORDING_RATE`/`INSUFFICIENT_DATA` が別途担う」として意図的にゲートを設けなかったが、この判断は**フラグが表示上は併記される前提**のもので、verdict規則では `INTAKE_OVER_TARGET`(先頭・`needs_attention`)が `INSUFFICIENT_DATA`(`slightly_behind`)を**上書きする**ことを見落としていた。結果として「データが少ない週は評価に適さない」という既存の防御を突き破るため、食事記録5日以上のゲートを追加した
    - (2) 「脂質を減らしてたんぱく質に置き換える」という文言を `INTAKE_OVER_TARGET` × `PROTEIN_UNDER_TARGET` の2条件だけで出しており、**炭水化物主導の超過で脂質が目標未満の週でも「脂質を減らせ」と言ってしまう**状態だった(すぐ上のPFCバーが脂質未達を示しているのと矛盾する)。画面は `FAT_OVER_TARGET` の有無で文言を出し分け、プロンプトは「減らす対象として名指ししてよいのは実際に目標を超過している栄養素だけ」と条件付きにした
  - **`FAT_OVER_TARGET`・`PROTEIN_UNDER_TARGET` が `slightly_behind` に落ちる点は許容する。** ペースが順調でカロリーも目標内の週でも、PFCの偏りだけで「やや遅れ」になりうるが、これは `LOW_RECORDING_RATE`・`ACTIVITY_DROP` など既存のamber系フラグと同じ挙動(ペースと独立に注意を促す)であり、`verdict` は総合的な注意度の要約という位置づけのため意図どおり
  - **`INTAKE_OVER_TARGET` の判定は「平均10%超過」と「目標以内の日が2日以下」のANDにした。** Issue本文のたたき台どおり採用。たまたま1日大きく食べただけの週(平均は上がるが他の日は守れている)と、週を通して超え続けた週を区別するため。追加の記録日数ゲート(`LOW_RECORDING_RATE`相当)は設けなかった — `daysOnTarget`は食事記録がある日を分母にした値で、記録の少なさそのものは既存の`LOW_RECORDING_RATE`/`INSUFFICIENT_DATA`が別途担うため、二重にゲートを課すと初期値の調整が複雑になると判断した
  - **`FAT_OVER_TARGET`は脂質単独で判定し、閾値は目標比+30%とした。** Issue本文は「少なくとも脂質の大幅超過は判定対象にする」とし、たんぱく質・炭水化物は見送った(脂質が最も「見えない超過」になりやすく、行動への効き目も大きいため)
  - **「どの栄養素が最も外れているか」を`pfc.mostOffTargetNutrient`としてflagsとは独立に常時計算する項目にした。** Issue本文は「超過の中身が分かると行動が具体的になる」というdigestへの追加要望で、フラグ化は求めていなかったため、フラグとは別の事実提示項目とした。超過側・不足側を問わず`(実績-目標)/目標`の絶対値が最大の栄養素を1つ返す(`aggregatePfcDeviation()`)
  - **AIプロンプトに「摂取量を増やしてとだけ言わせない」制約を追加した(Issue本文の主目的)。** `INTAKE_OVER_TARGET`と`PROTEIN_UNDER_TARGET`が同時にある週は、「脂質を減らしてたんぱく質に置き換える」方向の提案に固定する。同じ趣旨の注記を週次レビュー画面のPFCカードにも追加した(`WeeklyReview.tsx`の`needsProteinForFatSwap`)。**判定をフラグの組み合わせに寄せたことで、#174の`INTAKE_BELOW_BMR`×`ACTIVITY_DROP`と同じ形に揃った**(当初は画面側で`avgProteinG < targetProteinG`を直接見ていた)。verdictの判定規則にも`INTAKE_OVER_TARGET`を`needs_attention`側(`PACE_TOO_AGGRESSIVE`・`INTAKE_BELOW_BMR`と同格)として追加した
  - **月次レビュー(#114)では判定しない。** `MonthlyDigest`がまだ`pfc`(週内平均)を持たず、`FAT_OVER_TARGET`・`PROTEIN_UNDER_TARGET`の判定材料が無い。総カロリー超過(`INTAKE_OVER_TARGET`相当)だけなら`calories`で判定可能だが、3フラグを分けて一部だけ月次に出すと画面・AIの語彙が週次と食い違うため、まとめて月次への適用を見送り、`MonthlyDigestFlag`から明示的に除外した(Issue本文で「別途判断する」としていた論点への回答)
  - 閾値(10%・2日以下・脂質+30%・たんぱく質−20%)は運用しながら調整する前提の初期値(#174の閾値群と同じ扱い)

- **2026-08-23(#219): AIへの送信に同意の導線を作った(配布 #213 の一部)。** 自分ひとりなら自分のデータを自分で送っているだけだが、配ると他人の健康データを第三者へ送ることになる。決定:
  - **歯止めを画面ではなく送信経路(`assertAiConsent`)に置いた。** 画面の分岐だけだと、AIの呼び出し口が増えたときに素通りする経路ができる。UIのゲート(`useAiConsentGate`)は「聞く」ための入口で、送信を止めるのはAPI層
  - **同意はAI機能だけを止める。** 記録・グラフ・同期は同意なしで全部使える。このアプリの中心は記録で、AIコメントは補助のため(要件定義書3章の優先度)
  - **同意状態はシート同期に載せない。** 「この人が同意した」という事実であって記録ではない。バックアップJSONには入るので機種変更では引き継がれる
  - **文言はプライバシーポリシー(#238)4章と同じ内容にした。** 同じことを2つの言い方で書くと、片方を直したときにもう片方が嘘になる
  - **同意ダイアログは送るもの・送らないものを一覧で出す。** 特に「日記の本文は既定で送らない」は文章に混ぜると読み飛ばされ、誤解が同意の前提を壊す

## フェーズ4機能(生活ログへの拡張)

- **2026-07-14: フェーズ4(生活ログへの拡張)を新設し、血圧記録(#117)・周囲径記録(#118)・汎用の習慣トラッカー(#113)を実装した(2026-07-13の検討より)。** いずれも「からだログ」のスコープ(体の記録)を超え始める拡張で、既存パターン(データ層1ファイル+単一ファイルの記録画面+シート1タブの同期・取り込み)に小さく乗せる方針とし、アプリの土台は作り替えない。要件定義書3章のフェーズ表・4.8章に追記した。
- **血圧記録(#117)のデータモデル** → 体重記録と同型(1日1件・日付キー・後勝ち)。最高血圧・最低血圧を必須、脈拍を任意とした。家庭血圧の基準に合わせ「朝の測定値」を記録する運用を想定(体重測定と同じ朝のリズムに揃う)。
- **血圧の医療上の線引き(#117。重要)** → アプリは医療機器ではないため、警告は「家庭血圧135/85以上の日が◯日」のような事実の提示に留め、コード・画面・AIのいずれも医学的判断(診断・受診勧奨・薬の提案)をしない方針で決定。閾値135/85は高血圧治療ガイドラインの家庭血圧の値を「高値の目安」として使うだけで、判定・断定には使わない。血圧由来の新しい警告フラグ(`DigestFlag`)は設けない(高値の判断は医学的評価にあたるため)。AIコーチングにも同じ免責をプロンプトで課した(AIコンサルティング設計書5章)。
- **「体重×血圧」の見せ方(#117。#112のクロス分析の軸)** → 既存のクロス分析(`crossAnalysis`)は「各条件 × 摂取カロリー」の構造で血圧と噛み合わないため、`crossAnalysis` を作り替えるのではなく、`WeeklyDigest.bloodPressure` に同じ週の週平均体重(`weekAvgWeightKg`)を併記して体重と血圧を並べて見せる形にした。医学的に確立した体重×血圧の関係を「事実の並置」で提示する。月次では週平均体重の併記はせず、体重の推移は月次の体重セクションで見せる。
- **周囲径記録(#118)のスコープ** → まずは腹囲を必須とする最小構成に、胸囲・太ももを任意項目として加えた(将来の拡張余地を型に残す)。入力頻度が月1程度と体重・血圧と異なるため専用の記録画面を持ち、推移グラフは点が疎でも破綻しないよう1点のときは線を引かず点のみ描く。低頻度のため推移グラフは期間フィルタに関わらず常に全期間を表示する。週次レビューへの統合は入力頻度が合わないため見送り、月次レビュー(#114)への統合は今後検討とした。
- **2026-07-14: 周囲径記録の画面ラベルを「サイズ」に柔らかくした** → 「周囲径」は測定用語で硬いため、UIに出る文言だけを「サイズ」(主フィールドの腹囲は「おなか周り」)に置き換えた。内部の型名 `BodyMeasurementRecord`・同期シートのタブ名「周囲径記録」・スキーマ上の列名(`腹囲(cm)` 等)はデータ契約のため変更せず、既にシートが作られていても整合が崩れないようにした(表示と内部名を分離)。仕様書上の機能名も、issue・シートタブと対応がたどれるよう「周囲径記録(#118)」を正の名称として残し、表示ラベルが「サイズ」である旨を併記する形にした。
- **習慣記録(#113)の主キー設計** → 「1日1習慣1件・後勝ち」を`put()`だけで成立させるため、`id = ${date}_${habitId}` の合成キーにした(体重・日記の日付キーと同じ考え方を2軸に拡張)。チェックを外す操作は記録の削除(トゥームストーン)で表し、記録の存在が「その日にやった」を意味する設計にした(`done`のような真偽フラグは持たない)。習慣名はシートの可読性のために`HabitRecord`へ非正規化コピーする(筋トレの`exerciseName`と同じ)。薬・サプリの服用記録(#113コメント)は専用機能を作らず、汎用の習慣として扱う(「血圧の薬」「プロテイン」等を習慣マスタに登録)。
- **フェーズ4タブの自動作成(#117・#118・#113)** → 血圧・周囲径・習慣マスタ・習慣記録の4タブは後付けで既存スプレッドシートに存在しないため、マスタ2タブ(#96)と同じく初回同期時にWorkerがヘッダー行付きで自動作成する方式にした。記録5タブ(体重・食事・水分・筋トレ・日記)の「手動作成が前提」という扱いは変えていない(これらはフェーズ1〜2から存在するため)。旧Workerとの後方互換のため、`SyncPushResult`/`SyncPullResult` に追加した同期済みID・取り込みレコードのフィールドはすべて省略可(`?`)とし、未対応の旧Workerが返さなくてもクライアントは空として扱う(マスタ2タブ導入時と同じ方針)。
