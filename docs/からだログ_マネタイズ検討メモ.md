# からだログ マネタイズ検討メモ

作成日: 2026-08-05
最終更新日: 2026-08-27(17章「noteアクセス状況6週分の評価」を追記し、7章補足に更新先の注記を追加。2026-08-26 に16.9「その後に分かったこと」・14章「実行フェーズへの移行」・15章「発信の実測値による13章の判定と、ゲート運用の失敗」・16章「需要検証インタビューの回答」を追記、8章のnote1本目の書き出しの項目を完了に更新)
関連ドキュメント: からだログ_要件定義書.md、からだログ_意思決定ログ.md、content-ops の README.md・content-ops の docs/発信実測記録.md(発信の到達実績の実測値はこちらが正。15章が参照する)

**14章と15章は、どちらも 2026-08-25 に別々の作業で書かれ、いずれも「14章」を名乗っていた。** 内容は矛盾しないため、**先に書かれた「実行フェーズへの移行」を14章、後に書かれた「発信の実測値による13章の判定」を15章**として 2026-08-26 に採番し直した(15章の節番号 15.1〜15.10 は、この振り直しに伴って 14.x から変更したもの)。content-ops の README が参照している 14.5・14.9・14.10 は本ファイルの14章を指しており、番号は変わっていない。

本メモは、個人開発+SNS発信で月10万円を目指すにあたっての検討結果を、Claude Code 側で引き継ぐためにまとめたもの。**決定事項ではなく検討のたたき台**であり、確定した仕様は要件定義書、決定の経緯は意思決定ログが正。

1〜9章はチャットでの検討結果をそのまま持ち込んだもの。10章は、その内容をリポジトリの実装・仕様書・Issue/PRと突き合わせて検証した結果(2026-08-05、Claude Code)。

---

## 1. 前提と目標

- 目標: **月10万円**
- 稼働: 週10時間程度
- 発信チャネル: X(@yotti073)、note(4本)、Zenn(4本)。いずれも初期段階
- ルート選択: **受託寄りではなくプロダクト寄り**を選択
  - 受託(時給5,000円×週5h)なら半年で到達可能だが、資産にならない
  - プロダクト寄りは **2年計画**。これを最初に受け入れることが前提条件

### 目標の内訳(想定)

| 項目 | 想定 |
|---|---|
| 価格 | 月1,500円 |
| 必要人数 | 70人(月105,000円) |
| マイルストーン | 3ヶ月: 継続利用10人 → 半年: 有料化 → 1年: 30人 → 2年: 70〜100人 |

月500円×200人より、**ニッチで単価を上げて70人**の方が個人開発では現実的。

---

## 2. 対象プロダクトの選定

### 選定基準

1. **自分が使い続けられること**(週10時間しか使えない前提では、使わないアプリは1年もたない)
2. **自分なら月1,500円払ってでも使いたいと言えること**(ここにYesと言えないなら他人は払わない)

### 候補の評価

| 候補 | 評価 |
|---|---|
| **からだログ** | **本命。** 体重・食事の記録が前システム含め約90日継続しており、基準1を満たす唯一の実績 |
| Tech Stack Log | 勉強目的としては良いが、**エンジニア向けは最も課金しない層**。マネタイズの本命には据えない |
| データハブ構想 | からだログと同じ「収集→分析」型のため、別物として立てる必要はない |

### 想定ターゲット

**増減量サイクルを回す筋トレ層。** 「減量したい一般層」ではない。あすけん等が押さえているのは後者で、価格帯も市場も異なる。

---

## 3. 無料/有料の境界

要件定義書の章立てが、そのまま課金の線と一致している。

| 区分 | 該当機能 |
|---|---|
| 無料 | 記録+推移グラフ(4.1体重 / 4.2食事 / 4.4筋トレ / 4.5水分 / 4.6日記) |
| 有料 | **4.7 パーソナルコンサルティング**(4.7.1 目標カロリー自動計算 / 4.7.2 実測TDEE推定 / 4.7.3 週次レビュー / 4.7.4 記録率可視化 / 4.7.5 PFC目標 / 4.7.6 AIコーチング)、月次レビュー(#114)、クロス分析(#112) |

**「記録は無料、評価と助言は有料」。**

この線が有効な理由は、4.7.2 の実測TDEEが「記録が溜まるほど精度が上がる」構造を持つこと。**解約すると蓄積の価値がゼロに戻る**ため、サブスクとして自然にロックインが効く。要件定義書が「コンサルティング機能の数値的な心臓部」と位置づけている機能が、そのまま課金の心臓部になる。

---

## 4. 競合認識の再評価(#67の再検討)

### #67 で対応不要とした理由(意思決定ログ 2026-07-11)

- あすけん等が月額300円前後でAI写真判定+管理栄養士コメント+巨大食品DB+PFC管理を提供
- 個人開発の構造的弱点(食品DB・AI精度)に直撃し、技術的な堀にならない
- SaaS化には認証基盤・課金・法務(特商法、健康データ=要配慮個人情報)対応が必要

### 再評価

**この判断は「無料配布を前提とした場合は」正しい。有料化を前提にすると計算が変わる。**

| 論点 | 再評価 |
|---|---|
| 競合との衝突 | あすけんが強いのは食品DB・AI判定という**入力レイヤー**。からだログの武器は実測TDEE逆算・週次/月次レビュー・クロス分析という**評価レイヤー**で、ここは競合が弱い。正面衝突ではない |
| 月額300円との比較 | 300円は「減量したい一般層」の価格帯。増減量サイクルを回す層に月1,500円を出させる話とは市場が異なる |
| 法務コスト | **ここは有効な指摘。** 他人の健康データを自分のインフラで預かるのは個人開発には重い |

### 法務コストの回避案

**ローカルファースト構成を維持したまま有料化する。**

- データは相手の端末(IndexedDB)と相手のスプレッドシートにのみ置く
- 自分が保持するのは**メールアドレスと課金状態のみ**
- Worker中継はダイジェストを通過させるだけで健康データを保存しない
- → #67 で法務コストを理由に避けたSaaS化に踏み込まずに月額課金が成立する

**原価構造**: Gemini Flash-Lite級の週次生成はコスト誤差レベル、Cloudflare Workersも無料枠内。70人課金すればほぼ丸ごと粗利として残る。

### 未検証の論点

- 特商法表記(個人事業としての表示義務をどう満たすか)
- プライバシーポリシー・利用規約の整備
- 決済(Stripe等)とライセンス判定の実装量
- 公開リポジトリ+MITライセンスとの整合(下記6章)

---

## 5. リテンションの最大リスク

### 目標日(10/31)以降(#163・#116)

**目標を達成した瞬間に使わなくなるなら、90日の継続実績ごと本命が消える。**

これは自分自身の継続の問題であると同時に、**課金ユーザーが解約する理由そのもの**。減量アプリが儲かりにくい構造的な原因がここにある。

→ 現在の着手順は #164 → #161 → #162 だが、**マネタイズを軸に据えるなら #116(維持モード)を前に出す価値がある。**

### 日記が続かない問題(4.6)

| 機能 | 継続状況 | 理由 |
|---|---|---|
| 体重・食事 | 継続中(約90日) | 入力が数秒で終わり、数値として自動で積み上がる |
| 日記 | 継続していない | 入力に思考が必要なのに、書いた直後のリターンが無い。**コスト先払い・リターン後払い** |

さらに要件定義書 5.3 で日記本文はデフォルトでAIに送らない設計のため、**書いても評価ループに乗らない**。

**開発者本人が続かないものは、他人はまず続かない。** 「頑張って書かせる」方向での改善は打ち切る。

対応案:

1. **削る** — 続く機能に絞って記録の完成度を上げる
2. **生成に変える** — 週次の振り返りをAIが書き、本人は読んで一言直すだけにする(書くもの → 確認するもの)
3. **#162 と統合する** — 日記欄を「その日の相談を書く欄」に変え、書く→即返事の構造にする

3 を推す。#161・#162 は同じデータ契約の問題として同時検討する方針が既にあるため、そこに日記の位置づけを含める。

---

## 6. リポジトリ公開との整合

現状: `github.com/yotti773/lifelog` は公開・MITライセンス(スター0・ウォッチャー0)。

- コードが読めて自己ホストできるため、**エンジニアを客にするなら課金は成立しない**
- ただし**増減量サイクルを回す筋トレ層が客なら実害はほぼ無い**(彼らはCloudflareにデプロイしない)
- 公開のままの方が発信の説得力が高い

→ **公開は維持し、課金判定まわりのみ別リポジトリ(プライベート)に切る**のが落としどころ。

スター0は、note・Zennの記事からリポジトリへの導線が効いていないことを示している。記事本文にリポジトリリンクを置くだけで改善する余地がある。

---

## 7. 次のアクション: 需要検証

**実装より先に需要を検証する。** これは #165 で「配る相手が実在するかの確認が先で、需要がない状態で基盤を整えるのは #67 と同じ誤りを繰り返す」と判断した原則をそのまま適用したもの。

### 検証仮説

> 「増減量サイクル向けの、記録が溜まるほど精度が上がるTDEEコーチ」に月1,500円を払う人が実在するか。

### 手段: 実測TDEEを軸にした記事(未執筆・次に書く予定)

| チャネル | 内容 | 役割 |
|---|---|---|
| **note** | 数式を出さず「なぜ計算通りに減らないのか」。課題提示型 | **課金候補が読む側。事前登録の導線をここに置く** |
| **Zenn** | 逆算式・データ品質ゲート・平滑化の実装解説 | 読者はエンジニアで課金しないが、信用とシェアを生む |

**noteのフック**: 計算式のTDEEと、90日の実データから逆算した実測TDEEがどれだけズレたか。この数字は自分にしか出せず、減量経験者には全員心当たりがある。

**タイトル**: 「実測TDEE」では検索されない。「減量 停滞 カロリー」等の検索語から入り、中身で概念名を教える順序にする。

### 合格基準(ゲート)

- 見るのは**PVではなく事前登録数**。PV200で登録10なら合格
- **1本で判断しない。** 3週で3本出して累計10人がゲート
- 集まらなければ実装は1行も行わず、次の仮説へ移る

### 補足: 記事の伸び方の実績

note全体で週20〜30ビュー。1本目の課題提示型記事が3週連続で読まれ続けている一方、週次レポート形式は公開週のみで失速している。**課題提示型を主軸にし、レポート形式は比率を下げる。**

> **→ この節は 17.1 で6週分の実データにより更新されている(2026-08-21観測)。** 「課題提示型を主軸にする」という結論は初速3〜5倍という形で裏が取れたが、**「3週連続で読まれ続けている」は note01 固有で、後続のストック記事では再現していない**(n=1 の一般化だった)。上の文面は 2026-08-05 時点の記述として残してある。

---

## 8. 未決事項

- [x] noteの1本目の書き出しを「停滞した話」と「計算が合わなかった話」のどちらにするか(刺さる読者層が変わる) — **2026-08-20公開済み。** 10.6のとおり「減量が停滞する原因は、消費カロリーの見積もりでした」として、**停滞から入り消費カロリーの見積もりに着地する**形で出した。「計算が合わなかった話」を計算式(BMR×活動係数)とのズレで書く選択肢は、10.5-(1)で実データでは主張できないと判明したため残っておらず、乖離の相手はウェアラブルになっている。実績は15.1(48ビュー・事前登録0)
- [ ] #116(維持モード)を #164 より前に出すかどうか
- [x] 事前登録の受け口をどう作るか(フォーム / メール収集の方式) — **2026-08-06完了。** 10.3の推奨どおりGoogleフォーム1枚。導線は note06 本文と、11.3-(1)により需要検証期間中の週次レポートにも置く。**流入元は掲載先ごとのプレフィルURL(`entry.1305790066`)で区別する** — フォーム1枚のままだと記事3本+週次の登録が混ざり、10.2-(6)の「PV未達=判定不能」の分岐が機能しないため。値と貼り付け用URLは `content-ops` の `README.md`「事前登録フォームと流入元の計測」が正
- [ ] 特商法・プライバシーポリシー対応の実作業量の見積もり(→ 12.5。工数ではなく「自宅住所・電話番号を公開するか」の意思決定が本体)
- [ ] 課金判定の実装方式(Stripe + ライセンスキー)と、その置き場所(別リポジトリ)(→ 12章。前提条件は 10.2-(3) に加えて 12.2・12.4)

---

## 9. 判断の要点(要約)

1. **プロダクト寄りは2年計画。** それを受け入れられるかが前提
2. **本命はからだログ。** 90日続いている実績が唯一の根拠
3. **課金の線は「記録は無料、評価と助言は有料」。** 要件定義書4.7がそのまま有料機能
4. **#67の再検討は必要だが、ローカルファーストを捨てずに有料化する**ことで法務コストの大半は回避できる
5. **最大のリスクは10/31以降。** 維持モードはリテンションと解約防止の両方に効く
6. **実装より先に需要検証。** 記事3本で事前登録10人がゲート

---

## 10. リポジトリ側からの検証(2026-08-05)

1〜9章の主張を、実装コード・仕様書・Issue/PRの実物と突き合わせた結果。**裏が取れたもの**と**修正・追記が要るもの**に分ける。

### 10.1 裏が取れたもの

- **着手順 #164 → #161 → #162**(5章)は要件定義書7章・意思決定ログ(2026-08-03)の記載どおり。
- **#165 の「配る相手が実在するかの確認が先」**(7章)も意思決定ログの記述どおりで、需要検証を先に置く判断はこの原則と整合する。#165 は 2026-08-03 に `not planned` でクローズ済み。
- **開いたままのPR 7件**(8章)の内訳は #170・#169・#157(記事系)、#152・#150・#149(dependabot)、#125(維持モードの設計書き戻し)。うち3件はdependabotで、判断を要するのは実質4件。

### 10.2 修正が要るもの

#### (1) 4.7.4(記録率の可視化)は無料側に置く

3章は 4.7 を丸ごと有料に置いているが、**4.7.4 だけは無料側に残すべき**。4.7.4 の実体はホーム画面の連続記録日数で、無料ユーザーを毎日アプリに戻す唯一の仕掛けになっている。ここを有料にすると無料版が「入力しても何も返ってこない箱」になり、有料転換の前に無料の継続自体が死ぬ。要件定義書 4.7.4 自身が「記録の中断は減量記録の最大の失敗要因」と書いている。

逆に **4.7.1(目標カロリー自動計算)は有料側でよい**。`Settings.dailyCalorieTarget` は手動設定値として独立しており(`src/types/index.ts`、`src/db/settings.ts` の `DEFAULT_SETTINGS`)、自動計算はそこへ提案を書き込むだけの構造(4.7章の設計原則2「自動計算値は提案。設定値を自動で上書きしない」)。したがって無料側でも手動入力で 4.2 の「目標カロリーとの差分表示」は成立する。

→ 線の言い換え: **「記録と、その日のうちに閉じる数字は無料。週以上の窓で評価するものは有料。」** この方が「記録は無料、評価と助言は有料」より境界が実装に沿う。

#### (2) 「Worker中継は健康データを保存しない」は同期経路には当てはまらない

4章の法務コスト回避案は、**AIコーチング経路(ダイジェストのみ通過)には成立するが、スプレッドシート同期経路には成立しない。**

- `GOOGLE_SHEETS_SPREADSHEET_ID` は Worker のシークレット**1つ**(`worker/index.ts:25`)で、全リクエストが同じスプレッドシートに書き込む。このまま他人に配れば全員の健康データが自分のシートに混ざる — #165 が「相乗り案」の問題として挙げた構造そのもの。
- 「データは相手のスプレッドシートにのみ置く」を成立させるには、(a) スプレッドシートIDをユーザーごとに持つ、(b) そのシートにサービスアカウントを編集者として招待してもらう、の両方が要る。**(b) は非エンジニアには重く**、#165 が「エンジニア以外に現実的でない」と判断した障壁そのもの。
- 代わりにサービスアカウント側でシートを作って相手に共有すると、**シートの所有者が自分になり**「自分が保持するのはメールアドレスと課金状態のみ」が崩れる。

→ **法務コストの回避は「ローカルファーストだから」で自動的には成立しない。同期先シートの所有者を誰にするかで決まる。** 4章で最も詰まっていない点であり、需要検証を通過した後に最初に潰すべき論点。

#### (3) 認証も単一ユーザー前提のまま

`worker/auth.ts` はシークレット `API_AUTH_TOKEN` **1本との一致**を見るだけ(ファイル冒頭に「単一ユーザー前提の最小構成」と明記)。課金判定を入れるなら、トークンのユーザー別発行・失効・課金状態との突合が必要で、これは 6章の「課金判定まわりのみ別リポジトリに切る」では収まらず、**Worker本体(`worker/auth.ts`・`worker/index.ts`)の改修**になる。8章の実装量見積もりはこれを含める必要がある。

#### (4) #116 は設計が既に終わっていて、PR #125 が開いたまま

5章は「#116(維持モード)を前に出す価値がある」としているが、**#116 の設計は既に完了しており、書き戻しPR #125 が 2026-07-15 から未マージのまま開いている**(要件定義書4.7.7章の新設、画面設計書8章・8.2章・8.3章・9章、AIコンサルティング設計書、意思決定ログへの追記。コード変更なし)。

→ 「順序を上げるか」は実質「**PR #125 をマージして実装に入るか**」であり、ゼロからの検討ではない。順序を上げるコストは5章の想定より低い。

#### (5) 需要検証のゲートが、現在の運用ルールと衝突する

7章のゲート「3週で3本、累計登録10人」は、`content-ops` の `README.md` の運用ルール「**週1の定型レポート+月1〜2本のストック記事を上限**」「急いで収益化を狙わず淡々と継続する」(2026-07-16 本人決定)と衝突する。週1定型を維持したまま課題提示型を3週で3本出すと 3週で6本になり、上限の倍。**どちらを下げるかを決める必要がある**(未決事項に追加)。

#### (6) 合格基準の分母が現状の10倍以上を要求している

7章の合格基準「PV200で登録10」は、同章の実績「note全体で週20〜30ビュー」から見て**分母側が10倍近い飛躍**を前提にしている。このままだと、ゲート未達のときに「仮説が外れた」のか「単に読まれていない」のかを分離できず、次の仮説へ移る判断そのものを誤る。

→ **PVが閾値に届かなかった回は「判定不能」として扱い**、仮説の棄却ではなく配信側(X投稿・タイトル・タグ・公開時間)の改善に回す、という前段の分岐を足しておく。これは `content-ops` の `README.md` の「ビュー数を構成変更の評価指標にしない」(2026-08-04)とも整合する扱い。

### 10.3 8章「未決事項」への推奨

| 未決事項 | 推奨 | 根拠 |
|---|---|---|
| noteの書き出し | **本人判断**(刺さる読者層が変わるため代理決定しない) | 7章の「課金候補が読む側」という位置づけ次第 |
| #116 を前に出すか | **出す。まず PR #125 をマージ** | 設計は完了済み(10.2-(4))。残るのは実装のみ |
| 事前登録の受け口 | **Googleフォーム1枚**から始める → **2026-08-06に作成済み**(8章参照) | 実装ゼロで今週から回せる。専用ページは登録が付いてから |
| 特商法・プラポリの工数 | **需要検証の通過後まで見積もらない** | 7章の原則(実装より先に需要検証)を工数見積もりにも適用する |
| 課金判定の実装方式 | **同上。ただし 10.2-(2)(3) を前提条件として記録しておく** | 別リポジトリ分離では収まらないことが確定しているため |
| 開いたままのPR 7件 | dependabot 3件は先に処理、#125 はマージ、記事系3件は需要検証と一体で判断 | 10.1 の内訳より |
| **(追加)週1定型と課題提示型3本の両立** | どちらを下げるか要決定 | 10.2-(5) |
| **(追加)同期先シートの所有者をどうするか** | 需要検証の通過後、最初に潰す論点 | 10.2-(2) |

### 10.4 このセッションで決めたこと(2026-08-05)

- **記事の配分**: 週1定型レポートは維持し、課題提示型は「3週で3本」→**「6週で3本」**に伸ばす(10.2-(5)の衝突の解決)。ゲート(累計事前登録10人)は据え置き、判定までの期間だけ倍に取る。`content-ops` の `README.md` に書き戻し済み
- **ゲート判定の前段**: PVが閾値に届かなかった回は仮説の棄却ではなく**判定不能**として扱い、配信側の改善に回す(10.2-(6))。同上に書き戻し済み
- **需要検証1本目の初稿を作成**: `content-ops` の `note/note06_実測TDEE_下書き.md`。3つの消費カロリー(計算式の推定値・実データからの逆算値・Garminの実測消費)が最大1,361kcal食い違っていた、という実データを軸にした課題提示型
- **8章の未決事項1(書き出し)**: **本文の書き出しは「計算が合わなかった話」、タイトルは「停滞」の語彙**という両取りで構成した。「停滞した話」から入るとリーチが減量したい一般層に寄り、2章が想定ターゲットから明示的に外した層と噛み合わないため。判断を変える場合は書き出し3段落の差し替えで足りる形にしてある

**引き続き未決**(記事の公開前に必要): ~~事前登録フォームの作成とURL~~(2026-08-06完了)、計算式TDEEの実値確認(下書き内の2,335kcalは導出値)、~~サムネイル~~(作成済み)、対になるZenn記事の要否。

### 10.5 実データによる検証と、訴求点の修正(2026-08-05)

需要検証1本目の執筆にあたり、同期用スプレッドシートの実データ(2026-05-18〜08-02の77日間、体重93日・食事80日・Garmin活動92日)で7章のフックを再計算した。**結果、7章のフックはそのままでは成立しなかった。**

| 消費カロリーの出どころ | 値 | 実際の体重変化との整合 |
|---|---|---|
| Garminの実測消費 | 2,857kcal/日 | 正しければ77日で **9.0kg** 減るはず |
| 計算式の推定値(導出値・要確認) | 約2,335kcal/日 | 正しければ77日で **3.7kg** 減るはず |
| 実データからの逆算(全期間) | 2,210kcal/日 | 正しければ77日で **2.5kg** 減るはず |
| **実際の体重変化** | — | **−2.49kg** |

#### (1) 「計算式のTDEEが大きくズレる」は、このデータでは主張できない

7章のnoteのフックは「計算式のTDEEと実測TDEEがどれだけズレたか」だった。しかし**77日をまとめて逆算すると2,210kcal/日で、計算式の2,335との差は125kcal/日**しかない。大きく見えていたのは直近週の平滑化値(1,861〜1,896)との比較だが、**週次の逆算値は最小1,572〜最大3,447・標準偏差530でブレる**ため、この差はノイズの範囲に収まる。

#### (2) 一方、ウェアラブルとの乖離は断定できる

Garminとの乖離647kcal/日は、**体重の実測が反証する**(その消費が正しければ9.0kg減っていたはずが、実際は2.49kg)。感想ではなく算数として示せる唯一の乖離であり、記事の柱はここに移した。

#### (3) 訴求点の修正(3章・7章に効く)

3章は実測TDEEを「記録が溜まるほど精度が上がる」ロックインの核と位置づけているが、**このデータで示せる優位性は「計算式に対して」ではなく「ウェアラブルに対して」だった。**

- 計算式(BMR×活動係数)に対する優位は、少なくとも本人のデータでは**125kcal/日程度**で、有料の理由としては弱い
- **スマートウォッチの消費カロリーを見て食事量を決めている層**に対しては、647kcal/日の幻の赤字を潰せるという明確な優位がある。増減量サイクルを回す層(2章の想定ターゲット)はまさにウェアラブル利用率が高く、**ターゲット設定とは噛み合う**
- ただし「記録が溜まるほど精度が上がる」は**維持**できる。週単位では標準偏差530でブレ、窓を広げるほど水分変動・記録漏れが平均されて消えるため、蓄積が精度になる構造そのものは実データで裏づけられた。3章のロックインの論拠はこちらに置き換える

→ **需要検証の仮説文(7章)を「計算式とのズレ」から「ウェアラブルとのズレ」に差し替える必要がある。** ターゲットは変えない。

### 10.6 需要検証3本のラインナップと日程(2026-08-05決定)

7章は「3週で3本」とだけ決めていたが、日程と各本のネタを確定した。日程の詳細は `content-ops` の `README.md` の「公開スケジュール」節が正。

| # | 公開日 | 記事 | 軸 |
|---|---|---|---|
| 1 | 8/20(木) | 減量が停滞する原因は、消費カロリーの見積もりでした | ウェアラブルの消費カロリーが実際と647kcal/日ズレていた(10.5) |
| 2 | 9/3(木) | 停滞期の正体 — 止まった2週間、歩数はいちばん多かった | 停滞2週の平均歩数18,064歩(過去最多)・摂取+121kcal/日。代謝適応でも運動不足でもなかった |
| 3 | 9/17(木) | 週1回の体重チェックで食事を調整してはいけない | 週次逆算TDEEが1,572〜3,447・標準偏差530で振れる |
| — | 10/1頃 | **ゲート判定**(累計事前登録10人) | PV閾値未達の回は判定不能扱い |

**3本を「あなたが信じている数字は違う」型で揃えた。** 1本目で成立が確認できた構図(自分の実データが、広く信じられている数字を反証する)を軸に統一する。当初候補だった目次案#5(睡眠×食事・飲酒×食事のクロス分析)は、読み物としては成立するが**課金候補に対する切迫感が無い**ため検証の枠から外した(目次案には時期未定で残す)。

**日程を8/13ではなく8/20開始にした。** 8/13開始だと8月のストック記事が3本になり、`content-ops` の `README.md` の「月1〜2本を上限」を超えるため。判定は2週遅れて10/1頃になる。

**判定時期が5章のリスクと重なる。** 10/1のゲート判定は減量の目標日(10/31)の1ヶ月前で、5章が「最大のリスク」とした10/31以降の話・#116(維持モード)の実装時期と重なる。判定を通過した場合、課金の検討・維持モードの実装・最終回記事(目次案#6)が10月に同時進行することになるため、稼働(週10時間)の配分をこの時点で見直す必要がある。

---

## 11. 外部ロードマップのレビュー(2026-08-06)

X上の投稿を材料に別のAI(Grok)が作成した「からだログ収益化ロードマップ(現実的なバージョン)」を、本メモ1〜10章および実装と突き合わせた結果。`content-ops` の `README.md` の 2026-08-04(週次レポート形式の外部レビュー)と同じく、**採用・不採用を理由込みで残す**。

**結論: 現行方針(7章の需要検証 → 10/1のゲート判定)は変更しない。** 採用は11.3の2点のみ。

ロードマップの骨子は「フェーズ0: 土台作り(1ヶ月、クローズドβ5〜20人+ウェイトリスト50人) → フェーズ1: 価値検証(1〜3ヶ月目、月300〜500円の小さな有料テスト) → フェーズ2: 収益化スタート(3〜6ヶ月目、フリーミアムで有料10〜30人) → フェーズ3: 拡大(6ヶ月以降)」で、「早ければ3〜4ヶ月後には最初の売上が見える」と結んでいる。

### 11.1 不採用(現行方針と衝突するもの)

#### (1) 価格帯 300〜500円 と KPI「有料10〜30人」

このKPIは月3,000〜15,000円で、**1章の目標(月10万円)に1桁届かない**。ロードマップ自身が収益目標額を一度も明示していないため、この不足が表面化しない構造になっている。2章は「月500円×200人より、ニッチで単価を上げて月1,500円×70人」を明示的に選んでおり、逆方向。

さらに300円台は**あすけんの価格帯そのもの**で、4章が「入力レイヤーでは勝てない。評価レイヤーで戦う」と整理した競合認識に反する。同じ価格で並ぶと、食品DB・写真判定の弱さが直接比較される。

#### (2) 有料機能の候補(データエクスポート・バックアップ / 広告非表示)

- **データエクスポート・バックアップ**: これは製品の核であり、無料であること自体が差別化(ローカルファースト+自分のスプレッドシートに出せる)。有料の壁にすると売り文句が消える。
- **広告非表示**: 広告が存在しない。単一ユーザー規模のPWAに広告を入れて外す権利を売る構造は、必要なDAUから見て成立しない。

課金の線は 10.2-(1) の**「その日のうちに閉じる数字は無料、週以上の窓で評価するものは有料」**を維持する。要件定義書4.7の章立てにそのまま乗るぶん粒度が細かく、かつ4.7.2の実測TDEEが持つ「解約すると蓄積の価値が消える」ロックインの理屈まで説明できる。

#### (3) フェーズ0「クローズドβ 5〜20人」

ロードマップはβ開始を1ヶ月以内の前提条件として扱っているが、**現状は5人にも配れない**。

- `worker/index.ts` の `GOOGLE_SHEETS_SPREADSHEET_ID` はWorkerのシークレット1つで、全リクエストが同じシートに書く。配れば他人の健康データ(要配慮個人情報)が自分のシートに混ざる。
- `worker/auth.ts` は冒頭に「単一ユーザー前提の最小構成」と明記されているとおり、`API_AUTH_TOKEN` 1本との一致を見るだけ。ユーザー別の発行も失効も無い。

つまり「β開始」は**このプロジェクト最大の実装項目**であり、#165 が「エンジニア以外に現実的でない」として `not planned` で閉じた論点そのもの(10.2-(2)(3) と同じ指摘)。無料の前提条件として扱えない。

#### (4) 順序(実装 → 検証になっている)

フェーズ0が「β+使いやすさ改善」、フェーズ1が「価値検証」で、7章の原則(**需要検証を通過するまで実装は1行も書かない**)と逆。この順序は #67(需要未確認のまま基盤を整えた)の反復にあたる。

### 11.2 ロードマップに無かった論点

いずれも本メモでは検討済み。外部ロードマップ単体を採用すると欠落する。

- **10/31以降のリテンション崖(5章・#116 維持モード)。** 目標達成した瞬間の解約は減量アプリが儲かりにくい構造的原因で、5章が「最大のリスク」としたもの。ロードマップにchurnの記述が無い。設計は完了済みでPR #125 が未マージ(10.2-(4))
- **法務(特商法表記・プライバシーポリシー・要配慮個人情報)。** 金銭を受け取る前に必ず通るゲートで、これ無しではフェーズ2「正式に有料プラン公開」に到達できない(4章)
- **決済(Stripe)とライセンス判定の実装量。** (3)と合わせるとWorker本体の改修になり、6章の「課金判定だけ別リポジトリに切る」では収まらない(10.2-(3))
- **稼働時間(週10時間)の配分。** ロードマップは「X週3〜5本」+β運用サポートを追加で積むが、時間の出どころを書いていない。現状の枠は減量・アプリ開発・週1定型レポートで既に埋まっている
- **KPIの分母の飛躍。** 「1ヶ月でウェイトリスト50人」は note全体で週20〜30ビュー・フォロワー実質0 の現状から10倍以上の飛躍。10.2-(6) で入れた「PV閾値未達の回は判定不能として扱う」に相当するガードが無く、未達を「有料の価値なし」と誤判定する

### 11.3 採用する2点

1. **事前登録の導線を、検証記事3本だけでなく週次レポートにも毎回入れる。** 現状フォームは木曜のストック記事3本にしか置かない設計だが、8/20〜10/1の期間中は月曜の週次レポートの方が本数が多く、ゲートの分母を増やす実務として効く。`content-ops` の `README.md` の週次レポート定型フォーマット5(リンク)に書き戻す
2. **「有料化した理由」を過程付きで書く記事。** ゲートを通過した場合の記事候補として目次案に記録する。build in public の方針とも、検証3本で使った「あなたが信じている数字は違う」型の次の一手としても素直に接続する(通過が前提なので、現時点では記録のみ)

### 11.4 このレビューから残す注意

**「早ければ3〜4ヶ月で最初の売上」という時間感覚は採らない。** これを基準に置くと10/1のゲート判定が遅く見えるが、1章が前提条件として受け入れた2年計画から見れば前倒し気味の日程である。時間感覚がズレた状態でゲートを迎えると、未達時に「検証をもう1周する」ではなく「とりあえず配って様子を見る」を選ぶ動機が生まれる — それが 11.1-(3)(4) で挙げた #67・#165 の分岐そのもの。**個々の判断より、判断を焦らせる効果の方がリスクが大きい。**

---

## 12. 決済・課金まわりの実現可能性(2026-08-09)

本人の指摘「もろもろ整って公開したとしても、決済情報の管理ってできなくないか」を、8章の未決事項(課金判定の実装方式)と 10.2-(3) の続きとして、実装の現物と突き合わせて分解した結果。

**結論: 指摘は半分当たっている。「カード情報の管理」は Stripe に寄せれば自分で持たなくて済むので問題にならない。本当に無いのは (a) 課金状態の置き場、(b) ユーザー識別、(c) 有料機能を止める手段の3つで、(a)(b) は実装で埋まる。実装で埋まらないのは (d) 特商法の表示義務で、これは工数ではなく本人の意思決定。**

### 12.1 カード情報そのものは持たない(持ってはいけない)

「決済情報の管理」を「カード番号を自分で預かること」と読むなら、答えは「できない、かつやるべきでない」。Stripe Checkout / Customer Portal に寄せれば、カード情報は Stripe がホストするページに直接入力され、**自分の Worker もアプリ側のコードも一度も通らない**(PCI DSS の SAQ-A 相当に収まる)。カードの更新・期限切れ時の再試行・解約・領収書の発行も Customer Portal 側の機能で、自前実装は要らない。

つまりここは「管理できない」のではなく「管理しないのが正解」で、4章の「自分が保持するのはメールアドレスと課金状態のみ」とも整合する。

### 12.2 本当に無いもの (a): 課金状態を置く場所がゼロ

`wrangler.toml` のバインディングは `[assets]` の `ASSETS` **1つだけ**で、KV も D1 も R2 も Durable Objects も無い。`worker/index.ts` の `Env` も `ASSETS` +シークレット(`API_AUTH_TOKEN`・`GEMINI_*`・サービスアカウント・スプレッドシートID)だけで、**サーバ側に書き込める永続領域が1つも存在しない**。

これは設計上の判断というより、単一ユーザーでは必要が無かっただけ。ただし現状のまま「誰がいつまで有効か」を置く先はゼロなので、本人の直感はこの意味では正しい。

埋めるコスト自体は小さい。D1(または KV)を1つ足し、`{メールアドレス, ライセンスキー, 課金状態, 有効期限, Stripeの顧客ID}` だけのテーブルを1枚持てばよい。**健康データは一切置かない**ため、4章の法務コスト回避(要配慮個人情報を自分のインフラに持たない)は崩れない。

無料枠も問題にならない。D1 の Free プランはストレージ5GB・読み取り500万行/日・書き込み10万行/日で、1章の目標(70人)なら行数は数十〜数百、書き込みは決済イベントのたびの1行だけなので誤差の範囲。4章の原価構造(Gemini・Workers はほぼ無料枠)の結論は変わらない。

ただし、ここで**一段だけ性質が変わる**点は記録しておく。

- **メールアドレスを保存した時点で個人情報を預かる側になる**ため、プライバシーポリシーが必須になる(要配慮個人情報である健康データとは負担が段違いに軽いが、ゼロではない)。削るなら、メールアドレスは Stripe 側にだけ置き、自分の D1 には Stripe の顧客IDとライセンスキーだけを持つ設計にできる。**問い合わせ対応で本人を特定できるかとのトレードオフ**であり、決済導入時に決める
- **ライセンス照会を毎回ネットワーク必須にすると、オフラインで有料機能が死ぬ。** 本アプリはオフライン前提のPWAで、記録は IndexedDB だけで完結する。判定結果は有効期限までクライアントに保持し、オンライン時に更新する fail-open の作りにしないと、圏外で週次レビューが開けないという退行になる

### 12.3 本当に無いもの (b): ユーザー識別が1本のトークン

10.2-(3) の再確認。`worker/auth.ts` の `isAuthorized` はシークレット `API_AUTH_TOKEN` **1本との文字列一致**を見るだけで、ユーザー別の発行・失効・課金状態との突合が無い。

もっとも置き換え先の形は既にある。クライアントは設定画面で `Settings.apiToken` を入力し、`src/api/apiAuth.ts` 経由で全 `/api/*` に `Authorization: Bearer` を付けている — **この入力欄と送信経路をそのまま「ライセンスキー」に読み替えられる**。変わるのは Worker 側の突合先が「シークレット1本」から「12.2 のテーブルの1行」になる点で、クライアント側の改修はほぼラベルと文言の変更で済む。

### 12.4 本当に無いもの (c): 有料にしたい機能の大半が、サーバから止められない

本メモで未指摘の論点。3章・10.2-(1) が有料側に置いた機能の計算場所を確認すると、次のとおり分かれる。

| 機能 | 計算場所 | サーバ側で止められるか |
|---|---|---|
| 4.7.1 目標カロリー自動計算 | `src/lib/nutritionCalc.ts`(クライアント) | × |
| **4.7.2 実測TDEE推定** | `src/lib/tdee.ts`(クライアント) | **×** |
| 4.7.3 週次レビューの数値部分 | `src/lib/weeklyDigest.ts`(クライアント) | × |
| 月次レビュー(#114)の数値部分 | `src/lib/monthlyDigest.ts`(クライアント) | × |
| 4.7.3・4.7.6 AIコメント | `/api/weekly-advice`・`/api/monthly-advice`(Worker) | ○ |
| (参考)食事の写真判定 | `/api/judge-meal`(Worker) | ○ |

**3章が「課金の心臓部」と位置づけた 4.7.2 実測TDEE が、まるごとクライアント計算。** 記録が IndexedDB にしか無い以上サーバは計算に必要なデータを持っておらず、ローカルファーストを貫いた当然の帰結でもある。

結果として有料判定はクライアント側のフラグで表示を出し分ける形になるが、それは DevTools か IndexedDB の設定を1行書き換えれば外れる。6章は自己ホストのリスクを「筋トレ層は Cloudflare にデプロイしないから実害はほぼ無い」として受け入れたが、**フラグを立てるだけの回避は自己ホストよりはるかに敷居が低い**ため、6章の結論をそのまま流用できない。

選択肢は3つ。

1. **実効性のあるサーバ側機能を有料の主軸にする**(AIコメント2本+写真判定)。ただし 3章のロックイン論拠(蓄積で精度が上がる実測TDEE)を課金の柱から外すことになる
2. **クライアント判定のまま性善説で運用する。** 6章の延長で、想定ターゲット(2章)なら実害は小さい。ただし公開・MITのリポジトリに「ここを通せば有料機能が開く」コードが載る
3. **有料の計算だけサーバへ移す。** AIコメント用に既に Worker へ送っているダイジェストを使い、計算結果を返す。ローカルファーストは維持できるが、`src/lib/tdee.ts` を worker 側へ複製することになり、`SETTINGS_SYNC_FIELDS` と同種の「手で同期させる」負債が増える

現時点では 1 と 2 の組み合わせが素直。判断は需要検証(7章)の通過後でよいが、**「実測TDEEを有料の柱に据える」という3章の前提が技術的には裏づかない**点は、10.5 で訴求軸を「ウェアラブルとのズレ」に移した件とどう対応させるかに関わるため記録しておく。

### 12.5 実装で埋まらないもの (d): 特商法の表示義務

継続課金を受ける以上、特定商取引法の通信販売としての表示が要る。**個人事業主の場合、原則として氏名・住所・電話番号の表示義務がある。** 8章はこれを「実作業量の見積もり」として未決に置いているが、本体は工数ではなく**自宅の住所と携帯番号を公開するかどうかの本人の意思決定**。

#### 手続きの全体像(2026-08-09 時点で調べた範囲)

| # | やること | 実体 |
|---|---|---|
| 1 | **特商法に基づく表記ページを1枚作る** | HTML 1枚。氏名・住所・電話番号・販売価格(税込)・支払時期と方法・役務の提供時期・**返品/返金の可否**・動作環境・追加費用 |
| 2 | **住所と電話番号をどう書くか決める** | 下記の3択。**ここだけが本当の論点** |
| 3 | **申込みの最終確認画面を整える** | 2022年6月施行の改正(特商法12条の6)。月額であること・毎月の請求額・提供時期・**解約方法と連絡先**を、申込み確定の直前画面に分かりやすく出す |
| 4 | **プライバシーポリシー・利用規約** | 12.2 のとおりメールアドレスや顧客IDを持つ時点で必要。健康データを預からないぶん内容は薄い |
| 5 | **Stripe のアカウント審査** | 本人確認書類(免許証・マイナンバーカード等)、入金先口座、公開済みのサービスURL、1のページのURL。**開業届は必須ではない**(Stripe は個人と個人事業主を同じ扱いにする) |
| 6 | (任意)開業届・青色申告承認申請 | 税務の話で決済の前提ではない。消費税は 12.5 末尾のとおり免税 |

1・3・4 は文章を書くだけで、合わせて1〜2日。5 は書類を出して待つだけ。**「めんどくさい」の実体は 2 の一点に集約されている。**

#### 住所・電話番号の3択

| 案 | 内容 | 備考 |
|---|---|---|
| A | **省略+請求があれば遅滞なく開示** | 消費者庁の通信販売広告Q&Aは、請求があった場合に遅滞なく提供する旨を広告に表示し、かつ実際に遅滞なく提供できる措置を講じていれば住所・電話番号を省略できるとしている。個人開発サービスがこの書き方で Stripe の本番審査を通した実例もある |
| B | **バーチャルオフィス** | 消費者庁は一定の条件下でバーチャルオフィスの住所・電話番号の表示を認める見解。条件は運営事業者が本人の現住所と本人名義の電話番号を把握し、確実に連絡が取れる状態であること。住所貸し中心のプランで年4,000〜8,000円程度という個人開発者の実例がある(電話転送まで含めるかで変わる) |
| C | **事業用のIP電話番号+留守電** | 電話番号だけ分ける折衷案。住所の問題は残る |

→ **A で始め、Stripe の審査で通らなければ B に切り替える**のが順当。A が通れば費用ゼロで済む。

#### Androidアプリにしても回避できない(むしろ悪化する)

「Webアプリではなく Android アプリにすれば特商法を気にしなくてよくなるのでは」という発想は自然だが、**逆方向に効く。** 検討の打ち切りとして記録しておく。

- **特商法の義務は消えない。** Google が最終販売責任者(merchant of record)になるのは EEA など一部地域で、**日本向けの販売はデベロッパー自身が販売者**。Play Console には日本向けの特商法情報(氏名・電話番号・住所)の記入欄があり、記入が求められる
- **上記の案A(費用ゼロ)が使えなくなる。** Google Play はデベロッパーの身元確認を行い、**確認済みの氏名と住所をストアの「デベロッパーについて」欄で公開する**。個人アカウントだと自宅住所がそのまま世界に公開される(これを理由に個人開発者が撤退・組織アカウントへ移行した事例が多数ある)。組織アカウントならバーチャルオフィスの住所を使えるが、それは案Bのコストを払うのと同じこと。**「請求があれば開示」で伏せる余地が、Web配布より狭い**
- **手数料が桁違いに乗る。** 定期購入のサービス手数料は現状15%で、月1,500円なら225円/人・月。70人なら月15,750円・年189,000円になり、Stripe の3.6%(54円/人・月、年45,360円)との差は**年14万円強** — 1章の目標(月10万円)の1ヶ月分を超える。なお2026年6月30日からサービス手数料と決済手数料が分離され定期購入は10%+決済手数料になる予定だが、**日本での適用は2026年後半見込みで確定情報ではない**
- **iOS を捨てることになる。** 現状は PWA なので iPhone でも Android でも同じものが動く。2章の想定ターゲット(増減量サイクルを回す筋トレ層)を iOS 側ごと落とす判断は、特商法の回避と釣り合わない

技術面では TWA(Bubblewrap)で現在の PWA をほぼそのまま包めるため実装コストは低く、ローカルファーストも維持できる。したがって **Android 化を検討する価値があるとすれば「ストアで見つけてもらえる」という配布・発見チャネルとしてであって、法務や決済の回避策としてではない。** 日本では2025年12月19日に全面施行されたスマホソフトウェア競争促進法により代替課金・外部リンク誘導が認められる方向にあり、手数料の前提は今後動きうるが、住所公開の論点はそれとは別に残る。

**本メモは法律の専門的判断ではない。** 上記は一般に行われている運用を調べた結果で、消費者庁の一次情報(`no-trouble.caa.go.jp`)にはこの環境から直接アクセスできず二次情報に依っている。**実際に決済を入れる段階で一次情報にあたること。**

一方、周辺の懸念は現状の規模ではほぼ問題にならない。

- **消費税**: 課税売上1,000万円以下は免税事業者。1章の目標(月10万円=年120万円)は大きく下回る。B2C中心のためインボイス登録も基本的に不要
- **決済手数料**: 3.6%程度で、月1,500円あたり約54円。4章で確認した原価構造(Gemini・Workers はほぼ無料枠)を崩さない
- **アプリストア手数料**: PWA配布のため Apple/Google の課金経由にならず、30%の控除を受けない。**ローカルファースト+PWAという選択が、ここでは明確に有利に働く**

### 12.6 潰す順序

需要検証(7章)の通過後に着手する前提で、依存関係の順に並べると次のようになる。

| 順 | 論点 | 性質 |
|---|---|---|
| 1 | **特商法の表示(12.5)** | 手続き自体は1〜2日。実質は住所・電話番号の3択の意思決定で、ここが No なら以降が不要になるため最初に置く |
| 2 | **同期先シートの所有者(10.2-(2))** | 配布そのものの前提条件で、決済以前の問題。**有力な解が 12.7**(ユーザー自身のGoogleログイン) |
| 3 | 課金状態の置き場(12.2)+ユーザー別認証(12.3) | 実装。D1 1枚 + `worker/auth.ts` の置き換え |
| 4 | 有料機能の線引き(12.4) | 3章の「課金の柱」の再検討を含む |

なお本章は 11.4 の注意(判断を焦らせない)を踏まえ、**実装の着手を前倒しする根拠としては使わない。** 12.2・12.3 が「思ったより小さい」ことは、需要検証を飛ばしてよい理由にはならない — それが 11.1-(4) で不採用にした「実装 → 検証」の順序そのものになる。

### 12.7 同期先シートの所有者問題に、ユーザー自身のGoogleログインを充てる案(2026-08-09)

12.6 の順位2(= 10.2-(2))に対する本人の提案:「各自 Google ログインしてもらう」。サービスアカウント1つで全員分を1枚のシートに書く現状を、**ユーザー自身の OAuth 認可に置き換え、各自の Drive にシートを作る**というもの。

**結論: 筋が良い。10.2-(2) の芯がこれで解ける。** シートの所有者が本人になるため「自分が保持するのはメールアドレスと課金状態のみ」(4章)が本当に成立し、全員のデータが自分のシートに混ざる問題も消える。さらに #165 が「エンジニア以外には現実的でない」として `not planned` の根拠にした障壁 — サービスアカウントを編集者として招待させる手順 — が、**「Googleでログイン」ボタン1つ**に置き換わる。#165 の判断は、この方式を検討せずに閉じていた可能性がある。

#### 成立の鍵: スコープを `drive.file` だけに絞る

個人開発でこの手の OAuth を避ける最大の理由は Google のアプリ検証コストだが、**スコープ選択で回避できる見込み**(2026-08-09 時点で調べた範囲)。

| スコープ | 分類 | 影響 |
|---|---|---|
| `drive`・`drive.readonly` | **restricted** | 第三者セキュリティ監査(CASA)を求められうる。個人開発には非現実的 |
| `spreadsheets` | **sensitive** | アプリ検証が必要 |
| **`drive.file`** | **non-sensitive** | **非機微スコープのみのアプリは検証が必須にならない** |

`drive.file` は「**アプリが作成した、またはユーザーが明示的に選んだファイルだけ**」にアクセスできるスコープで、Sheets API もこれで叩ける。アプリ側でスプレッドシートを新規作成すれば以後そのファイルは操作でき、**ユーザーの他のファイルは一切見えない**。相手への説明としても強い。**`spreadsheets` を足した瞬間に sensitive に落ちる**ため、そこを踏まないことが条件。

#### 詰めるべき4点

1. **OAuth同意画面は「本番(In production)」にする。** 公開ステータスが「テスト」のままだと refresh token が**7日で失効**し、毎週ログインし直しになる。非機微スコープのみなら本番公開に重い検証は要らない見込み(要確認)。本番でも**6ヶ月間リフレッシュされないと失効**するため、長期離脱したユーザーは再ログインが要る
2. **どこに何を置くかが、アーキテクチャの分岐点**(詳細は 12.8)
3. **Garmin連携は配布版には付いてこない。** `scripts/garmin/` の GitHub Actions はサービスアカウントで自分のシートに書いており、他人の Garmin データを各自のシートに書き込む仕組みは存在しない。**配布版では活動記録タブが空**になる前提で、UI が壊れないかの確認が要る(取り込みは常にシート側で上書きする作りのため、空なら何も起きないはず)
4. **記録5タブも自動作成の対象にする。** 現状は「フェーズ1〜2の記録5タブは手動作成前提、それ以外は同期時に自動作成」(CLAUDE.md・画面設計書)。他人に配るなら手動作成の前提は成立しないため、5タブも見出し行付きの自動作成に寄せる

#### 変わらないもの・自分の環境

AIコメントの経路(`/api/weekly-advice` はダイジェストだけを送る)とライセンス判定(12.3)はこの案の影響を受けない。自分自身については、サービスアカウント方式と OAuth 方式の二重メンテを避けるため自分も OAuth に寄せ、**Garmin の書き込み用にサービスアカウントを自分のシートへ編集者として招待しておく**のが素直。

#### 補足: 自分の環境で Garmin 連携を維持できるか(2026-08-09)

「自分で使うときは Garmin 連携を続けたいが問題ないか」を確認した結果。**問題ない。Garmin の経路はアプリの認証と完全に独立している。**

`scripts/garmin/garmin_to_sheet.py` は `service_account.Credentials` でサービスアカウントとして認証し、`spreadsheets` スコープで活動記録タブに書く。GitHub Actions → サービスアカウント → Sheets API という経路で、Worker もアプリの OAuth も通らない。

さらに、`scripts/garmin/README.md` の「サービスアカウントには同期用スプレッドシートの編集権限が必要(Worker同期用に共有済みならそのまま使える)」という記述から、**現在のスプレッドシートは自分が所有し、サービスアカウントを編集者として招待している構成**であることが確認できた。つまり案Aが目指す「シートの所有者が本人」という状態に、自分の分については既になっている。移行で所有者が変わる問題は起きない。

ただし実装時に踏みやすい罠が2つある。いずれも Issue 側(#215・#216)に警告として反映済み。

1. **シークレット名が Worker と GitHub Actions で同一。** `GOOGLE_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` / `GOOGLE_SHEETS_SPREADSHEET_ID` の3つは両方に存在する。#215 で「不要になったサービスアカウント関連を整理する」際に**Actions 側の Secrets まで消すと翌朝の Garmin 同期が黙って落ちる**。消してよいのは Cloudflare Worker のシークレットだけ。サービスアカウントをシートの編集者から外すのも同じく不可
2. **既存シートの引き継ぎには Google Picker が要る。** `drive.file` は「アプリが作成した、またはユーザーが Picker で明示的に選んだファイル」にしかアクセスできず、**スプレッドシートIDを手入力しただけでは対象にならない**。新規作成したシートに乗り換えると Garmin の書き込み先(`garmin-sync.yml` の `GOOGLE_SHEETS_SPREADSHEET_ID`)と分岐し、活動記録だけ別のシートに溜まる。**既存シートをそのまま使い続けられることが自分の移行の条件**になる

→ 結果として自分の環境は「**アプリの認証だけ OAuth に変わり、シートも Garmin もそのまま**」という形になる。

**この案の着手も 12.6 の順序に従う**(ゲート判定の通過後、特商法の意思決定の次)。ここで解けたのは「配れない理由」であって、「配る相手がいる」ことの証明ではない。

### 12.8 12.7-(2) の詳細: 何をどこに置くか

12.7 の4点のうち、これだけは後から変えると同期エンジンの引っ越しになるため、独立させて詰める。

#### 前提となる制約: Google は refresh token に client_secret を要求する

まず調べて分かった制約から。**Google は refresh token の発行と使用に client_secret を要求し、PKCE でこれを代替できない**(PKCE は認可コード横取りの防止であって、client_secret の置き換えではない。他の一部の IdP は PKCE のみでのリフレッシュを許すが、Google は許さない)。

つまり「ブラウザだけで完結して長期の認可を保つ」構成は Google では**取れない**。client_secret を持つサーバ側の口が必ず1つ要る。ただしこれは「Worker が同期を行う」ことを意味しない — **Worker はトークン交換だけを担い、健康データには触れない**という形が取れる。

#### 軸は2つあり、独立している

議論が混ざりやすいので分解する。

- **軸1: Google Sheets API を誰が叩くか**(= 健康データが自分のインフラを通るか)
- **軸2: refresh token を誰が保管するか**(= 相手の Drive への権限を預かるか)

Sheets API は CORS に対応しておりブラウザから直接叩けるため、軸1でクライアントを選べる。組み合わせると次のようになる。

| 構成 | Sheets APIを叩く | refresh tokenの保管 | 健康データがWorkerを通る | 相手のDriveへの権限を預かる |
|---|---|---|---|---|
| **現状**(サービスアカウント) | Worker | —(秘密鍵から都度JWT) | **通る** | —(自分のシート) |
| **B: Worker完結** | Worker | Worker + D1 | **通る** | **預かる** |
| **A′: 権限だけ預かる** | クライアント直 | Worker + D1 | 通らない | **預かる** |
| **A: クライアント保持** | クライアント直 | クライアント(IndexedDB) | 通らない | 預からない |

案Aでも、上記の制約により**Worker に薄いトークン交換エンドポイントが1つ要る**。クライアントが refresh token を送り、Worker が client_secret を添えて Google から access token を受け取って返すだけの、**保存を伴わないステートレスな中継**。refresh token は Worker を通過するが保存されない。

#### それぞれの評価

**案A(推奨)の利点**

- **健康データが自分のインフラを一切通らない。** プライバシーポリシーに「記録はお預かりしません」と書けるようになり、4章の法務コスト回避が完全な形で成立する。10.2-(2) が「同期経路には成立しない」と否定した主張が、ここで初めて本当になる
- D1 が漏れても他人の Drive には手が届かない(refresh token を持っていないため)
- **Worker に残るのが有料機能だけになる。** AIコメント2本と写真判定だけが Worker 経由になり、「Worker を叩ける = 課金済み」という単純な構造になる。12.3 のライセンス判定の対象がきれいに揃い、無料ユーザーはほぼ Worker を叩かないので原価も下がる

**案Aのコスト・注意**

- `worker/sheetsSync.ts`・`sheetsImport.ts` のロジックをクライアントへ移す必要がある。ただし `planUpserts`・`planRowDeletions`・`planWeightImport` などは**既に純関数として切り出してある**ため(Issue #30・#54)、移設のコストは見た目より小さい
- 端末ごとに Google 認可が要る(refresh token が端末ローカルのため)。現在の apiToken 手入力と同じ運用感なので違和感は小さい
- **完全バックアップ(`src/db/backup.ts`)に refresh token を含めるかの判断が要る。** #164 で apiToken をシート同期の対象から外した判断と揃えるなら含めない(復元後は再ログイン)。含めるとバックアップJSONが認証情報を持つファイルになる
- XSS で refresh token が盗まれうる。ただしスコープが `drive.file` のため、被害範囲は**アプリが作ったシート1枚**に限定される(12.7 のスコープ選択がここでも効く)

**案Bの利点と欠点**

利点は実装が軽いこと。`worker/googleSheetsAuth.ts` の「サービスアカウントの秘密鍵からJWTを組んで署名する」を「D1 の refresh token で access token を更新する」に差し替えるだけで、`sheetsSync.ts` 本体はほぼ無傷で済む。端末をまたいで一度認可すれば済むという UX 上の利点もある。

欠点は、10.2-(2) の指摘がそのまま残ること、そして**全ユーザーの Drive 書き込み権限を1つの D1 に集めること**。漏れたときの被害は「メールアドレスと課金状態」とは桁が違い、プライバシーポリシーに書く内容も重くなる。

#### 決定(2026-08-09): 案A(軸1・軸2ともクライアント側)

**有料化に進む場合の同期構成は案Aとする**(本人決定)。当初は「軸1を先に決め、軸2は運用しながら選ぶ」と整理したが、軸2で案A′(refresh token だけ Worker + D1 に預かる)を選ぶ理由が無い — 軸1をクライアント直にした時点で Worker はトークン交換の中継だけになり、そこに保管を足すと**得るものは端末をまたいだ再認可の省略だけで、代わりに全ユーザーの Drive 書き込み権限を集約する**ことになるため。割に合わない。

**これは条件付きの決定である。** 有料化そのものが 10/1 のゲート判定に依存しており、現時点で実装に着手する根拠にはならない(11.4)。確定した仕様ではないため、要件定義書・画面設計書・意思決定ログへの書き戻しは行わず、本メモに留める。

#### 決定から派生する、いま守れる制約

着手は先でも、**今後の実装で「案Aへ移せる形」を壊さない**ことは今日から守れる。検証通過後の移設コストを下げるための制約として記録しておく。

1. **`worker/sheetsSync.ts`・`sheetsImport.ts` に手を入れるときは、純関数の切り出しを維持する。** `planUpserts`・`planRowDeletions`・`planWeightImport` などが Worker 固有の依存(`Env`、Web Crypto での JWT 署名、`fetch` の呼び出し)から独立している状態を崩さない。ここが崩れるとクライアントへの移設が書き直しになる
2. **新しい同期ロジックを Worker 側にだけ足さない。** 増やすなら純関数として、`src/` からも呼べる形で書く
3. **完全バックアップ(`src/db/backup.ts`)に refresh token は含めない。** #164 で `apiToken` をシート同期の対象から外した判断と揃える(認証情報を持ち出せるファイルにしない)
4. **OAuth クライアントの「承認済み JavaScript 生成元」に配信URLの登録が要る。** #180 のように配信URLを変更すると OAuth 設定の更新も必要になる、という紐付きが新たに生まれる。IndexedDB がオリジン単位である件(CLAUDE.md)と同じ注意が、認可にも及ぶ
5. **`D1` に置くのは課金状態だけ。** 12.2 で引いた「メールアドレスと課金状態のみ」の線が、案A確定によってそのまま守られる
6. **ライセンス判定は「`/api/*` を叩けるか」の一点で済む。** 案Aでは Worker に残るのが有料機能(AIコメント2本・写真判定)だけになるため、12.3 の突合はこの単一の関門に集約できる

#### 補足: refresh token の性質から出る要件

案Aで端末に置く refresh token は「いつでも access token を作れる券」= 実質的に権限そのもの。ただし **Google は refresh token の使用にも client_secret を要求する**ため、トークン単体では鍵にならない(アプリの身分証と組でないと使えない)。この二重化は、12.8 で挙げた XSS リスクの評価に効く一方、次の要件を生む。

- **トークン交換エンドポイントは「client_secret の代行窓口」になる。** 盗んだ refresh token を持つ攻撃者は、Google を直接叩けなくてもこの窓口を叩けば代行してもらえる。**ライセンスキーによる認証(12.3)とレート制限をこのエンドポイントにも必ず掛ける**こと。認証の外に置いてはならない
- **refresh token をログに出さない。** `wrangler.toml` の `[observability]` に付けた注意(機密を出さない。Issue #206)の対象に、これを明示的に含める
- **失効は正常系として扱う。** ユーザーによる連携解除・6ヶ月の未使用・認可の積み上がり(1アカウント1クライアントあたり100個の上限を超えると古いものから無効)で失効しうる。失効時は既存の同期エラー表示に乗せて再ログインを促す。なお `drive.file` のみのためパスワード変更では失効しない(失効するのは Gmail スコープを含む場合)

### 12.9 完全バックアップ(JSON)の廃止案(2026-08-09)

本人の提案:「認証さえできればシートにほぼ全部書き込まれているので、最終的にJSONのバックアップ機能は廃止でよいのでは」。**方向は妥当。** ただし「ほぼ」の実体を実装で確認したところ、廃止前に埋めるべき穴が1つある。

#### 前提の検証: テーブル単位の穴は無い

- `BACKUP_TABLES`(`src/db/backup.ts`)の15テーブルに対し、シート側のタブも15(記録5+マスタ3+血圧・周囲径・習慣記録+AIコメント2+設定+活動記録)。**1対1で対応しており、丸ごと落ちるテーブルは無い**
- CLAUDE.md と `backup.ts` の冒頭コメントが挙げる「**写真参照**」は、実質デッドフィールド。`photoLocalRef` は型と `addMealRecord` の入力にあるだけで、**UIのどこからも渡していない**(写真は `/api/judge-meal` に送るだけで保存していない)。失われるものが無いため、廃止理由の一つは既に消えている
- 「`apiToken`」も、12.8 の案A確定により refresh token へ置き換わり、**そもそもバックアップしない**と決めた。ここも消える

#### 残る差分3つ

1. **食事のAI推定値は生きている。** `MealRecordPage.tsx` が保存済みレコードを開き直すとき `aiEstimated*` を読んで「AI推定はこうだった」を復元している。シート経由の復元ではこれが落ち、過去の食事を編集で開いてもAI推定が表示されない。**確定値(実データ)は残る**ため、損失は表示の忠実度のみ。許容範囲
2. **`skipped`(食べなかった)が取り込みで復元されない — これは現在すでにある欠落。** `PulledMealRecord`(`src/sync/types.ts`)は `skipped` を除外していないのに、`planMealImport`(`worker/sheetsImport.ts`)が組み立てるレコードに `skipped` が無い。シートには品目名「食べなかった」・0kcal の行として載るため、取り込むと**「食べなかった」という名前の通常記録**に化け、「未記録」との区別(Issue #143)が壊れる。JSONが完全復元経路として存在する今は表面化しにくいが、**シートを唯一の経路にするなら先に直す**
3. **用途が違う。** JSONは全削除+書き戻しで**完全に元の状態**へ戻す。シート取り込みは追加のみ・ローカル優先で、**空のDBに入れる前提**(既存キーはスキップする)。部分的に壊れたDBを直す用途には使えないため、廃止はこの用途を捨てる判断になる

#### 見落としやすい副作用: 番人が消える

`src/db/__tests__/backup.test.ts` は `db.tables` と `BACKUP_TABLES` を突き合わせ、**新しいテーブルを足したのに載せ忘れた状態を検出する**。**コードベース全体で `db.tables` を参照しているのはこのテストだけ**で、シート同期側に同等の仕組みは無い。CLAUDE.md が「この番人を外さないこと」と明記しているのはこの機能であり、廃止するなら**同じ突き合わせをシート同期の設定(`worker/sheetsSync.ts` の `*_CONFIG` 一覧)に対して立て直す**必要がある。

#### 依存の集中

JSONファイルはユーザーの手元にあり Google にも自分にも依存しない。シートだけにすると、Googleアカウントの停止・連携失効・シートの誤削除が単一障害点になる。ただし案Aではシートは**ユーザー自身の Drive** にあり、Drive 自体にゴミ箱と版履歴がある。原則からは後退だが実質的な保護は残る。

#### 結論と順序

**廃止に賛成。ただし案Aの実装後**とする。今廃止すると、シート同期が自分のシート前提のまま唯一の退避経路を失う。特に #180 のような配信URLの変更(IndexedDBはオリジン単位)では現状もJSONが要る。

順序: **案Aの実装 → (2)の `skipped` 復元を修正 → シート同期側に番人を立てる → JSONバックアップを廃止。** (1)のAI推定値の欠落は許容し、(3)の部分復旧の用途を捨てることは意識して決める。

**(2)は Issue #212 として登録済み**(`bug` / `優先度: 低`)。対応方針は**案1(品目名「食べなかった」かつ0kcalの行を `skipped: true` として取り込む)**で確定。列を増やさず手入力の過去データにも効くため。誤判定の余地は残るが、その場合も失われるのは表示上の区別だけで kcal・PFC の数値には影響しない。

---

## 13. 需要検証ゲートが判定として機能するか(2026-08-09)

本人の問題提起:「10/1のゲート判定について、今のSNSの拡散力だと正直そもそも不要になると思っている。これを正しく判断するにはどうするのが良いか。ここを誤ると判断そのものを間違える」。

**結論: 懸念は正しい。現在のゲートは「登録が集まらなかった」の原因を需要と配信に切り分けられず、実質的に判定不能が既定値になる。ただし直すべきはゲートの測り方であって、検証そのものを外す判断ではない。**

### 13.1 まず、混同してはいけない2つの主張

- **「需要検証は不要」** — これは 11.1-(4) で不採用にした「実装 → 検証」の順序であり、#67(需要未確認で基盤を整えた)・#165 と同じ分岐。11.4 が「判断を焦らせる効果の方がリスクが大きい」と名指しで警告した動機がこれにあたる
- **「今の形の需要検証は機能しない」** — こちらは正しい。以下の通り、数字で裏づく

**捨てるべきは検証ではなく測定器。** この区別を落とすと、測定器が壊れていることを需要が無いことの証拠として使ってしまう。

### 13.2 なぜ現在のゲートが判定として機能しないか

10.2-(6) は「分母が10倍の飛躍」と指摘し、`content-ops` の `README.md` に「PV閾値未達の回は判定不能扱い」の分岐を入れた。しかし**判定不能に有効期限もエスカレーションも付いていないため、既定値になると判断そのものが先送りされる**。

観測ゼロから言えることは統計的にほぼ決まっている。**n回の露出で登録が0件だったとき、真の登録率について言えるのは「3/n 未満」だけ**(rule of three、95%水準)。

| 到達 n | 0件だったとき言えること | 判定として |
|---|---|---|
| 13(note04の実績) | 登録率は23%未満 | **何も言っていない** |
| 30 | 10%未満 | ほぼ無意味 |
| 60 | 5%未満 | ようやく弱い証拠 |
| 100 | 3%未満 | 使える |

つまり**現在の到達(記事あたり13前後、note全体で週20〜30ビュー)では、登録が0でも仮説は棄却できない。** 「累計10人」という絶対数の基準は、分母がこの規模だと達成不能であると同時に、未達だったときに何も意味しない。

さらに、必要な規模の感度も見ておく。目標70人(1章)に対し、記事→登録の転換率と登録→課金の転換率を仮に置くと:

- 登録率5%・課金率20% → 必要な累計到達は7,000。2年で週67 — **現状の2〜3倍**で届く
- 登録率1%・課金率10% → 必要な累計到達は70,000。2年で週673 — **現状の25倍**で届かない

**この2つのどちらなのかを知ることがゲートの本来の仕事**であり、n=13ではその区別が付かない。「到達が足りないから判定できない」のではなく、**到達を確保することがゲートの前提条件**という位置づけが正しい。

### 13.3 提案: ゲートを「合否」から「どちらの課題か」の分岐に変える

10/1に判定するのを「有料化するか」ではなく、**次に取り組む課題がどちらか**にする。これなら到達が足りなくても判定は必ず下りる。

| 10/1時点の観測 | 判定 | 次にやること |
|---|---|---|
| 累計到達 < 100 | **配信の課題** | 需要検証は保留。到達を作れるかを別の仮説として検証する |
| 到達 ≧ 100 かつ 登録率 < 2% | **需要の課題** | 仮説を棄却し、2章の候補選定に戻る |
| 到達 ≧ 100 かつ 登録率 2〜5% | 判断保留 | もう1周。ただし**最大1回まで**(下記) |
| 到達 ≧ 100 かつ 登録率 ≧ 5% | **通過** | 12.6 の順序へ |

**「判定不能」「もう1周」には回数制限を付ける。** 到達不足による保留が2回続いたら、それ自体を「この配信チャネルでは検証できない」という結論として扱い、検証手段を変える(13.4)。この上限が無いことが現在の設計の唯一かつ最大の欠陥。

### 13.4 到達に依存しない検証を並走させる

記事の到達が育つのを待つと、判定が何ヶ月も後ろにずれる。**分母を借りるか、1件あたりの情報量を上げる**ことで、拡散力ゼロのままでも検証できる。

1. **直接インタビュー5人(最優先)。** 増減量サイクルを回している知人・ジムのつながり・Xで実際に会話した相手に、15分聞く。質問は「月1,500円払うか」ではなく「**今どうやって消費カロリーを見積もっているか**」「**スマートウォッチの数字を信じて食事量を決めているか**」。10.5-(2)で唯一断定できた優位(ウェアラブルとの乖離647kcal/日)が刺さる相手が実在するかを直接確かめる。3人が「ウォッチの数字で食事を決めている」と答えれば、メール登録10件より強い証拠になる。**これは今日からできて、到達ゼロでも成立する。** #165 が問うた「配る相手が実在するか」と同じ問いを、配布ではなく会話で確かめる形
2. **分母を借りる。** 自分のフォロワーに配るのではなく、既に人がいる場所へ置く(はてなブックマーク経由のZenn、筋トレ・減量系のコミュニティ、Xで該当層への返信)。`content-ops` の `README.md` の「急いで収益化を狙わない」方針とは、宣伝ではなく検証のための一時的な措置として区別する
3. **事前決済・デポジットは採らない。** 1件あたりの情報量は最強だが、12.5 の特商法・決済の手続きが先に来てしまい、ゲート通過前に整えるべきでないものを整えることになる。順序が逆転するため不採用

### 13.5 すでに走っている実験がある

2026-08-07 から X の毎日投稿(ストック34本・予約投稿、`content-ops` の `README.md`)を開始しており、これは事実上「**到達を作れるか**」の実験になっている。需要検証1本目(8/20)までに2週間分のデータが出るため、**13.3 の分岐の一部は前倒しで判断できる**。

→ **8/20 の公開前に、Xのインプレッションとプロフィールアクセスが8/7以前と比べて伸びているかを確認する。** 伸びていなければ、記事3本の到達も伸びない可能性が高く、その時点で 13.4-(1) のインタビューを主線に切り替える判断ができる。10/1まで待つ必要はない。

### 13.6 まとめ

- **「拡散力が無いから需要検証は不要」は採らない。** 測定器が壊れていることを、需要が無いことの証拠に使ってしまう
- **「今の合格基準(累計10人)は機能しない」は正しい。** n=13 では0件でも何も言えない(rule of three)
- **ゲートを合否判定から「配信の課題か、需要の課題か」の分岐に組み替える。** これなら到達が足りなくても判定は必ず下りる
- **保留の回数に上限を付ける。** 現在の設計の唯一の欠陥はここ
- **インタビュー5人を並走させる。** 到達ゼロでも今日からできる唯一の検証
- **8/20 の前にXの2週間の数字を見る。** 判定の一部は前倒しできる

### 13.7 決済を外した「数人への配布版」を検証手段にする案(2026-08-09)

本人の提案:「数人に使ってもらえる形にして配布するのが良いと思っている。決済は除いて、各自のスプレッドシートに保存できる。Garmin連携が無くても動く。これを実装すれば配布自体はできるのでは」。

**結論: 賛成。ただし実装の前にインタビュー(13.4-(1))を置くこと、および測るものを先に決めることが条件。**

#### 11.1-(3) の不採用を機械的に引き継がない

11.1-(3) は外部ロードマップの「クローズドβ 5〜20人」を「このプロジェクト最大の実装項目であり、無料の前提条件として扱えない」として不採用にした。その根拠は (a) 全員が同じシートに書く構造、(b) 認証が単一トークン、の2点だった。**このうち (a) は 12.7 の OAuth + `drive.file` で解け、(b) は数人への無償配布なら D1 すら不要**(下記)。前提が変わったため、同じ結論をそのまま引き継ぐべきではない。加えて 13章 で「記事のチャネルでは測定そのものが成立しない」ことが分かったため、到達に依存しない検証手段の価値が上がっている。

#### 確認できたこと: Garmin 無しで動くのは設計上すでに担保されている

- `TrendsPage.tsx` の活動記録クエリは、レコードが1件も無ければ `null` を返す(CLAUDE.md が定めた `useLiveQuery` の null 正規化パターン)。ローディングから抜けられなくなる問題は起きない
- `weeklyDigest.ts` の活動集計は**活動記録0件の週を `undefined` にして digest から activity セクションを省く**(mood と同じ扱い)。**AIコメントの経路も degrade する**

→ 本人の見立てどおり。残るのは「活動」タブを選んだときの空状態の見た目で、ここだけ実機確認が要る。

#### 見つかった実害: `DEFAULT_SETTINGS` に自分の減量目標が入っている

`src/db/settings.ts` の `DEFAULT_SETTINGS` は `goalWeightKg: 64` / `goalDate: "2026-10-31"` / `dailyCalorieTarget: 1900` — **開発者本人の目標がハードコードされている**。しかも #164 の設計で「既定値は読み取り時にだけ被せ、保存はしない」ため、他人が未設定のまま使うと**自分の減量目標に対する進捗が表示され続ける**。配布前に必ず潰す。既定値を撤去して未設定を明示するか、初回オンボーディングで入力を必須にする。

#### 実装として要るもの(最小構成)

1. **OAuth(案A、12.7・12.8)+ ユーザーの Drive にスプレッドシートを新規作成する導線** — これが本体
2. **記録5タブの自動作成**(12.7-(4)。現状は手動作成前提)
3. **`DEFAULT_SETTINGS` の撤去と初回設定の導線**(上記)
4. **`API_AUTH_TOKEN` の複数トークン化** — `worker/auth.ts` の `isAuthorized` はシークレット1本との文字列一致なので、**カンマ区切りの複数値にするだけ**(数行)で人ごとに違うトークンを配れ、1人分だけ失効もできる。12.3 の D1 は課金を始めるときの話で、無償配布には要らない
5. **Gemini への送信に関する同意の導線**(下記)

#### 明示的に外すもの

決済・課金判定(12.2・12.3)、D1、特商法の表記(12.5 — 無償配布は通信販売にあたらないため不要)。

#### 注意すべき点

- **他人の健康データが Gemini に流れる。** 記録が各自のシートに保存されるようになっても、AIコメントと写真判定は自分の Worker 経由・自分の API キーで Google に送られる。要配慮個人情報にあたるため、**無償でも説明と同意の導線が要る**。要件定義書5.3 のプライバシー方針を配布用の説明文に起こす
- **原価が自分持ちになる。** 数人なら誤差だが、人数と頻度の上限を意識しておく
- **サポートが週10時間の枠に入る。** 5人でも「動かない」は来る
- **一度配ると引っ込めにくい。** データは相手の Drive にあるので消えはしないが、止めれば相手は困る

#### 最重要: 何を測るのかを先に決める

配布は手段であって、目的を決めずに実装すると #67 の反復になる。13章の枠組みに載せると:

- **測るもの: 継続率。** 2章の基準1(自分が使い続けられること)を他人で検証する。5章が「最大のリスク」とした目標達成後の離脱を、自分以外のデータで観測できる
- **測らないもの: 支払い意思。** 無償配布は価格の検証にならない(継続した人に後から聞くことはできる)
- **合格基準と期限を先に決める。** たとえば「5人に配り、4週間後に3人以上が週4日以上記録していれば継続の目安」。届かなければ課題は有料化ではなくプロダクト側にある

#### 順序: インタビューが配布のゲートになる

**13.4-(1) のインタビュー5人を先に置き、そこで「使いたい」と言った相手に配るために実装する。** この順序なら、実装前に「配る相手が実在するか」(#165 の問いそのもの)を確かめられ、11.1-(4) が警告した「実装 → 検証」の反転も避けられる。

1. インタビュー5人(実装ゼロ、今日からできる)
2. 「使いたい」が出たら、その相手に配るために上記の最小構成を実装する
3. 配布して4週間の継続率を見る

#### Issue化(2026-08-09)

上記の最小構成を **Issue #213(親)** と子6件に分割した。**着手のゲート(インタビュー5人)は #213 の本文に明記してある。**

| Issue | 内容 | 依存 |
|---|---|---|
| #214 | Google OAuth の認可フローとトークン交換エンドポイント | すべての起点 |
| #215 | シート同期・取り込みをクライアントから直接 Sheets API に呼ぶ | #214 |
| #216 | スプレッドシートをユーザーのDriveに新規作成し、記録5タブも自動作成に含める | #214 |
| #217 | `DEFAULT_SETTINGS` から本人の減量目標を撤去し、初回セットアップの導線を作る | 独立 |
| #218 | `API_AUTH_TOKEN` の複数トークン化 | 独立 |
| #219 | AIに何を送るかの説明と同意の導線 | 独立 |

#### 日程(2026-08-09決定)

**8月中の全件完了は目指さない。実装は9月に置く。** 8月に詰め込むと (a) #213 のゲート(インタビュー5人)を自分で踏み越えることになり、(b) 単純に時間が合わないため。8件の合計は30〜50時間規模に対し、8月の残りは3週×週10時間で、そこには週次レポート4本(8/10・17・24・31)、8/20の需要検証1本目の公開準備、X毎日投稿の返信対応が既に入っている。

| 時期 | やること | 備考 |
|---|---|---|
| **8月** | **インタビュー5人**(#213 のゲート)、#212、#217 | #212・#217 は**配布するかどうかと無関係に正しい修正**のため、ゲートに抵触しない。合わせて8〜10時間程度 |
| **9月** | #214 → #215 → #216 | **インタビューの通過が条件。** #215 が最長。9/3・9/17 の需要検証記事と週次4本が同じ枠にある |
| **配布直前** | #218、#219 | 合わせて4〜5時間。先に作っても腐るだけなので後ろに置く |

**9月末に「配布できる状態」まで到達するのは楽観的な見立て。** #214〜#216 だけで22〜33時間規模に対し、9月の空き時間も15〜20時間程度。**10月前半までは見ておくのが現実的**で、そこは 10.6 が「10/1のゲート判定・#116 維持モードの実装・最終回記事が同時進行する」と警告した混雑月にあたる。**遅れてから気づくのではなく、最初からそう見積もっておく。**

**#215 を早めに軽くする選択肢がある(ゲートに抵触しない)。** #215 の実体の多くは `worker/sheetsSync.ts`・`sheetsImport.ts` の純関数(`planUpserts`・`planWeightImport` ほか)を `src/` 側から呼べる場所へ移すこと。これは**振る舞いを変えない配置替え**であり、12.8 で「いま守れる制約」として挙げた2点(純関数の切り出しを維持する / 新しい同期ロジックを Worker 側にだけ足さない)の延長線上にある。配布の実装ではないためゲートには当たらず、長い工程を前倒しで削れる。

---

## 14. 実行フェーズへの移行(2026-08-25)

本人の意向「マネタイズに向けて本格的に動き出したい」を受け、12・13章(2026-08-09)以降に変わった前提を洗い直し、**検討から実行へ移すための計画**にしたもの。本章も決定ではない — 決めるべきものは 14.6 に4点だけ切り出してある。

### 14.1 8/9 以降に変わった前提

#### (1) 配布版が完成した(#213、8/23クローズ)

12.6 の「潰す順序」で2番目に置いた同期先シートの所有者問題(10.2-(2))は解けた。#214〜#219 と #238(プライバシーポリシー)が 8/23 に main へ入り、13.7 の最小構成は**全部そろっている**。13.7 の日程(9月〜10月前半)より 1〜1.5ヶ月早い。

副産物として **12.4 の前提が変わった**。#215 でシート同期がクライアント直になり、Worker に残ったのは `/api/google-oauth/*` と AI 3本(週次コメント・月次コメント・食事判定)だけになった。**「サーバから止められる機能」と「Worker に残った機能」が一致した** — これが有料の線を確定させる(14.3)。

#### (2) 着手ゲート(インタビュー5人)は踏まずに実装が進んだ

#213 の本文は「この実装に着手する前に、インタビュー5人を行う」と明記していたが、`content-ops` の進捗ログ・README のどこにもインタビューの記録が無い一方、実装は完了している。**事実として記録しておく**(13.4-(1) は 8/9 に「今日からできる唯一の検証」としたもので、2週間実行されなかった)。

ただし **#67 の反復にはなっていない。** 作ったのは配布に要るものだけで、決済・D1・特商法には手を付けていない(#213 が「明示的に外すもの」に挙げたとおり)。失ったのは順序であって、金と時間ではない。

→ **ゲートの役割を移す。** 実装のゲートとしては役目を終えた。**配布のゲートとしては生きている** — 配る相手が実在しないと、配布版があっても検証は始まらないため(14.2-B)。

#### (3) 需要検証の測定器が壊れていることが、数字で確定した

2026-08-25 に Drive を確認したところ、**「からだログ 案内待ちリスト 回答」はいまもヘッダー行のみ・最終更新は作成時刻(8/6 09:56)のまま**。募集開始(8/8)から17日、**需要検証1本目 note06(8/20)の公開から5日を含めて、登録は0件**。

同じ期間に X の到達は伸びている(フォロワー 11 → 250超、昼枠の平均いいね 3.07 → 5.51、8/20 は単独でいいね60)。**到達は増えたのに転換は0** — 13.3 が用意した分岐(配信の課題 / 需要の課題)のどちらでもない第3の状態にある。

| 段 | 現状 | 判定 |
|---|---|---|
| 到達 | 伸びている | 配信の課題ではなくなりつつある |
| **導線** | **フォーム名は「先行利用の案内待ちリスト」のまま(使えるアプリを「案内待ち」と呼んでいる)。1枚のフォームが「使う人集め」と「月1,500円の需要検証」を兼ねている** | **ここが未処理** |
| 需要 | 測定不能 | 導線が通っていないので、まだ何も言えていない |

→ **10/1 の判定表(13.3)に「導線が成立していない期間は判定しない」を足す。** これは 13.3 が回数制限を付けた「保留」とは別物 — 保留は**測ったが足りない**状態、こちらは**まだ測っていない**状態。`content-ops` の README「マネタイズの需要検証との関係」が 8/18 に挙げた3点((1) 主導線をアプリに短くする、(2) フォーム名を直す、(3) 固定ポストの差し替え)が、いずれも未実行のまま note06 の公開を迎えたことと同じ事実を指している。

### 14.2 実行計画(8/25〜10/1)

3本を並行させる。互いに依存が無いので順序ではなく同時に走る。

#### A. 導線を通す(実装ゼロ・今週)

1. **主導線を「フォームに登録」から「アプリを開く」に変える。** #217(初回セットアップ)・#238(プライバシーポリシー)・#219(AI送信の同意)が入り、開いてすぐ使える状態になっている。「案内待ち」はもう事実に反する
2. **フォームを2つの仕事から解放する。** 使う人集めはアプリURLに任せ、**フォームは「月1,500円の有料機能に払うか」の意思表示だけを聞く1枚に絞る**。名前も実態に合わせる
3. **固定ポストを募集導線に差し替える**(8/10決定・未実行)。250人が最初に読む1本
4. **配布に要る残りの実装は #246(配布手順書)と #245(活動タブの空メッセージ)の2件だけ。** #244(レート制限)は配布が2桁になってからでよい

**注意: 「アプリを開く」を主導線にすると、APIトークンの配り方が即座に問題になる。** 本番は `API_AUTH_TOKEN` が設定されているため、トークンを持たない人は記録・グラフ・バックアップ(すべてクライアント完結)までは使えるが、**Google連携もAIも動かない**(`worker/index.ts` は `/api/*` を全部この認証の内側に置いている)。**「誰でも開ける導線」と「トークンを配る導線」は別物**であり、これは無料/有料の分け方と同じ問題なので 14.3 で併せて扱う。

#### B. 相手を作る(インタビュー5人 → 配布5人)

- 聞くことは 13.4-(1) のまま。「月1,500円払うか」ではなく「**いま消費カロリーをどう見積もっているか**」「**スマートウォッチの数字を信じて食事量を決めているか**」
- **配布の条件を先に決めておく: 4週間、週1回、3行の返信**(記録した日数 / 使わなかった日の理由 / 詰まった所)。14.4 のとおり継続率は原理的に観測できないため、この報告が唯一の測定手段であり、同時に**相手の本気度のフィルタ**にもなる
- 9月末まで配布0人なら、それは配信でも需要でもなく**接触の課題**。13.4-(2)(既に人がいる場所へ置く)へ切り替える

#### C. 有料化で「いま決められること」だけ決める(実装ゼロ)

12.6 の順位1(特商法の住所・電話番号)と 12.4(有料の線)は**意思決定であって実装ではない**ので、ゲートの前に決めてよい。決めても金は動かない。むしろ 12.5 の3択が No なら以降が全部不要になるため、**先に決めるほど無駄が減る**。→ 14.6

### 14.3 有料の線を、サーバで止められる面に合わせる

12.4 は3案を並べて「1 と 2 の組み合わせが素直」としていたが、**#215 の後は案1がそのまま現在の構成と一致する。**

| 面 | 計算場所 | サーバで止められるか | 区分 |
|---|---|---|---|
| 記録・推移グラフ・完全バックアップ | クライアント | × | 無料 |
| 実測TDEE・週次/月次ダイジェストの数値 | クライアント | × | **無料** |
| スプレッドシート同期 | クライアント直 + `/api/google-oauth/*` | △(トークン) | 無料 |
| **週次・月次のAIコメント** | Worker | ○ | **有料** |
| 食事のテキスト/写真判定 | Worker | ○ | 無料(下記) |

**3章の「課金の心臓部は実測TDEE」は、ここで正式に取り下げる。** クライアント計算で止められない以上、有料の柱には据えられない(12.4)。代わりに**実測TDEEは無料の目玉に回す** — note06 で書いた主張(ウェアラブルの数字より逆算のほうが実態に近い)を、読んだ人が無料でそのまま確かめられる状態にしておくほうが、記事と製品が一致する。

**ロックインの理屈は無料側に置いても崩れない。** 蓄積が増えるほど「この数字をどう読むか」の需要が増え、それに答えるのがAIコメント=有料側だから。3章の「解約すると蓄積の価値がゼロに戻る」は、「解約すると**蓄積を読んでくれる相手がいなくなる**」に読み替わる。

**食事判定を無料に残す理由**: 原価は Gemini Flash 級で誤差、かつ入力コストは継続率の生命線(記録が止まれば有料も無い)。10.2-(1) の線(その日のうちに閉じるものは無料)とも一致する。ただし**人数に対して原価が線形に効く唯一の面**なので、#244(レート制限)はここに効く。

#### 実装は「トークンを2種類にする」だけで足りる(D1は当面不要)

`worker/auth.ts` は許可トークンの配列との一致を見るだけになっている(#218)。**シークレットを2本に分け、パスで見る先を変える**と、無料/有料が数行で分かれる。

- `API_AUTH_TOKEN`(無料) — `/api/google-oauth/*` だけ通す
- 有料トークン — 上記に加えて `/api/weekly-advice`・`/api/monthly-advice` も通す

補足:

- **課金状態の置き場(12.2 の D1)は、手運用が痛くなるまで要らない。** 決済(Stripe Payment Link)→ 手でシークレットに1本足す、解約 → 1本外す。70人では破綻するが、**最初の10人では破綻しない**。12.2 の実装は人数が痛みになってからでよい
- **有料機能を無料トークンで叩いたときの返り方を分けること。** いまの401の文言は「設定画面のAPIトークンが…一致しているか確認してください」で、無料ユーザーには意味不明になる。`402` + `code: "plan_required"` のように分け、#214 の失効判定(`google_reauth_required` のときだけ保存済みトークンを捨てる)に**巻き込まれない**形にする
- **無料でも同期にはトークンが要る**点は残る(`/api/google-oauth/token` は client_secret の代行窓口で、認証の外に出せない。12.8)。配布のたびに無料トークンを1本発行して渡す運用になる

### 14.4 継続率は、いまの設計では測れない — 5人なら「聞く」

#213 の合格基準(4週後に3人以上が週4日以上記録)には**観測手段が無い**。記録は各自の IndexedDB と各自の Drive にあり、こちらからは見えない。これはプライバシーポリシー(#238)の「記録はお預かりしません」の裏返しで、**正しい状態**である。

| 手段 | 分かること | コスト |
|---|---|---|
| **(1) 本人に週1で聞く** | 記録日数・詰まった所・**やめた理由**。理由まで分かる唯一の手段 | 実装ゼロ。5人なら現実的 |
| (2) Worker でトークン別の最終アクセスを残す(KV 1本) | 「AI機能を使った日」だけ。無料ユーザーは認可更新でしか映らない | 数行+KV。**プライバシーポリシーに「いつ使ったか」の記録が増えるため要説明** |
| (3) Cloudflare Web Analytics | 訪問数。人単位にはならない | ビーコン1行 |

→ **(1) を主、(3) を補助。**(2) は課金を入れるとき(D1)に一緒に考える。**5人の検証のために計測基盤を作るのは、また実装を先に置くことになる。**

これに伴い、`content-ops` の `X投稿テンプレート.md`・`x-context.md` にある「使い続けている人は原理的に測れない(**認証は単一トークン**)」の記述は根拠が古い(#218 で複数トークンになった)。測れないという結論は変わらないが、理由は「単一トークンだから」ではなく「**記録がこちらに来ないから**」。

### 14.5 10/1 に何を判定するか(13.3 の差し替え)

判定を2本立てにし、それぞれ「次にやること」まで先に書いておく。

**判定1: 配布(継続率)**

| 10/1時点 | 判定 | 次にやること |
|---|---|---|
| 配布0人 | **接触の課題** | 13.4-(2) 分母を借りる。記事チャネルでの検証は一旦下げる |
| 配布1〜4人 | サンプル不足 | 4週回してから判定。判定日を後ろへずらす |
| 配布5人・4週後に3人以上が継続 | **プロダクトは成立** | 14.6 の決定に沿って有料化の準備へ(12.6 の順位3・4) |
| 配布5人・継続2人以下 | **プロダクトの課題** | 有料化を止め、やめた理由に対処する。5章の10/31問題がここに出る可能性が高い |

**判定2: 需要(記事 → 意思表示)**

| 10/1時点 | 判定 | 次にやること |
|---|---|---|
| 導線の修正が未実行 | **判定しない**(14.1-(3)) | 修正して4週見る。**この保留は1回まで** |
| 修正済み・note06〜08のPV合計 < 100 | 配信の課題 | 13.3 のまま(配信側の改善) |
| PV ≧ 100・意思表示 < 2% | **需要の課題** | 仮説を棄却し、2章の候補選定に戻る |
| PV ≧ 100・意思表示 ≧ 5% | **通過** | 12.6 の順序へ(順位1・2は 14.6 で先に決まっている) |

**分母を必ず記録すること。** note の PV は各記事のダッシュボードで見えるので、9/3・9/17 の公開翌週に控える(**現状 note06 の PV はどこにも記録されていない**)。Google フォームはビュー数を取れないため、分母は記事 PV で代用する。

**「累計事前登録10人」という絶対数の基準は捨てる。** 13.2 のとおり、この分母では達成できず、未達でも何も言えないため。

### 14.6 いま本人が決めること(4点)

| # | 論点 | 選択肢 | 推奨 | 決めないとどうなるか |
|---|---|---|---|---|
| 1 | **特商法の住所・電話番号**(12.5) | A: 省略+請求時に開示 / B: バーチャルオフィス / C: IP電話 | **A で始める**。費用ゼロ。Stripe の審査で弾かれたら B へ | ここが No なら有料化そのものが成立しない。**最初に決めるほど無駄が減る** |
| 2 | **有料の線**(14.3) | AIコメントのみ / +食事判定 / 3章のまま(実測TDEE) | **AIコメントのみ** | 価格も説明文も書けない |
| 3 | **価格** | 1,500円 / 下げる | **1,500円据え置き。**ただし売り文句は「AIコメント月4本」ではなく「**毎週、続いていることを評価してもらえる**」。300〜500円はあすけんの土俵(11.1-(1)) | 記事の書き方が決まらない |
| 4 | **10/31以降の自分の目標**(5章、8/31 が期限の3択) | 目標日を延ばす / 目標体重を上げる / 摂取を下げる | 本人判断。**#116(維持モード)の実装順とセットで決める** | 先送りすると自動的に「摂取を下げる」を選んだことになる(note10 で公開済み) |

### 14.7 いまはやらないこと(明示)

- **D1・決済の実装**(14.3 の手運用で最初の10人までは足りる)
- **特商法の表記ページの作成**(決めるのは 14.6-1 だけ。書くのは課金を始める直前)
- **Android / TWA**(12.5 のとおり法務・決済の回避策にならない)
- **#244 レート制限**(配布が2桁になってから)
- **有料機能の新規開発**。有料側に置くのは既存の AI 2本で、**新規開発ゼロで始められる状態にある**

11.4 の注意をそのまま引き継ぐ: **本章は「思ったより近い」ことを、判定を飛ばす理由に使わない。** 14.1-(2) で順序が一度反転している以上、ここは意識して守る。

### 14.8 日程(8/25〜10月)

| 時期 | やること |
|---|---|
| 8/26〜8/31 | 導線 A-1〜3(実装ゼロ)、#246・#245、インタビュー着手、14.6 の 1〜3 を決める |
| 8/31 | 週次レポート(8/24週)。**14.6-4(10/31目標の3択)の期限** |
| 9/1〜9/7 | 配布5人。**配布日を各人ごとに記録する**(4週後が判定日になるため) |
| 9/3 | 需要検証2本目。**一部有料(300円)で出す**(14.9)。**翌週に PV を控える** |
| 9/17 | 需要検証3本目。同上(**無料**) |
| 9月中 | 週1の報告を回収(14.4-(1)) |
| 10/1 | 判定1・判定2(14.5) |
| 10月 | 通過なら 12.6 の順位3・4(トークン2種類化 → Stripe)。10.6 が警告したとおり最終回記事・#116 と重なる混雑月 |

### 14.9 note の有料記事(2026-08-25、本人の意向)

本人の希望:「note で有料記事を出してみる。**モチベーションアップが目標**」。

**結論: 賛成。ただし本数を増やさず「一部有料」でやること、導線を有料の壁の向こうに置かないことが条件。**

#### 現行方針と衝突しない理由

`content-ops` の README(2026-07-16)は「note/Zenn の有料記事は数千人規模の読者がいて初めて意味を持つ」としており、**収益手段としてはこの判断のまま**。今回の位置づけは収益ではなく、次の2つ。

1. **モチベーション**(本人の目的)。2年計画(1章)を走らせるうえで、初めての売上が出ることの価値は金額と比例しない
2. **13.4-(3) で不採用にした「事前決済」の代わりになる。** 当時これを外した理由は「12.5 の特商法・決済の手続きが先に来てしまい、ゲート通過前に整えるべきでないものを整えることになる」だった。**note は決済をプラットフォーム側が担うため、その順序の逆転が起きない** — こちらは Stripe も特商法ページも用意せずに「**実際にお金を払う人が実在するか**」を測れる。14.5 の判定2(フォームでの意思表示)より1段強い証拠になる

**一次情報を確認した(2026-09-01)。** 以下は note ヘルプおよび利用規約からの引用にもとづく。

**手数料は「事務手数料 → プラットフォーム利用料 → 振込手数料」の順に引かれる。**

- **事務手数料**(決済ごとに発生): クレジットカード5% / PayPal6.5% / PayPay7% / Amazon Pay7% / noteポイント10% / **携帯キャリア決済15%**。**「クリエイターご自身が登録されている決済手段ではなく、購読者の決済手段によります」** — **こちらでは選べない**
- **プラットフォーム利用料**: 有料記事・有料マガジン・チップ・メンバーシップは**10%**(定期購読マガジンのみ20%)。**売上から事務手数料を差し引いた金額に対してかかる**
- **振込手数料**: **270円**(売上金支払い1回につき1回)

**★単純な足し算ではない**(5%+10%=15% ではなく、10%は事務手数料を引いた後の額にかかる)。300円の記事だと:

| 購読者の決済手段 | 事務手数料 | 利用料10% | 手取り | 手取り率 |
|---|---|---|---|---|
| クレジットカード | −15円 | −28.5円 | **256.5円** | 85.5% |
| noteポイント | −30円 | −27円 | 243円 | 81% |
| 携帯キャリア決済 | −45円 | −25.5円 | **229.5円** | 76.5% |

**★振込には下限がある。**「**前月末までの未振り込みの売上金額が合計1,000円以上の場合、振込申請いただけます**」。**300円の記事はクレカ決済でも4件売れて1,026円で、そこで初めて申請できる。** そこから振込手数料270円が引かれるので、**4件で手元に残るのは756円**。振込日は 2〜20日申請なら当月末、21日〜翌月1日申請なら翌月末。

**表示義務はクリエイター側に立つ。** 利用規約は「**クリエイターは、自身のページに特定商取引に関する法律その他の法令に従った表示を行います**」。ただしヘルプは、義務がかかるのは「**販売業者または役務提供事業者**」= 「**営利の意思をもって、反復継続して取引を行う**」場合に限るとし、当てはまらなければ掲載不要と明記している。該当する場合は「**原則的には、住所、氏名、電話番号が必要**」。

**★住所氏名の掲載を省略する規定がある。**「**開示請求があった際に『遅滞なく開示できる場合に限り、その旨を掲載の上、掲載を省略することができる』**」。**12.5 で「工数ではなく自宅住所・電話番号を公開するかの意思決定」としていた論点は、note で売る限りにおいてはこの省略規定で回避しうる**(Stripe で自前決済する場合の 12.5 の議論とは別に扱う)。

**「反復継続」に当たるかの線引きは、ここでは断定しない。** 9/3 の1本だけなら単発だが、週次で有料記事を継続するならどこかで該当する側に寄る。**法律判断なので、必要なら公開前に専門家に確認すること。** なお省略規定があるぶん「念のため表示しておく」を選んでもコストは低い(住所氏名を晒さずに済む)。

**出典**: [コンテンツを販売する際に引かれる手数料](https://www.help-note.com/hc/ja/articles/360011358873) / [売上金を受け取る](https://www.help-note.com/hc/ja/articles/360010430873) / [特商法の表示について](https://www.help-note.com/hc/ja/articles/360008947533) / [note ご利用規約](https://terms.help-note.com/hc/ja/articles/44943817565465)

#### やり方

| 論点 | 決め |
|---|---|
| **どの記事** | **9/3 の需要検証2本目「停滞期の正体」を一部有料にする**(2026-08-25、本人の「もう少し前に出したい」を受けて 9/17 から変更。**9/17 は無料に戻す**)。新規の本数を増やさないため(README の「月1〜2本のストック記事を上限」を崩さない)。**8月に足す案は採らない** — 8/27(木)は空いているが、それは実質「9/3を1週間前倒しする」ことで**8月のストックが4本**(上限の2倍)になる。8/13(note08)の例外は「既に書き上がっていて新規取材が要らない」が根拠で、**失速2の期間集計という新規の算出が要る有料記事には使えない** |
| **形式** | **一部有料(前半無料)。** 全文有料にすると到達が死に、14.5 の判定2の分母ごと失う |
| **無料部分** | **主張が無料部分だけで完結すること。** 「停滞の原因は毎回違う。だから決め打ちしてはいけない」という記事の柱(停滞1は摂取が増えて止まり、失速2は摂取を減らしても減らない)はここに置く |
| **有料部分** | **読み物ではなく道具を売る。** 14週の全数表(体重・摂取・PFC・歩数・筋トレ・Garmin総消費・逆算TDEE)と、**同じことを自分のデータでやるための手順**(局面の切り方・見る順番・やってはいけない読み方・再現の型・答え合わせの設計)。**なお `content-ops` は private** — 2026-08-25に一度「全数表は公開リポジトリにあるから有料にできない」と判断したが、**それは content-ops を public と誤認していたための誤り**で、撤回した(公開なのは `lifelog` のほう) |
| **価格** | **300円。** 個人noteの標準帯。モチベーションが目的なら、単価より購入数が出るほうが効く。月1,500円の検証は14.5の判定2(フォームの設問4)で別に測る |
| **導線** | **アプリURLとフォームは必ず無料部分に置く。** 壁の向こうに置くと購入者にしか届かない |
| **やらないこと** | **週次レポート(無料の定型)を有料にしない。** 読者との約束を後から変えることになる |

#### 測るもの

- **購入数**(note のダッシュボード)。**1件でも売れたら「お金を払う人が実在する」の最初の実例**になる
- **14.5 の判定には混ぜない。** 判定2の分子は設問4の意思表示のままにする(価格帯も買うものも違うため)。有料記事の実績は別に記録する

### 14.10 外部レビュー(note有料化の提案)のレビュー(2026-08-25)

本人が別のAIから受け取った「現在の記事構成を踏まえた収益化戦略(アップデート版)」を、14.9 および `content-ops` の運用ルールと突き合わせた結果。11章(Grokのロードマップ)と同じく**採用・不採用を理由込みで残す**。

**結論: 骨子(週次=無料の信頼装置 / 深い分析=有料 / 固定記事=入口)は 14.9 とほぼ同じで、追認として有効。ただし4点は採らない。**

#### レビューから得た新しい事実(リポジトリに無かった数字)

**スキ数: note01=4、note06(実測TDEE)=24。** 週次レポート系より**課題提示型が明確に反応が良い**という7章の実績(「課題提示型を主軸にし、レポート形式は比率を下げる」)を、note06 の実データが追認した形。**ただしスキは購入でも登録でもない** — note06 の告知投稿はインプ117・記事への詳細クリック2で、事前登録は0件のまま(14.1-(3))。**「情報密度は十分」は質の話で、到達の話ではない。**

#### 採用(3点)

1. **週次レポートは無料のまま「信頼を積む装置」として続ける。** 14.9 の「週次を有料にしない」と同じ結論に、別ルートから到達している
2. **X告知で「無料部分だけでも読めます」と明示する。** 一部有料は「全文有料」と誤解されると開かれずに終わる。9/3 の告知文にこの1行を入れる
3. **有料部分の素材として「77日分の実データ全公開」。** 14.9 が「読み物ではなく道具を売る」としたものと中身が一致する。レビューの候補1が言う「詳細な表・計算式・失敗パターン」は**そのまま有料部分の構成になる**(`content-ops` は private なので、下書きをリポジトリに置いても公開にはならない)

#### 不採用(4点)

1. **「note06 をベースにした有料版」= 既発表記事の焼き直しを有料化する案。** note06 は 14.5 の判定2の分母を作っている記事で、同じ素材を有料で出し直すと、無料で読んだ読者には二度目に見える。`content-ops` の運用ルールは「記事は公開後に直さずスナップショットとして扱う」で、note01 を直さず note08 を新規で書いた前例もある。**新規の記事に有料部分を付けるほうが素直**で、レビューが挙げた素材はそのまま 9/3 の有料部分に入る
2. **「月1〜2本:深い分析を有料で出す」という定常運用。** `content-ops` の上限は「週1の定型レポート+**月1〜2本のストック記事**」で、これを全部有料に回すと**無料のストック記事が消える**。到達(=14.5 判定2の分母)を作っているのはその無料ストックなので、判定そのものが成立しなくなる。**当面は 9/3 の1本だけを試す**
3. **候補2「週次AIコーチの設計・プロンプトの全過程」を有料の1本目にする案。** 読者はエンジニアで、2章が「**エンジニア向けは最も課金しない層**」として本命から外している層にあたる。Zenn の無料記事としては良い題材だが、有料の1本目には向かない
4. **「有料記事の最後にβテスター導線を置く」。** そこは**購入者にしか見えない**。14.9 のとおり**アプリURLとフォームは必ず無料部分に置く**(壁の向こうに置くと判定2の分母が死ぬ)。有料部分の末尾に「重ねて」置くのは構わない

#### 位置づけの修正(1点)

レビューは有料記事を**収益化戦略そのもの**として提示しているが、**1章の目標(月10万円)への寄与は誤差**で、`content-ops` の README(2026-07-16)が「note/Zenn の有料記事は数千人規模の読者がいて初めて意味を持つ」とした判断は変わらない。11.1-(1) で外部ロードマップに対して指摘したのと同じ構造(**収益目標額を明示しないまま「収益化」と呼ぶ**)がここにもある。

**14.9 の位置づけを維持する**: 有料記事は (1) モチベーションと (2) 13.4-(3) で順序の都合から外した「事前決済」の代わり。**本線は 14.2〜14.5(導線 → 配布 → 10/1の2本立て判定)のまま。**

#### 価格について

レビューは「500円で出し、反応を見て980円」を推している(根拠はスキ24)。**14.9 の300円から500円に上げるのは許容範囲**だが、**スキは購入の予測子ではない**。目的がモチベーション(=1件でも売れること)である以上、**購入確率の高い側に倒すのが目的に沿う**。980円は、有料の本線(月1,500円のAIコメント)と価格帯が近すぎて、記事1本と月額サブスクの価値が読者の中で競合する点にも注意。**300円のまま、500円は本人判断で可。**

---

## 15. 発信の実測値による13章の判定と、ゲート運用の失敗(2026-08-25)

13.5 が「8/20の公開前に確認する」と指定した前倒し判定を、実際の観測値で下した結果。あわせて、13章の枠組みそのものに欠けていた分岐と、#213 のゲート運用が機能しなかった事実を記録する。

**結論: 13章の設計は正しかったが、運用されなかった。判定に必要な数字は揃いつつあり、13.5 の前倒し判定は「Xの到達は伸びた・noteへは流れなかった」という13.5の分岐に無い第3のケースで着地した。加えて 13.4-(1) のインタビューが3週間未実施のまま #213 の子Issueが全件完了しており、11.1-(4) が警告した「実装 → 検証」の反転が3度目に起きている。**

---

### 15.1 発信3チャネルの実測値(2026-08-25)

**数値の詳細は `content-ops` の「発信の実測記録」が正。** 本節は以降の判断に必要な範囲だけを再掲する。

| 指標 | 8/7以前 | 2026-08-25 |
|---|---|---|
| X 1投稿あたり imp | 30前後 | 40〜110 |
| フォロワー | 50 | 323 |
| note 週全体ビュー | 20〜30 | 90 |
| **note 記事1本の最高ビュー** | **13** | **48**(需要検証1本目・8/20公開) |
| Zenn 累計表示(5本・約1ヶ月半) | — | 144 |
| **事前登録** | **0** | **0** |

判断に効く点は3つ。

1. **7章補足の「note全体で週20〜30ビュー」は週90へ約3倍に伸びた。** 一方で記事1本あたりの最高は48にとどまり、10.2-(6) が指摘した「分母が10倍の飛躍」は解消していない
2. **Zenn は5本・累計144回。** 7章は Zenn を「読者はエンジニアで課金しないが、信用とシェアを生む」と位置づけたが、**現状はシェアも生んでいない**。13.4-(2) の「分母を借りる(はてなブックマーク経由のZenn)」は、想定ではなく**必須の条件**であることを意味する
3. **告知ファネルの制約は分母側にある。** 8/24の実測でフック投稿60imp → リンク投稿35imp(-42%) → note記事12view。**CTR 34% は低くない**ため、リンクをスレッド2投稿目に置いていること(外部リンク抑制と二重に効く)が制約であり、記事側の問題ではない

---

### 15.2 13.5 の前倒し判定 — 分岐に無い第3のケースで着地した

13.5 の指定:

> 8/20 の公開前に、Xのインプレッションとプロフィールアクセスが8/7以前と比べて伸びているかを確認する。伸びていなければ、記事3本の到達も伸びない可能性が高く、その時点で 13.4-(1) のインタビューを主線に切り替える判断ができる。

| 指標 | 8/7以前 | 2026-08-25 |
|---|---|---|
| X 1投稿あたり imp | 30前後 | 40〜110 |
| フォロワー | 50 | 323 |
| note 週全体ビュー | 20〜30 | 90 |
| note 記事1本の最高ビュー | 13 | 48 |
| **事前登録** | **0** | **0** |

**13.5 は「伸びた/伸びていない」の2分岐で書かれていたが、実際は「Xの到達は伸びた。noteの到達も伸びた。しかし登録は動かなかった」だった。** この形は分岐のどちらにも該当しない。

したがって 13.5 の「伸びていなければインタビューへ切り替え」は発火せず、**結果としてインタビューへの切り替え判断が下りないまま3週間が経過した。** 条件分岐に第3のケースを置いていなかったことが、13章の設計上の抜けにあたる。

---

### 15.3 13.3 の分岐表を現在の数字に当てた場合(暫定)

需要検証記事は **3本中1本のみ公開済み(8/20・48ビュー)**。2本目9/3・3本目9/17。

- 2〜3本目が同程度なら **累計約144** → 100を超える
  - そのとき0件なら rule of three で **登録率2.1%未満** → 13.3 の「登録率 < 2%(需要の課題)」にぎりぎり入る
- 2〜3本目が20〜30なら **累計100前後** → 「配信の課題」と「需要の課題」の境界上

**現状(n=48・0件)で言えるのは「登録率6.25%未満」のみ**で、13.2 の表では「30→ほぼ無意味」と「60→ようやく弱い証拠」の中間にあたる。まだ何も棄却できない。

**13.3 の分岐表には、境界値をどちらに倒すかのルールが無い。** 累計到達が100の前後で結論が反転する設計のままなので、判定日までに以下のどちらかを決めておく必要がある。

1. 境界(たとえば累計80〜120)を「判定不能」に含め、13.3 の回数制限のカウントに乗せる
2. 判定日までに意図的に到達を積む(13.4-(2))。これは「測定器を直すための介入」であり、13.1 の「捨てるべきは検証ではなく測定器」に照らせば正当と考えられるが、明示的に決めておく

---

### 15.4 note の「スキ」は指標として使えない

需要検証1本目は **48ビューに対してスキ24(スキ率50%)**、週次レポート(8/24公開)は **12ビューに対してスキ7(58%)**。note の一般的なスキ率は3〜10%であり、**一覧画面からスキを押せる仕様のため、記事を開かずに押された分が大半を占めているとみられる**(内訳は `content-ops` の実測記録を参照)。

7章補足は「1本目の課題提示型記事が3週連続で読まれ続けている一方、週次レポート形式は公開週のみで失速している」としてビューで比較していたが、**以後の記事評価にスキ数を混ぜないこと。** ビューで見ると需要検証1本目(48)は他記事(1〜12)の4〜5倍で、課題提示型を主軸にする7章の方針自体は支持される。

---

### 15.5 #213 のゲートが機能しなかった事実の記録

13.7 の記述:

> 13.4-(1) のインタビュー5人を先に置き、そこで「使いたい」と言った相手に配るために実装する。
> 着手のゲート(インタビュー5人)は #213 の本文に明記してある。
> **8月中の全件完了は目指さない。実装は9月に置く。** 8月に詰め込むと (a) #213 のゲート(インタビュー5人)を自分で踏み越えることになり、(b) 単純に時間が合わないため。

意思決定ログに記録された実績:

| 日付 | Issue | 内容 |
|---|---|---|
| 8/09 | #217 | `DEFAULT_SETTINGS` の目標撤去・`/setup` 新設 |
| 8/16 | #218 | `API_AUTH_TOKEN` の複数トークン化 |
| 8/17 | #214 | ユーザー自身のGoogle認可(**配布版の中核**) |
| 8/23 | #215・#216 | シート同期をクライアント直に移し、同期先をユーザーのDriveへ |
| 8/23 | #219 | AIへの送信の同意導線 |
| 8/23 | #238 | プライバシーポリシー画面 |

**#213 の子Issueは8月中にほぼ全件完了し、ゲートに置いたインタビューは実施されていない。** 13.7 が「8月に詰め込むとゲートを踏み越える」と名指しで警告した状態が、そのまま発生した。

#217・#238 は 13.7 自身が「配布するかどうかと無関係に正しい修正」としてゲート対象外に置いており、これらは問題にならない。**問題は #214〜#216 が検証より先に進んだこと**で、#67(需要未確認で基盤を整えた)・#165(配る相手の実在確認より先に基盤整備)に続く3度目にあたる。

**この節は責任の所在の記録ではなく、ゲートの実効性に関する観測として残す。** 文書にゲートを書くだけでは踏み越えを防げないことが3回の実績で示された。次の仮説では、ゲートを「文書に書く」以外の形(たとえば通過前のIssueをクローズできない状態にする、着手可能な子Issueを作らない)にする必要がある。

---

### 15.6 13.4-(1) が3週間実行されなかった理由 — 「聞く相手がいない」

本人の申告は「相手がいない」。X フォロワーの構成を確認したところ、これは主観ではなく構成の問題だった。

**直近フォロワー19人の内訳**(詳細は `content-ops` の実測記録):

| 層 | 人数 | 例 |
|---|---|---|
| ダイエット/筋トレ層 | **2** | 朝活くん@元体育教員(80→67kg・体脂肪20→9%)、Hana(銀座の会員制プライベートジム代表・食事と運動をアドバイス中) |
| エンジニア/AI層 | 3 | AIコンサル会社代表、AI系発信、社内SE |
| ジャンル無関係 | **14** | 競馬2、仮想通貨2、推し活、二次元、コスメ、料理・地方議員、スピリチュアル、MLM集客、夫婦暮らし ほか |

19人のほぼ全員に「フォローバック」表示があり、**相手側から先にフォローされている**。ジャンルを問わずフォローして回るアカウントの典型的な挙動で、**2章が定めたターゲット(増減量サイクルを回す筋トレ層)は323人中ごく少数**とみられる。

**これは 15.4(スキ率50%)と 15.2(到達は伸びたが登録は0)の両方を説明する。** 1日5〜20件のリプライ運用は、フォロワー数を増やす一方で **13.1 が守ろうとした測定器を毎日濁らせる方向に働いていた。**

→ **リプライ運用の目的を「フォロー返しを得ること」から「該当層を発見すること」に組み替える。** 13.4-(2) の「Xで該当層への返信」は、この形でなければ機能しない。

---

### 15.7 13.3 の分岐表に足りない行

15.6 の観測は、13.3 のどのセルにも当てはまらない。

| 観測 | 判定 | 13.3 の表 |
|---|---|---|
| 累計到達 < 100 | 配信の課題 | あり |
| 到達 ≧ 100・登録率 < 2% | 需要の課題 | あり |
| … | … | あり |
| **接触可能なターゲットが手元に数人しかいない** | **集客の宛先の課題** | **無い** |

これは「配信の課題(到達量が足りない)」とも「需要の課題(欲しい人がいない)」とも異なる、**「到達している相手が違う」**という第3の課題である。到達量を増やす前に直すべきもので、しかも量を増やさずに直せる。

**13.3 の分岐表に、この行を追加することを提案する。** 判定は「累計到達」だけでなく「**到達のうちターゲット層の割合**」も見る必要がある。

---

### 15.8 実施したこと(2026-08-25) — インタビューの最初の2件

15.6 で特定した2名に、X の DM で 13.4-(1) の質問を送付した。

| 相手 | フォロワー | 位置づけ |
|---|---|---|
| 朝活くん(@Sportsgfku) | 3,461 | 80→67kg・体脂肪9%。**増減量サイクルを回し切った実績**。noteでダイエットを発信しており言語化ができる |
| Hana(@hana_lmit) | 698 | 銀座の会員制プライベートジム代表。**背後に顧客がいる**ため、1件の回答以上の情報量が見込める |

**13.4-(1) からの変更点: 「15分ください」ではなく、質問を本文に直接書いた。** 初回接触で時間を確保する形は返信率が低いため、テキストで答えられる形にした。返信があれば深掘りする。

**アプリのURL・事前登録フォームは意図的に入れていない。** 13.4-(1) が「質問は『月1,500円払うか』ではなく」と定めた趣旨に従い、初回は情報収集に限定した。売り込みが混ざると回答が歪む。

**5人ではなく2人で開始した。** 5人が揃うまで待つ設計が3週間の停止を生んだため、母数より着手を優先した。

#### 返答の読み方(先に決めておく)

| 返答 | 判定 | 次にやること |
|---|---|---|
| 「ウォッチの数字を見て食事量を決めていた」 | 10.5-(2) の優位が刺さる相手が実在 | 仮説を維持。3人目以降を探す |
| 「ウォッチは見ていない/気にしていない」 | 647kcal/日の乖離が刺さらない | 2章の候補選定に戻る |
| 「そもそも計算していない、感覚でやっている」 | ターゲット設定そのものの見直し材料 | 2章の想定ターゲットを再定義 |

**いずれの返答でも 13.4-(1) の目的は達成される**(到達ゼロでも成立する検証)。

---

### 15.9 検討したが採らなかった案(2026-08-25)

今回の議論で出たが、既存章と矛盾するため不採用としたもの。**同じ案が再提案されたときに再検討を省くため記録する。**

| 案 | 不採用の理由 |
|---|---|
| 相談・伴走(5,000円×2人)で当面のキャッシュを作る | **1章**が受託ルートを「資産にならない」として明示的に不採用にし、プロダクト寄り2年計画を前提条件としている。目的(課金の心理的ハードルを外す)と規模は異なるが、評価軸は同じく当たる |
| プロダクトをエンジニア向けに寄せ、単価1,500円×70人を狙う | **6章**「コードが読めて自己ホストできるため、エンジニアを客にするなら課金は成立しない」と正面から矛盾。なお1,500円×70人は1章がからだログで既に置いている想定であり、対象を変える必要はない |
| Zenn を主軸に据えれば単価の高い層に届く | **15.1** のとおり Zenn 累計144回。トレンド入りしない限り自然流入がほぼ無い |

---

### 15.10 まとめ

- **13章の設計は正しかった。** rule of three・分岐への組み替え・インタビュー並走は、今回ゼロから再検討しても同じ結論に至った
- **13.5 の前倒し判定は第3のケースで着地した。** 「到達は伸びたが登録は動かない」を分岐に持っていなかったため、切り替え判断が下りないまま3週間が経過した
- **#213 のゲートは踏み越えられた。** 文書に書くだけでは3回とも防げていない。次はゲートの実装形態を変える
- **「相手がいない」の正体は、集客の宛先が違ったこと。** リプライ運用がターゲットでない層を集め、同時に測定器を濁らせていた
- **13.3 の分岐表に「集客の宛先の課題」の行を追加する必要がある**
- **インタビューを2件で開始した。** 5人を待つ設計が停止の原因だったため、母数より着手を優先した
- **note のスキ数は指標に使わない。** ビューのみで評価する

---

## 16. 需要検証インタビューの回答 — 入口の発見と、答えの不一致(2026-08-26)

15.8 で2名に送った DM の続き。公開リプで3名を加えて**計5名に接触し、8/26時点で3名から実質回答**を得た。13.4-(1) のインタビューは、人数の条件を満たす前に判定材料を出している。

**結論は3つあり、うち2つは同じ方向を向いていない。**

1. **想定していた入口(ウォッチのズレ)は、思っていたほど共有されていない。** 実質回答3件のうち2件が「ウォッチの数字では食事量を決めていない」
2. **代わりに「停滞」が共通の痛点として浮かんだ。** アマチュアもプロも停滞を代謝適応で説明しており、10.5 の検算はその説明に対する反証データを持っている
3. **ただし、その痛点に対してこちらが出せる答えは、現場の解き方と噛み合っていない。** 停滞した人が欲しいのは処方で、「消費カロリー推定が600ズレている」は診断にあたる

**2 だけを見ると「入口が見つかった」話になり、3 を併せて見ると「入口の先が用意できていない」話になる。両方を残す。**

---

### 16.1 接触の結果(5名中3名から実質回答)

| 相手 | 経路 | 回答の要旨 | 15.8 の判定表 |
|---|---|---|---|
| 朝活くん(@Sportsgfku) | DM・3往復 | ウォッチの日次消費は**参考程度**。基礎代謝×活動係数で維持カロリーを出し、1〜2週の体重推移で補正する。**食事記録はあすけんを使っている** | **ウォッチは見ていない** |
| にすい(@nisui_gakumas) | 公開リプ | 「停滞期があるのは調べて知っているので、気にせず継続する」 | **どれにも該当しない(第4パターン)** |
| Hana(@hana_lmit・銀座の会員制プライベートジム代表) | DM・4往復 | 顧客にスポーツウォッチ着用者はいるが、**食事量を消費カロリーで決めている人は少ない**。ジムでは糖質と脂質ベースで指導し、カロリーはあまり数えさせない | **ウォッチは見ていない**(顧客集団の観測なので実質 n>1) |

**未返信2名**: 減量修行僧KEN(@cutting85to75)・しょーた(@zelvo_5044)。**KEN は唯一「停滞は飢餓状態」と自己診断しており、課題認識がある側**なので、判定材料としては濃い。返信が来た場合は 16.6 の停止条件の「追加3件」に数える。

> **→ この段落の KEN の評価は 16.9-(1) で取り下げた**(2026-08-26 夜)。アフィリエイト集客アカウントで、「停滞は飢餓状態」は本人の困りごとではなくコンテンツである可能性が高い。**また、未返信が2名しかいない以上「追加3件」は算数上ありえない**(16.9-(2))。**上の記述は誤ったまま残してある** — 間違え方そのものが 15.5 と同じ観測対象になるため。

---

### 16.2 15.8 の判定表に足りない4行目(提案)

にすいの回答は、15.8 で先に決めた3つの読み方(「ウォッチの数字で食事量を決めていた」/「ウォッチは見ていない」/「そもそも計算していない、感覚でやっている」)の**どれにも当てはまらない**。**停滞そのものを課題として認識していない**状態だった。「知っているから気にしない」は、計算していないのとも、ウォッチを見ていないのとも違う。

これは 15.7 で 13.3 の分岐表に「集客の宛先の課題」を足したのと**同じ構造の穴**にあたる。表が想定していない状態が、実際の回答として出てきた。

**追加を提案する行**:

| 返答 | 判定 | 次にやること |
|---|---|---|
| **「停滞は知っているので、気にせず続ける」** | **痛点として認識されていない** | その相手には刺さらない。ただし「気にせず続けて実際に抜けられたか」を追って聞く価値はある(抜けられていなければ、認識されていないだけで痛点は存在する) |

**これは 15.8 の表への追加提案であり、15.8 および13章以前の既存記述は書き換えていない。**

---

### 16.3 入口は「ウォッチのズレ」ではなく「停滞」 — メッセージ層の付け替え(提案)

Hana(銀座の会員制プライベートジム代表)の発言:

> 第2の壁は1回目の停滞期。人間は体重が5-10%減ると省エネモードに入る。これは99%の方が引っかかるので、乗り越えさせるためのトレーナーの腕の見せ所

未返信の KEN も、自分の停滞を「飢餓状態」と説明している。**アマチュア(KEN の「飢餓状態」)もプロ(Hana の「省エネモード」)も、停滞を代謝適応で説明している。** これが市場のデフォルトモデルであり、**10.5 の検算はその説明に対する反証データを持っている**。

→ **入口は「ウォッチのズレ」ではなく「停滞」。** ウォッチのギャップは**答えの機構**であって、フックではない。**誰も自分の消費カロリー推定が狂っているとは思っていないが、「なんで減らなくなったの?」はほぼ全員が抱く。**

**これは 2章のターゲットに対する申し送りであって、4章の位置づけを変える話ではない。** 変わるのは**メッセージの層(何を最初に言うか)だけ**で、ターゲット定義(増減量サイクルを回す筋トレ層)も、4章の「入力レイヤーでは勝てない。評価レイヤーで戦う」も動かさない。**むしろ 4章は補強される** — 停滞の説明は評価レイヤーの仕事だから。

10.5 が需要検証の仮説文を「計算式とのズレ」から「ウェアラブルとのズレ」に差し替えたときと**同じ層の変更**にあたる(10.5 も「ターゲットは変えない」と明記している)。

---

### 16.4 ただし、その答えは現場の解き方と噛み合っていない

**16.3 だけで止めると、実態より楽観的な記録になる。**

Hana に消費カロリー推定の 600kcal のギャップをどう見るか**明示的に聞いた**ところ、返ってきたのは「トレーニンクはかなり優秀ですね」だった。**同意でも反論でもなく、論点として拾われなかった。**

彼女が示した「第2の壁」の越え方は、別のモデルだった。

> 食事量を少し増やす / 食事のパターンを変える / 筋トレのパターンを変える
> 刺激するポイントを変える
> 人間は慣れの生き物なので、それに慣れさせないことが成功の鍵

**からだログのモデル(正確に測る → 正しい目標値を再計算する → それに合わせる)とは別モデルで、処方が違う方向を向いている。** 彼女は**増やす**方向を示し、TDEE のギャップの論理からは**減らす**方向が出る。未返信の KEN も独立に「夜の炭水化物を増やして3日で −0.8kg」に到達している。

→ **停滞が痛点なのは確かだが、その痛点に対して出せる答えが現場と噛み合っていない。** 停滞した人が欲しいのは「**次に何を変えるか**」という処方であり、「消費カロリー推定が600ズレている」は**診断であって処方ではない**。

**この不一致をどう埋めるかは、まだ答えが無い。** 16.6 の判定までに埋まらなければ、2章の候補選定に戻る材料になる。**16.3 の「入口が見つかった」は、この節と必ずセットで読むこと。**

---

### 16.5 あすけんの推定方式を確認し、4章の競合認識を裏づけた

朝活くんの「あすけんも体重によって補正されるやつだったはず」という指摘を受け、あすけんの公式情報(asken.jp/info/211)を確認した。

**結果: 「体重 × 基礎代謝基準値 × 身体活動レベル」の式ベース。** 体重が変われば推定値も動くが、それは**式に体重が入っているため**で、実測の収支から逆算しているわけではない。

これは 10.5 の検証結果と整合する。

| 比較 | 差 | 10.5 の判定 |
|---|---|---|
| 式ベース推定 vs 実測TDEE | 約125kcal/日 | 誤差範囲内 |
| Garmin 推定 vs 実測TDEE | 647kcal/日 | 差別化の根拠になる |

→ **4章・10.5 の差別化ロジックは競合されていない。** あすけんが押さえているのは式ベース推定で、実測TDEE(体重の実推移からの逆算)とは別物。4章の「入力レイヤーでは勝てない。評価レイヤーで戦う」はそのまま維持できる。

**4章の本文は書き換えていない。** 競合認識の前提が一次情報で裏づいた、という記録を本章に置くにとどめる。

---

### 16.6 判定の停止条件(事前コミット)

**同じデータで、1日に3回読みが変わった。**

| 時点 | 読み |
|---|---|
| 朝 | 実質回答が3/3で揃い、順風に見えた |
| Hana 3通目 | 停滞が入口だと分かり、仮説が復活した(16.3) |
| Hana 4通目 | 痛点ではあるが、答えが噛み合っていない(16.4) |

**n が小さく、1件ごとに振り回されている。** 13.2 が rule of three で示した「小さい n では何も棄却できない」の実演にあたる。

→ **実質回答が追加で3件揃った時点、または 9/3(需要検証2本目の公開日)の、どちらか早い方で判定を下す。延長しない。**

**これは 15.5 で問題にした「ゲートの実効性」への対処でもある。** 15.5 は「文書にゲートを書くだけでは踏み越えを防げないことが3回の実績で示された」と結論している。今回は**判定日を先に固定する**形にした。**停止条件を後から動かさないことが条件**で、動かした場合は 15.5 の4件目として記録する。

---

### 16.7 接触の設計から分かったこと(方法論)

- **共有ベースの公開リプでは判定材料が取れない。** にすいは共感返しで会話が終わった。一方、DM で明示的に質問した朝活くんからは判定できる回答が来た。**15.8 が DM を選んだのは結果的に正しかった**
- **Hana への2回目で「最初につまずくこと」を聞いたのは設計ミス。** 初心者フェーズの問題だけを引き出す形になり、**TDEE のギャップが効く2〜3ヶ月目の停滞フェーズを構造的に除外していた**。3通目で停滞の話が出たのは、こちらの質問設計ではなく相手が自分から段階の話をしたため。**聞く相手が段階を持っている場合、どの段階の話かを先に指定する**
- **接触の設計そのものは効いた。** 15.8 で決めた3点(「15分ください」ではなく質問を本文に直接書く / URL・登録フォームを入れない / 相手の専門領域を聞く)を守った結果、**5名に接触して数時間で3件の実質回答**が得られた。記事のビューが数週間で0シグナルだったこと(15.1・15.2)と対照的で、13.4-(1) が「到達ゼロでも成立する検証」とした狙いは機能している

---

### 16.8 #213 のゲート条件「インタビュー5人」の扱い(提案)

現状は**接触5名・実質回答3名**(うち1名は顧客集団の観測なので実質 n>1)。

前提として、このゲートの役割は 14.1-(2) で既に移してある — **実装のゲートとしては役目を終え、配布のゲートとしては生きている**。ここで問うのは、配布のゲートとしての「5人」を**何で数えるか**。

| 案 | 内容 | 評価 |
|---|---|---|
| **A(推奨)** | **「接触5名」で人数条件を満たしたとみなし、中身の判定は 16.6 の停止条件で下ろす** | 15.8 が「5人揃うのを待つ設計が3週間の停止を生んだ」として母数より着手を選んだ判断と一貫する。判定材料は既に出ている(16.1〜16.4) |
| B | 実質回答が5件揃うまで待つ | 未返信2名に依存する。**待ち時間の下限が相手側にあり、15.8 が壊したはずの構造に戻る** |
| C | ゲートを人数から外し、「判定が下りたか」だけで見る | 実態には最も近いが、**条件を後から緩める**形になり、15.5 で記録した踏み越えと見分けがつかない |

**推奨は A。** 人数条件は「接触」で満たし、中身の判定は 16.6 で先に固定した停止条件に委ねる。結論は C とほぼ同じになるが、**条件を緩めたのではなく「数える対象を接触に定めた」**という形で残るため、15.5 の指摘に抵触しない。

**#213 本文の更新案**(A を採る場合):

> 着手ゲート(インタビュー5人)は、実装のゲートとしては 14.1-(2) で役目を終えた。配布のゲートとしては、**接触5名をもって人数条件を満たしたものとする**(2026-08-26、接触5名・実質回答3名)。配布の可否は 16.6 の停止条件に従って判定する。

**決定は本人が行う。本節は提案にとどめ、#213 本文はまだ更新していない。**

---

### 16.9 その後に分かったこと(2026-08-26 夜)

16.1〜16.8 を書いたあとに調べて分かったこと。**16.1 の記述が1件はっきり誤りになり、16.6 の停止条件にも算数の穴が見つかった。** 16.1〜16.8 は書き換えず、ここに追記する形で残す。

#### (1) 16.1 の KEN の評価を取り下げる

16.1 は未返信の KEN について「唯一『停滞は飢餓状態』と自己診断しており、課題認識がある側なので、判定材料としては濃い」と書いた。**これは取り下げる。**

プロフィールに「※Amazonアソシエイトとして適格販売により収入を得ています」とあり、投稿はほぼ全て自分のブログ(cutting85to75.com/blog)へのリンクに繋がる **アフィリエイト集客アカウント**だった。投稿の型も「共感を呼ぶ気づき ＋ リプ欄に記事リンク」で揃っている。

つまり「停滞期は体が飢餓状態」は**本人の困りごとの表明ではなく、コンテンツである可能性が高い**。ダイエット領域で書くなら「飢餓状態」は刺さる語彙にあたる。

**教訓: ツイート単体で候補を拾うと、アカウントの性格を見落とす。** もう1名のしょーた(@zelvo_5044)も、プロフィールは「歌うことが好き/ライブ観戦が好き」でダイエットは付随的な話題だった。**次から候補選定にプロフィールの確認を含める。**

**16.3 への影響は限定的。** 16.3 は「アマチュア(KEN)もプロ(Hana)も停滞を代謝適応で説明している」を根拠のひとつにしていたが、KEN 側がコンテンツだとすると**「市場のデフォルトモデル」の証拠としてはむしろ強くなる**(書き手が「刺さる」と判断して選んだ語彙、という意味になるため)。ただし**「困っている当事者の声」としては数えられない。**

#### (2) 16.6 の停止条件が構造的に発火しない

16.6 は「実質回答が追加で3件、または 9/3 のいずれか早い方」とした。しかし**未返信は KEN としょーたの2名のみで、満額でも3件に届かない。** 16.1 の「返信が来た場合は追加3件に数える」は、算数上その条件を満たしえない。

追加接触をしない限り、**実質「9/3に判定」の一択**になっている。

**これは条件を破ったのではなく、条件どおりの帰結として記録する。** 16.6 は「停止条件を後から動かさないことが条件」としており、ここで3件を2件に下げると、まさにその禁じ手にあたる。**下げずに、9/3 で判定する。**

#### (3) DM は検索で見つけた相手には使えない(構造的な制約)

16.7 は「DM で明示的に質問した相手からは判定できる回答が来た」と書いたが、**DM を送れる相手が限られる**ことが分かった。

X は既定でフォロワー以外からの DM を受け取らない。成功した2件(朝活くん・Hana)は**どちらも相手から先にフォローされていた**ため送れたもので、DM が開いていたわけではない。実際に候補2名(まんごー@mango_japon・根岸拓也@NG_takuya)を確認したが、両方とも DM アイコンが無かった。

既存フォロワーの該当層は 15.6 の監査で19人中2人 — **その2人を既に使い切っている。**

| 経路 | 送付 | 実質回答 | 使える相手 |
|---|---|---|---|
| DM | 2 | **2** | 相互フォローのみ |
| 公開リプ | 3 | 1 | 誰にでも送れる |

→ **DM の回答率は高いが、宛先を選べない。** 検索で見つけた層に届けるには公開リプ経由でフォローを得る2段階が要り、9/3 までの日数では現実的でない。**16.7 の「DM を選んだのは結果的に正しかった」は、選べる相手がいた場合に限る**という条件付きに読み替える。

#### (4) 6件目の接触(判定材料ではなく、次のターゲット定義のヒント)

まんごー(@mango_japon)に公開リプを送付した。「ダイエット出だしはスッと痩せられるが停滞期来てからは1ヶ月2kg痩せるのがなかなかしんどい」という投稿が、**狙っていた2〜3ヶ月目の停滞フェーズに合致**したため(16.7 で「設計ミス」として記録した、初心者フェーズを引き出してしまう問題の裏返しにあたる)。

プロフィールに「**ダイエットアドバイザーはチャッピー**」とあり、ChatGPT をダイエット相談に使っている。リプでは共感に加えて「停滞期のとき、なんてアドバイスされるんですか?」と聞いた。

**16.6 の停止条件には数えない。** 判定材料ではなく、「**AI にダイエット相談をしている層が、停滞期に何を言われているか**」を知るための接触。16.4 で「出せる答えが現場の解き方と噛み合っていない」と書いた、その現場側に AI が入っている場合の観測にあたる。

---

## 17. noteアクセス状況6週分の評価(2026-08-21 の分析・2026-08-27に遡って記録)

**この章は 2026-08-21 に書かれたまま、PRが作られず未マージで残っていた分析を、2026-08-27 に遡って記録したもの。** 観測時点は14章・15章(いずれも 2026-08-25)より**前**にあたるため、判定基準に関する部分は既に置き換わっている(17.3 に対応表を置く)。

**それでも残す理由**: 17.1 の**記事ごとの到達の分析**は、14章・15章・16章のいずれにも無い。15.1 が持っているのはチャネル単位の合計値で、「どの記事が、どう伸びて、どう減衰したか」はここにしか無い。

---

### 17.1 記事の伸び方の実績(7章補足の更新)

**旧記述(2026-08-05時点)**: 「note全体で週20〜30ビュー。1本目の課題提示型記事が3週連続で読まれ続けている一方、週次レポート形式は公開週のみで失速している。**課題提示型を主軸にし、レポート形式は比率を下げる。**」

**更新後**: 週合計は 27〜29 → 59〜62 に増えたが、**増えたのは在庫本数であって1本あたりの到達ではない**。公開直後の初速(ビュー/日)は、ストック記事 note01 14.5・note05 10.0・note06 9.3・note08 5.5 に対し、週次レポートは note02 3.3・note04 2.6・note07 2.6。**「課題提示型(ストック)を主軸にする」という結論は、初速で3〜5倍という形で裏が取れた。**

**ただし「読まれ続ける(尾を引く)」のは note01 だけで、これはストック記事一般の性質ではなかった。** note01 は公開5週後も週5〜10ビューを引き続け、累計76は全期間の約32%。一方、同じストック記事でも note05(累計33〜35)・note08(累計14、11→3)は公開週にほぼ全部を消化して尾が出ていない。**旧記述の「3週連続で読まれ続けている」は n=1 の観察を一般化したもので、後続2本では再現していない。** note01 固有の条件(初出・Zenn同時公開・固定ポストからの導線)を疑うべきで、**「ストック記事だから尾が出る」という前提でゲートを設計しない。**

**この母数では、ビューは記事の内容ではなく配信を測っている。** 編集面の評価が最も高かった note07 が13ビューで最下位、同じ流れの続きの note09 が26で週次最高と、内容の良し悪しと数字が対応していない(content-ops の 2026-08-04「ビュー数を構成変更の評価指標にしない」と整合する)。

**15.4 との関係**: 15.4 は「note のスキ数は指標に使わず、ビューのみで評価する」と決めたうえで、7章補足のビュー比較そのものは支持していた。本節はその**ビュー比較の中身を6週分に更新した**もので、15.4 の結論と矛盾しない。ただし 15.4 が引用している7章補足の文面は**更新前のもの**である点に注意(引用としては正しい)。

---

### 17.2 分子(登録数)の汚れ — 14.5 の判定に効く未処理項目

**この節の内容は、8/21時点から未処理のまま残っている。**

βテスター募集(note08・Xの常設導線)と需要検証の事前登録は**同じフォーム**を使っており、区別は「流入元」欄のプレフィルだけで付けている。**note08(8/13公開)とXの常設導線が素のURLのまま**で、そこから来た登録は流入元が空欄で溜まる。

- **14.5 の判定2(需要)は、記事ごとの登録数を分子に取る。** 空欄が混ざると記事ごとに下ろせない
- **現状は登録0件なので実害は出ていない**(14.1-(3))。ただし**登録が入り始めた瞬間に効く**ため、導線の作り直し(14.2-A)と同時に処理すること
- content-ops 側では、note11(9/3)のフォームURLに `note-tdee` とは別のプレフィル値(`note-plateau` 等)を入れる方針が公開前TODOに入っている。**note08 と X の常設導線は、まだ素のURLのまま**

---

### 17.3 この章のうち、14章・15章に置き換わった部分

**再適用しないこと。** 元の 2026-08-21 の分析には以下も含まれていたが、いずれも後の章で差し替わっている。

| 8/21 の記述 | 置き換えた章 | 内容 |
|---|---|---|
| 「PV200 という下限を 9/3 より前に引き直す」 | **14.5** | 「累計事前登録10人」という絶対数の基準そのものを破棄し、判定を2本立て(配布の継続率 / 記事の需要)に差し替えた。PV閾値も 100 になっている |
| 「告知インプ → note PV → 事前登録 の3段のどこで落ちているかを測る」 | **15.1** | 実測が出た(フック投稿60imp → リンク投稿35imp・−42% → note記事12view)。**CTR 34% は低くない**ため、制約は分母側にあると結論している |
